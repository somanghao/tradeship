# changelog — tradeship · 2026-08-16

> [changelog.md](changelog.md)에서 시점 경계로 분리한 과거분(08-16 하루, 3건).
> 상한 40KB를 넘어 분할했다. 더 과거는 [changelog-2026-08-15.md](changelog-2026-08-15.md) ·
> [changelog-2026-08-early.md](changelog-2026-08-early.md).

## 2026-08-16

- **[2026-08-16] #balance #combat #ui #dashboard** — **테스터 셋을 다시 투입해 "고쳤다"를 검증했고, 그중 하나가 안 고쳐져 있었다.**
  - ★ **사건 판정이 실제로는 안 먹히고 있었다.** 아프리카 테스터가 희망봉 16구간을 뛰어
    6구간(37.5%)이 여전히 사건 0건임을 보고 잡았다. `update()`가 판정 **전에**
    `eventDone = true`로 잠그고, 다음 자리를 잡는 `resumeVoyage`는 사건이 났을 때만 불린다 —
    **첫 판정이 잔잔하면 남은 세 장이 통째로 버려졌다.**
    ★ 왜 못 봤나: `rollSeaEvent`를 직접 네 번 불러 재고 "93%·나쁜일 1.11회"라 적었다.
      그건 규칙 함수의 값이지 **게임이 밟는 경로**가 아니다. 이번엔 브라우저로 실주행해 쟀다.
      실주행 8구간씩 — 루안다~모잠비크 사건0 37.5%→**13%**·구간당 0.75→**1.75건**,
      킬와~소팔라 0.25→0.63건. 긴 항로가 짧은 항로의 **2.8배**(전에는 같았다).
  - **원양이 근거리보다 못 벌던 것**(C-7)을 고쳤다. 테스터 셋이 각각 −2,847 / −5,911 / −367닢을
    재 왔다. 원인은 사다리가 아니라 **대량거래 벌점이 그 사다리를 도로 깎는 것**이었다 —
    같은 은 항로가 45개면 실거래 +45%인데 200개면 **+5%**. 이 벌점은 "한 번에 크게 사고 파는 쪽"을
    때리는데 **원양은 그럴 수밖에 없다**(왕복 48~66일이라 나눠 팔면 몇 해가 걸린다).
    근거가 이쪽을 가리켰다 — `caribbean-evidence.json`이 포르토벨로를 두고 "페리아에서 팔린
    물건은 지협을 넘어 페루 부왕령 전체로 퍼졌으므로 **소비권이 이 항구 규모보다 훨씬 넓었다**"고
    적어 두었다. 깊이를 도시 규모로만 재는 것이 애초에 틀렸다 → `MARKET.gateDepth 2.5`
    (원양 관문 29곳). 은 항로 실거래 5%→**21%**, 근거리는 15% 그대로. 시뮬 12회로 근거리
    경제가 안 움직임을 확인했다.
  - **적의 이름만 갈고 깃발을 안 갈았다** — 명 수군 순찰선이 부르봉 백합기를, 왜구 대선단이
    오스만기를 달고 있었다(`localize()`가 `flag: skin.flag ?? base.flag`인데 `FOES`에 flag가 없었다).
    아홉 권역 45줄에 국적기를 붙였다. 조우 문구도 셋으로 갈랐다 — 전에는 "왜구에게 사략 허가장"이
    붙고 명 수군 정규 순찰선도 같은 문장이었다(무법자 · 왕의 배 · 사략선).
  - **지도가 캔버스의 31%만 쓰던 것**을 고쳤다(이것도 내가 만든 부작용). 패널을 피해 가용폭이
    1104가 되자 `1104/400 = 2.76` → 정수배 2로 떨어졌다. 그런데 **도시가 실제로 쓰는 폭은
    292~354칸뿐**이라 그 범위로 재면 3이 나온다 → `setViewSpan(lo, hi)`. 대서양(가장 넓다)에서
    scale 2→**3**, 푼샬 x=35·노브고로드 x=1097 둘 다 안 가림. 패널 268→240px, 그 대가로
    원양 항로 이름이 "살바도 르"로 잘려 CSS도 함께 손봤다.
  - **해적 소굴 셋(토르투가·쌍서·계롱)의 선원이 이탈리아 이름**이던 것 — 깃발이 `pirate`라
    나라를 알 수 없어 `latin`으로 떨어졌다. `PIRATE_NAME_POOL`로 **그 바다**를 보게 했다.
  - **대시보드에 선박 탭 신설.** 배가 60척인데 한눈에 견주는 화면이 없었다(조선소는 그 항구 것만,
    그마저 60줄). 바다별 선단·명부·화물칸↔값 산점도·해금 사슬·전통 조선지 어긋남·공업력 분포.
    계측이 `sellsShip`을 항구마다 그대로 물어보게 했더니 켜자마자 C-2가 숫자로 드러났다 —
    **사선·세키부네가 155곳에서 지을 수 있고 그중 152곳이 전통 조선지 밖.**
  - **지도 PNG에 판 번호**를 붙였다. manifest는 `no-store`로 읽는데 그 안의 그림은 캐시된다 —
    `serve.py` 아닌 서버로 띄우면 다시 뽑아도 옛 지도가 뜬다. `gen-map-png`가 판을 올린다.
  - **동아시아 선박을 나라별 파일로 갈랐다**(`ships-{ming,joseon,japan}.js`). 세 나라를 동시에
    조사·확장할 때 같은 줄에서 부딪히기 때문이다. `ships.js`는 합치기만 한다.
  - `tools/check-map.py`가 인자 없이 부르면 죽어 있었다(지운 `.webp`를 찾았다).
    **시흐르에서 출항 단추가 화면 밖**(y 941·창 900)이던 것도 고쳤다 — 출항은 그 화면에서
    나가는 유일한 길이라 안 보이면 항구에 갇힌 것처럼 읽힌다(패널 아래 sticky).

- **[2026-08-16] #map #balance #combat #memory** — **테스터 다섯이 돌아왔고, 그들이 잡은 것 넷을 고쳤다.**
  - **지도를 파일로 굳혔다.** 좌표는 `regions/<권역>/geo.js`에 박혀 바뀌지 않는데 지도는 매 부팅
    그 고정된 좌표로 다시 그리고 있었다. `tools/gen-map-png.mjs`로 아홉 장을 뽑아
    `assets/map/*.png` + `manifest.json`(9/9 물림). 사람이 그린 지도가 오면 같은 이름으로 덮으면 된다.
  - 굳히려고 보니 **그림 자체가 틀렸다.** `auto.js`가 뭍을 기본으로 두고 물을 파는 구조라
    여덟 바다가 전부 "항로를 따라 난 운하"였다(바다 23~41% — 그 파일 주석이 스스로 경고한 상태).
    뒤집어 대양을 열고 대륙을 계단 사각형으로 쌓았다. 바다 40~79%. 곁가지로 셋:
    ① `landmass` 경계가 **안쪽으로** 물러나 계단 사이가 줄무늬 바다로 벌어졌다(남미가 가로줄 친 대륙)
    ② 지중해는 섬을 물길보다 **나중에** 그려 사르데냐·시칠리아가 판 물길을 도로 덮었다(항로 육지 75%)
    ③ `temperate`의 `desertY`는 "그 선보다 **남쪽** 전부"라 동아시아 육지가 통째로 모래색이었다
       — 광저우가 사막에 있었다. 북이 마르는 바다이므로 `extra`로 직접 가른다
    ④ manifest를 붙인 순간 `gen-map-png`가 **자기가 만든 PNG를 다시 저장**하는 순환이 생겼다
  - **검수기 둘의 잣대를 맞췄다**(`check-map.py` ↔ `mapcheck.html`). 지중해(내해) 기준으로 만든
    "육지 60% 넘으면 반려"가 대륙 연안을 부당하게 걸러내고 있었다 → 바다 20~80% · 항로 3px→34% ·
    항구 반경 2px→6px + 섬 예외 · 색 수는 경고로. 9/9 통과.
  - **시세 사다리를 세웠다.** 테스터 셋이 서로 다른 바다에서 같은 병을 잡아 왔다 —
    포토시 360 → 파나마 291(광산에서 멀어질수록 은이 싸진다), 세계 최저여야 할 엘미나 금이 아심보다 비쌈.
    원인 ① 흔들림이 도시마다 따로 ±15%씩 놀아 사다리(SPREAD로 5.6%까지 눌린)를 뒤집었다
    → 공통 시황 ±12% + 도시 사정 ±3.5%로 갈랐다. ② 두 권역이 각자 "세계에서 가장 싼 은"을 주장했다
    → 파나마 0.66 · 포르토벨로 0.70 · 놈브레데디오스 0.74로 지리 순서를 회복. `SPREAD` 0.62 → 0.70.
    역전 은 4~5/8 → **0/8**, 금 1/5 → **0/5**. 시뮬 12회 10항차 파산 **11회 → 0회**.
  - **지도 오른쪽 항구를 누를 수 없었다.** 캔버스가 무대 전체 폭에 그려지는데 `#map-side`가
    268px를 덮는다 — 창 1920px 미만 전부 해당이고, 가려진 자리를 누르면 **엉뚱한 항구로 출항**했다.
    `main.js: setInsetRight()`로 지도 씬에서만 물린다.
  - **아홉 바다가 한 얼굴이었다.** `ENEMIES` 다섯이 원래 지중해를 그린 것인데 전 세계 공용이라
    홍해에 프랑스 프리깃, 대만 해협에 바르바리 기함이 떴다. 각 권역 `npc-pirates.js: FOES`(티어 1~5)를
    두고 `pickEnemy(rand, regionId)`가 얼굴을 입힌다 — **수치는 한 줄도 안 건드렸다.**
  - **서른 날 항해가 이틀 항해와 같은 위험이었다.** `startVoyage`가 항해 한 건에 `eventAt`을 한 번만
    굴려서 요율 11.0을 적어도 상한이 1건이었다. 8일에 한 번씩 넉 장까지, 뒤로 갈수록 눅인다
    (`rollSeaEvent`의 `damp` 1·0.62·0.45·0.34). 4일 나쁜일 0.27회 ↔ 44일 1.11회.
    사건 뒤 항해를 잇는 코드가 네 군데 흩어져 있던 것도 `resumeVoyage()`로 모았다.
  - **메모리 품질 패스** — `claude-memory.md`가 아직 "지중해 교역기"였고 "지도는 2차까지 반려해
    manifest가 비어 있다"는 **틀린 결론**이었다(P6). 아홉 바다·권역 폴더 구조를 반영하고,
    `content/city-evidence.json`·`route-evidence.json`을 가리키던 **dead pointer 13곳**을 정정했다
    (코드는 이미 `content/regions/<권역>-evidence.json`을 읽고 있었고 문서만 낡아 있었다).
    "다음 후보" 절 10줄이 `UNIMPLEMENTED.md`와 중복이라 무손실 이관 → 15,350B(상한 이하).

- **[2026-08-16] #balance #combat #events** — **돌발 이벤트·전투 보상 감사 — 세계가 열 배가 됐는데 보상은 지중해 한 바다 시절 그대로였다.**
  - **먼저 잴 도구부터 만들었다 — `tools/sim-events.mjs`.** 초·중·후반(200 / 4,000 / 20,000닢) 상태를 실제로 만들어 화물을 싣고, `jettisonCargo`·`banditRaid`·`payToll`·`pickEnemy`·`pirateEnemy`를 **실물 함수로** 돌려 한 건의 크기를 **자산 대비 %**와 **항차 순이익 대비 배수**로 낸다. 씬에 박힌 값(표류물·폭풍)만 `MIRROR` 블록에 거울로 두었다.
  - **잡아낸 것 ① 전리품이 성장 사다리를 통째로 건너뛰었다.** 세기 1 좀도둑 하나 = 금화 300~1,160 + 나포선 ≈ **시작 자산의 573% · 초반 항차 순이익의 39배**. 첫 배로 하나만 잡으면 코카(1,100)를 샀다. 원인은 전리품이 **진 자의 크기**로 정해지는데 뜻은 **이긴 자의 크기**로 읽힌다는 것 — 해적을 가난하게 만드는 대신 **옮겨 실을 수 있는 양**에 상한을 걸었다(`state.js: spoilsCap/capSpoils/capLoot`, `data.js: SPOILS_SHARE 0.30 · TAIL 0.12 · FLOOR 60`). 상한 위로 꼬리가 남아 현상금 큰 놈이 여전히 더 값나간다. 결과: 초반 119% · 중반 28~107% · 후반 6~47%. **`pickEnemy`(떠돌이)와 `world.js: pirateEnemy`(명부) 둘 다** 같은 규칙을 통과한다.
  - **잡아낸 것 ② 낡은 바사가 브리간틴(4,200닢)을 통째로 끌고 갔다.** 나포선에 **나포 선원**을 요구한다(`data.js: PRIZE_CREW 0.35` · `state.js: spareCrew/prizeCrewNeed`). 모자라면 해체값만 받고 그것도 상한을 탄다. 씬은 이미 "끌고 갈 인원이 없어 해체해 팔았다"는 갈래를 갖고 있어 화면 변경이 필요 없었다. **불가능이 아니라 대가다** — 바사도 열둘을 태우면(선실 16) 브리간틴을 몰고 간다. 실측: 선원 6명 → 해체 204닢 / 12명 → 선단 편입.
  - **잡아낸 것 ③ 세계를 넓히자 "대박은 사건에서 나온다"가 조용히 죽어 있었다.** `SHOCK.events[].perDay`는 **세계 전체** 하루 발생 건수라, 도시가 16 → 175가 되며 한 도시가 사건을 겪는 주기가 20개월 → **216개월**로 벌어졌다. 사건이 드물어진 게 아니라 **닿을 수 없는 곳에서만** 일어나게 된 것이다. `SHOCK.densityBase 16`으로 도시당 빈도를 지중해 시절과 맞췄다(`state.js: rollShockEvents`).
  - **곡선 대조(같은 트리에서 A/B · 24시드×90항차)** — 환산 전 항차 ROI 중앙값 7.1%(목표 밴드 8~16% **아래**)·완주 258항차·적자 31.0% → 환산 후 **12.5%**·**541항차**·적자 21.4%. 20시드 자산 곡선에서는 환산 전 **20항차를 넘긴 판이 0/20**(전부 낡은 바사에서 끝)이었고 환산 후 5/20이 넘겨 3판이 90항차 75,432닢까지 갔다. **`WORLD-INTEGRATION-TODO.md` §6("시뮬이 열 항차에서 멈춘다")의 절반은 이것이었다** — 중앙값은 여전히 11항차라 나머지 절반(수리비·시작자본)은 남아 있다.
  - **근거·검사기**: `voyage-evidence.json`에 `spoilsVsAssets`(밴드 15~100% · verdict gameplay)와 `shockDensityPerCity`를 추가하고 `check-voyage.mjs`가 대조하게 했다 — 상한 배선이 끊기거나(`world.js`에서 `capLoot` 제거) 밀도 환산이 사라지면 실패한다. 실측 47%로 밴드 안.
  - **대시보드 해적 탭은 상한 전 정가를 보인다** — 배들끼리 비교하는 자리라, 재는 동안만 금고를 크게 잡아 상한이 안 걸리게 했다(공식을 재구현하지 않기 위해).
  - **못 고친 것(씬 소유)**: 표류물(금화 60~300 + 화물 3~11개 ≈ **1,174닢 고정**)과 폭풍 선체 피해(6~19pt ≈ 182닢 고정)가 `scenes/map.js`에 박혀 있다. 고정액이라 초반엔 자산의 **587%·91%**, 후반엔 6%·1%다. 표류물은 7% 가중치라 **14항차에 한 번** 뜨는데 초반 판이 11항차에 끝나므로, 사실상 절반의 판이 "코카를 주는 이벤트"를 만난다. 값을 `data.js`로 빼는 것이 다음 수.
  - 검증: `test-rules`·`test-payroll`·`test-world`·`test-tavern`·`check-voyage`·`check-evidence`·`check-prices`·`check-routes`·`check-wages`·`check-world`·`check-architecture` 전부 exit 0.
