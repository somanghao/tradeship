# 아직 없는 것 — 미구현 목록

> 세계를 아홉 바다로 넓히면서 **데이터는 들어왔는데 규칙이 없는 자리**가 여럿 생겼다.
> 여기가 그 목록이고, 하나씩 지운다. 우선순위는 **"플레이어가 지금 못 하는 것"** 순이다.
>
> 상태 표기: `없음`(아예 안 만들었다) · `데이터만`(값은 있는데 게임이 안 읽는다) · `절반`(되다 만다)

---

## A. 사용자가 명시적으로 요청한 것

> ★ **2026-08-20 — A-1·A-2·A-3의 사양이 값까지 확정됐다.** 소설 `story/`의 교차 검수가
> 이 셋을 "후반이 없다"는 하나의 구멍으로 묶고 사양서를 냈다 — **정본은 [story/GAME-LINK.md](../../story/GAME-LINK.md) §8**.
> 거점 종류별 가격·유지비(연 6%)·공업력 승급 3중 비용(금화·자재·공기)·등록 플래그·정박 유지비(A-1b 신설)까지 있다.
> 구현할 때 여기서 값을 가져온다. ★ **A-1 창고의 "보관 중인 화물은 `state.impact`를 누적시키지 않는다"** 한 줄이
> 지금 게임에서 성립하지 않는 *"짐을 쪼갠다"*를 처음으로 전략으로 만든다(`MARKET.decay 0.68`이라 나눠 팔아도 벌점을 못 피한다).

### A-0. 세이브/로드 — ✅ **해소**(2026-08-22) ★A-1보다 먼저였다
`js/` 전체에 **`localStorage` 호출이 0건**이라 새로고침 한 번에 판이 통째로 사라졌다.
소설 검수가 이것을 A-1·A-2보다 **앞으로** 올렸고 근거가 숫자였다 —
캠페인 규모가 **총 항차 330~380 · 총 항해일 900~1,100**(`story/level/economy.md §1`)이라
**한 세션에 끝나지 않는다.** 세이브가 없으면 이 게임의 후반은 아무도 못 본다.
- **`js/save.js`** — `state`를 **통째로** 직렬화한다. 저장 대상 목록을 따로 관리하면
  **필드가 늘 때마다 세이브가 조용히 낡기** 때문이다(`Set` 둘만 배열로 눕혔다 되돌린다).
- **항구가 세이브 포인트**다 — 바다 위 상태(`sailing`)는 씬의 모듈 변수라 어차피 안 담긴다.
  그 사실과 규약이 맞아떨어진다. 입항할 때마다 자동 저장.
- ⚠️ **타이틀이 닫히기 전에는 저장하지 않는다**(`main.js: gameStarted`). `boot()`이 타이틀보다
  먼저 `go('port')`를 부르므로 그대로 두면 **부팅할 때마다 새 판이 옛 판을 덮어썼다** — 실제로 그랬다.
- 타이틀 맨 앞에 **이어하기**가 뜬다(`2일차 · 강진 / 낡은 바사 · 99닢 · 좌수영의 군관`).
  판 형식이 바뀌면 `VERSION` 불일치로 **조용히 무시**한다 — 억지로 이어 붙이는 것보다 낫다.
- 크기는 한 판에 **약 295KB**(도시 265곳 시세표가 대부분이다). `localStorage` 한도 안이다.

### A-1. 항구 거점 구매 — ✅ **해소**(2026-08-22)
**다섯 종류를 세운다**(`data.js: HOLDINGS` · 규칙 `state.js` · 화면 `port.js: holdingCard`) —
임차창고(320+size×90 · 보관 20) → **창고**(size×8,000 · 보관 60) → **상관**(size×20,000 ·
입항세 **−1.5%p**·하한 1% · 시장 깊이 **+50%**) / **조선대**(40,000+size×10,000 ·
중고 매물 +1 · 수리 −30%) → **부두**(60,000+공업력×40,000 · **`industry` +1**, 상한 3).
- ★ **창고에 둔 짐은 `state.impact`를 누적시키지 않는다**(`state.stored`) — 지금 게임에서
  성립하지 않던 *"짐을 쪼갠다"*가 이것 하나로 처음 전략이 된다.
- **자산이면서 고정비다** — 들인 돈의 **연 6%를 30일마다** 그 항구에서 문다. 못 내면 **압류**되고
  창고에 둔 짐도 함께 넘어간다. 후반이 "그냥 부자"가 아니라 **더 벌지 않으면 지킬 수 없는** 구조가 된다.
- 실측(부산포·전부 세운 뒤): 세율 3.30% → **1.80%** · 시장 깊이 90 → **135** ·
  공업력 2 → **3** · 수리 14 → **10닢** · 보관 **60칸**.
- **A-2의 첫 계단이 여기 있다** — 부두가 `industryOf`를 +1 하므로 `tierNeeded`·`sellsShip`·
  `shipPriceAt`·`usedListings`가 전부 따라 움직인다.

**해소 전에 무엇이 문제였나**(재발 진단용): 금화의 쓸 곳이 배·대포·개장·선원뿐이라
배 꼭대기를 사고 나면 **돈이 갈 데가 사라졌다**. 완주 플레이가 *"끝을 만드는 것이 규칙이 아니라
지루함뿐"*이라고 적은 자리다.
- **무엇**: 항구마다 창고·상관을 산다. 그 항구의 시장 깊이·보관·거래 특권이 붙는다.
- **있는 것**: `content/asset-evidence.json`이 사료 앵커를 갖고 있고(집세·주택값이 선원 연봉의 몇 배인가),
  대시보드 **항구** 탭이 그것을 게임 화폐로 환산해 보여준다. **게임 기능만 없다.**
- **걸리는 것**: `state.js`에 소유 상태 필드, 항구 씬에 매입 UI, 세이브 대상 추가.

### A-1b. 정박 유지비 — ✅ **해소**(2026-08-22)
**정박 중인 날이 공짜였다.** 날은 `advanceDays()`에서만 가는데 그것은 항해할 때만 불린다 —
항구에 서 있는 동안은 급여도 보급도 유지비도 나가지 않았다. 소설이 게임과 **가장 크게 어긋난 자리**로
*아덴 억류 · 반다 반년 · 아바나 한 해 대기*가 게임에서는 아무 대가가 없는 일이었고,
그 규약이 없으면 급여 위기 네 장면이 통째로 사라진다.
- **`state.js: waitDays(n)`** — 항구에서 며칠을 보낸다. 세계가 돌고(시세 회복·사건) 삯이 쌓인다.
  보급만 **절반**이다(뭍에서 사 먹는다). 계산은 `voyageCost`를 그대로 써 값이 갈리지 않게 했다.
- **`dischargeCrew()` / `recallCrew()`** — **창고가 있는 항구에서만** 사람을 내려놓을 수 있다(A-1과 짝).
  내리면 삯이 멎고, 다시 태우는 값은 새로 뽑는 값의 **절반**(그 사람들이 거기 그대로 있으므로).
- 화면: 항구에 「정박」 카드 — `사흘 머문다 (−145닢 · 삯 73 쌓임)` · `열흘 머문다 (−483닢 · 삯 242)`.
- ★ 이제 **사람이 곧 시계**다. 기다리는 것이 공짜가 아니면 *"언제 떠나나"*가 판단이 된다.

### A-2. 공업력 성장 → 히든 배 해금 — ✅ **해소**(2026-08-22)
**자재를 실물로 실어 와야 오른다** — 그래서 승급은 돈 쓰는 일이 아니라 **항로를 짜는 일**이다
(`data.js: YARD_UPGRADE` · 규칙 `state.js` · 화면 `shipyard.js: yardUpgradeCard`).

| → | 금화 | 공기 | 자재 |
|---|---|---|---|
| 1 | 9,000 | 90일 | 목재 90 |
| 2 | 34,000 | 180일 | 목재 240 · 철 90 |
| 3 | 96,000 | 360일 | 목재 480 · 철 220 · **구리 60** |
| 4 | 240,000 | 540일 | 목재 900 · 철 420 · 구리 120 |

- **대가 넷** — 유지비 `등급×8닢/일`(놀려도 나간다) · 입항세 `+0.5%p/등급` ·
  **공사 중에는 그 항구의 신조·중고 매물이 0**(부두가 제 일을 못 한다 — 돈보다 이것이 크다) ·
  자재값 상승.
- **`ironclad` 철갑 거북선(tier 4 · 염포 · 24,000닢)**을 얹었다. 기존 거북선(tier 3)은 **그대로** 둬
  밸런스 회귀가 없다. 조선 항구도 `tierNeeded 3`이라 **염포를 3까지 올린 플레이어만** 본다.
  ★ 여수는 나무가 남고 쇠가 없으며 염포는 그 반대다 — **둘이 물려야 이 배가 나온다.**
  구리는 조선에 산지가 없어 **아홉 바다와 끊기지 않는다.**
  화물 40칸에 유지비 44닢/일이라 **어떤 항로에서도 회수되지 않는다** — 후반 금화의 종착지가
  *"안 쓰는 배 한 척"*이 되는 것이 이 게임의 주제와 맞는다.
- 실측(염포 1→2): 34,000닢·180일·목재 240·철 90을 붓고 **공사 중 신조·중고 0**,
  완공 뒤 세율 2.75% → 3.25% · 하루 유지비 8닢.

**해소 전에 무엇이 문제였나**: 도시 `industry`가 **고정값**이라 배 사다리의 꼭대기가
**첫날부터 정해져** 있었고, 거북선이 부산포 공업력 2로 열려 있어 "히든"이 아니었다.
- **무엇**: 거점(A-1)에 투자해 그 항구의 공업력을 올린다 → 더 큰 배를 그 항구에서 지을 수 있다.
- **히든 배**: 공업력을 끝까지 올린 항구에서만 나오는 배. 사용자가 **거북선**을 예로 들었다.
  ★ 거북선은 상선이 아니라 돌격선이다 — 화물칸을 작게, 선체와 백병을 세게 두는 것이 사실에도 맞고
  "히든"이라는 자리에도 맞다. 동아시아 역사학자에게 판단을 맡겨 두었다.
- **걸리는 것**: `SHIPS[].tier`가 도시 `industry`와 맞물리는 규칙은 **이미 있다**(`yardCapable`).
  올릴 수단만 없다. 즉 A-1이 되면 이것은 작다.

### A-3. 지역별 주인공 — `절반` (시작지 선택은 ✅ 2026-08-17) · **차등안 확정**
- 차등은 자산이 아니라 **문서·평판**으로(총가치 ≈1,600닢). 부산포는 **특전 0 · 대가 1**(사무역 금지)이되
  **아홉 시작지 중 유일하게 tier 3 배를 시작 항구에서 짓는다**(industry 2 + 조선 깃발) — 이 비대칭을 깎지 말고 강조할 것. → `story/GAME-LINK.md` §8
✅ **아홉 바다에서 골라 시작한다.** 타이틀에 3열 아홉 칸이 뜨고 고르면 `resetGame(at)`이 판을 그 부두에서
다시 만든다(`js/data.js: START_PORTS` · 화면 `main.js: startPicker`). 기본은 **부산포**(`DEFAULT_START`) —
조선은 사무역을 금해 열린 부두가 하나뿐이고, 그 폐쇄성이 시작지의 성격이 된다(항로가 마포·하카타 둘뿐).
- 시작지는 실클릭으로 고른 아홉 곳이다(막다른 주머니를 피했다) → [wiki/playtest-log.md](wiki/playtest-log.md) §4-1·4-5
- `?start=`(개발용)는 그대로 이기고, 그때는 고르는 줄을 띄우지 않는다.
✅ **시작배가 바다마다 갈렸다**(2026-08-26 · 사용자 지시 *"주인공은 각 지역의 가장 싸구려배로 시작해야지"*).
  전에는 아홉 어디서 시작해도 `hulk` 하나였다 — 지중해 배가 광저우에도 아르갱에도 떠 있었다.
  **그 바다 배의 「삭은」 판을 여덟 종 새로 만들었다**(tier 0 · `leak` 있음 · 시중에 안 나온다).
  지중해의 `hulk`가 원래 그 자리였고 나머지 여덟에 같은 자리를 준 것이다. 선종 **94 → 102**.
  - ★ **제원은 `hulk`에 맞춘다 — 바다마다 다른 것은 얼굴이지 난이도가 아니다.**
    처음엔 그 바다에서 가장 싼 **정품 tier 1 배**를 줬는데, 그것들은 `leak`가 없고 속력이 1.7배라
    **첫 배가 10~16항차 → 1항차**로 무너졌다(10항차 금고 899 → 2,144닢 · 40항차 자산 최대 **+177%**).
    *"돈은 어렵게 벌려야 재미가 있다"*가 최상위 원칙이므로 되돌리고 낡은 판을 만들었다.
    **콘텐츠는 늘고 곡선은 그대로다** — 이것이 이 프로젝트가 두 원칙을 함께 지키는 방식이다.
  - 시작지 고르는 화면이 **어떤 배인지 말한다**(`main.js: startPicker` — 이름·화물칸). 모르고 고르면 고른 게 아니다.
  - 갈래 다섯도 조선 배(삭은 사후선)를 탄다. **군관만 사람을 열넷 데려오므로** 정원이 그만큼이다.
  - 검사 여덟이 이것을 지킨다(`test-rules.mjs` — 아홉이 다 다른가 · 다 물이 새는가 · tier 0인가 ·
    **화물 ×1.20 · 선체 ×1.25 · 속력 ×1.12 안에 나란한가** · 데려온 사람이 다 타는가).

- **남은 것 — 인물·조건이 아직 하나다.** "복건 선주의 아들", "구자라트 상인의 대리인"처럼
  바다마다 다른 시작 **인물·자산·평판**은 없다. 배는 갈렸고 자금은 아홉 다 200닢이다.
- **판단이 필요한 것**: 주인공이 다르면 무엇이 더 달라지나 — 자금·특전·평판·해금까지 갈지.

### A-4. 함선 여러 척 동시 운용 — ✅ **해소**(2026-08-23)
`state.fleet`이 **선종당 1척을 정박시켜 두고 갈아타는** 구조라 여럿을 함께 몰고 나갈 수 없었다.
**`state.consorts`**(shipKey → `{crew, captain}`)가 그 자리다 — 기함 1 + 동행 8 = **9척**(`data.js: FLEET`).
- 대가 넷: 배마다 선원을 따로 태우고 · 정박비 대신 **항해비**를 물고 · **가장 느린 배에 맞춰 느려지고**
  (`fleetSpeedPenalty` — 항해 일수와 **도주 확률에 함께** 곱한다) · 포격에서 피해를 나눠 받다 **격침**된다.
- 실측(9척): 총적재 **1,081칸** · 총선원 313명 · 하루 564닢 · 속력 **−35%**.
- ★ **선장 자리는 뚫려 있고 비어 있다** — `consorts[key].captain`과 `FLEET.requireCaptain(false)`.
  A-5가 채우면 `true`로 올리는 것만으로 "선장 없는 동행"이 막힌다(코드 수정 불필요).

### A-5. NPC 동료 등용 — ✅ **해소**(2026-08-25) · 남은 것은 perk 둘
> ★ 배선이 들어왔다 — `regions/index.js: ALL_MATES`(51명) · 등용/해고(`hireMate`·`dismissMate`) ·
> 항구 카드(`scenes/port.js: mateCard`) · `consorts[].captain` 연결 · **`FLEET.requireCaptain: true`**.
> 계약 모양은 **코멘다**다(`data.js: COMMENDA` — 편무 25% · 쌍무 50%+밑천 · 사료 `commendaSplit`).
> 서술은 [wiki/world-npc.md](wiki/world-npc.md) §동료가 정본.
> **남은 것**: perk 열 중 **`gunUp`·`crewLossOff` 둘**이 아직 안 붙었다(전투 씬 안쪽) ·
> ⑥ `content/regions/*-evidence.json`에 근거를 적는 일.

### A-5(원문). NPC 동료 등용 — `데이터 완료 · 배선 없음`
사용자가 **"npc동료 등용해서 안되면 구현해야 돼"**라고 명시했다. 지금 동행 인물은 **에이미 하나**고
등용·해고 함수 자체가 없다(`state.js` 주석이 그 이유를 적어 두었다 — 에이미는 *주어진 동행*이다).
- **데이터는 아홉 권역이 다 있다**(`js/regions/<권역>/npc-mates.js` · **51명** · id 중복 0 · 2026-08-23 카리브·남미 12명 추가).
  `dashboard/architecture.mjs`에 아홉이 등록돼 **`check-architecture`는 통과한다**(전에는 이것 때문에 실패했다).
  그러나 **`regions/index.js` 배선이 없어** 게임에는 아직 아무 영향이 없다.
- 남은 일: ~~① 두 권역 데이터~~ ✅ ② `index.js`가 `MATES`를 합쳐 내보내기 ③ 등용/해고 규칙(`state.js`)
  ④ 항구 화면의 등용 UI ⑤ `consorts[key].captain`에 연결 + `FLEET.requireCaptain: true`
  ⑥ `content/regions/*-evidence.json`에 근거 ⑦ `dashboard/architecture.mjs` 등재.
- perks 키는 **정해져 있다**(`sailDaysOff·gunUp·meleeUp·hullUp·repairOff·upkeepOff·tariffOff·impactOff·fleeUp·crewLossOff`) —
  새 키를 만들면 규칙이 조용히 무시한다. **에이미보다 세면 안 된다**(에이미는 `tariffOff .35 + impactOff .22`).

### A-6. 권역 패권 — ✅ **해소**(2026-08-23)
끝이 조선 하나뿐이라(`ENDING`) 아홉 바다에 중간 목표가 없었다. 조건 넷(`state.js: hegemonyOf`) —
**모든 항구에 거점** · **상관 3곳** · **최상급 적(등급 5) 격파**(`state.slain`) · **그 바다 최고 tier 배**.
아홉 전부면 두 번째 엔딩 「아홉 바다」. **기존 `ENDING`은 안 건드렸다**(밸런스 회귀 없음).
- 기계 검증: 9/9 달성 가능 · 거점 총투자 **912,630닢** · 30일마다 유지비 4,563닢.
- **조선의 "숨은 항구" 둘**(마포·의주 — `industry 0`)은 `slipway`+`dock` 12만닢이면 공업력 1이 된다.
- ★ **보상으로 돈을 주지 않는다.** 거점이 느는 만큼 유지비가 무거워지는 것이 이 목표의 대가다.

### A-7. 게임 배속 — ✅ **해소**(2026-08-23)
자동 완주 검증이 불가능했다(포격 한 턴 2초 · 사람 속도 클릭). `js/speed.js`가 시간 스케일 정본이다.
포격 한 턴 **2.06초 → 0.27초**(8×) · 항해 연출 2,793 → 365ms. **연출과 대기시간만** 줄인다 —
조준 바늘은 난이도라 제외했고 rAF 루프도 안 건드렸다. 하네스는 `PLAYTEST_SPEED`/`--fast=N`(옵트인).

### A-8. 자동 완주 테스트 — ✅ **해소**(2026-08-23) · 남은 것은 §A-8b
사용자 지시 ①②를 둘 다 돌렸다. 종합 `.playtest/origin-sweep/RESULTS.md`.
- **① 갈래 다섯 × 아홉 바다 — 1,325/1,325 항구(100%) · 거점 1,352개 실클릭 매입 · 항로 외 FAIL 0.**
  사건 **19종 11,255회**(권역마다 17~19종). `interpreter`가 **소설 6부 엔딩 모달을 화면에 띄웠다**.
- **② 동아시아 패권 4/4 완주**(430일·1,796,283닢) — 41/41 항구 · 상관 3 · 왜구 대선단 격파 · 철갑 거북선.
- 러너: `.playtest/origin-sweep/{sweep,run-all,hegemony-run}.mjs` · 갈래별 발견 `out/FINDINGS-<갈래>.md`.
- **하네스 결함 여덟**을 고치고서야 결과가 믿을 만해졌다 — 그중 **초행 배지**(`^이름$`가 `"의주 초행"`을 못 잡음)와
  **창 포커스 경합**(`front:true`)이 다섯 갈래에서 동시에 거짓 미도착을 냈다. → `wiki/playtest-harness.md`

### A-8b. 패권 아홉 바다 확장 — `1/9`
동아시아 하나만 4/4를 밟았다. 나머지 여덟은 **최고선이 tier 3이고 그 조선지가 이미 공업력 3**이라
④ 추가 비용이 0이다(`hegemony-feasible.mjs` 실측). 한 권역 약 6분 · `GOLD=4000000` 권장.
- 고칠 곳 하나: `regionTopShips`가 동률 여럿을 주는 바다(지중해 넷·아프리카 둘·중동 둘)에서
  러너가 `keys[0]`의 사슬만 본다. `topShipAny: true`이므로 **후보를 순회하며 가장 싼 사슬**을 골라야 한다.
- ③은 **자산 하한**이 있다 — 사냥 중 금고가 30,000닢 밑으로 가면 등급 5가 표에서 사라진다(§A-8c).
- 남미는 사냥터 선택이 권역 안 `ALL_ROUTES`만 본다(순회는 원양을 이미 쓴다).

### A-8c. 패권이 화면에서 말을 안 하는 자리 둘 — ✅ **해소**(2026-08-26)
- **파산하면 이미 딴 패권이 되돌아간다.** 거점을 잃는 두 자리(압류·매각)가 이제 `hegemonyLoss()`로
  항해일지에 *"패권이 되돌아갔다 — 41/41 → 40/41"*을 적고, **카드는 나기 전에도 말한다.**
- **조건 ③은 "강해지면"이 아니라 "부자가 되어야" 열린다.** `pickEnemy`에 상수로 박혀 있던 등급표를
  `foeOdds()`로 꺼내 조건 ③ 밑에 *"자산 1,200닢 — 등급 5는 지금 확률이 0이다. 14,001닢을 넘겨야
  두목이 붙는다"*를 적는다. 문턱은 `foeWealthGate(tier)`가 표를 훑어 돌려준다.
- ⚠️ **옛 기록의 "30,000닢"은 틀렸다**(2026-08-26 실측 정정) — 등급 5가 **처음 0을 넘는 것은 14,001닢**이고,
  30,000은 그 확률이 25%가 되는 **다음 밴드**다. always-load가 아니라 여기 적혀 있던 값이라 오래 남았다.
- ★ **그리고 삭은 배를 모는 동안은 영영 0이다** — `foeOdds`가 `SHIPS[].leak`면 `[0.90,0.10,0,0,0]`을
  돌려준다(*"볼품없는 배는 큰 놈이 상대해 주지 않는다"*). 아홉 시작배가 전부 삭은 배이므로
  **두목을 만나려면 배부터 갈아야 한다.** 이것도 화면이 말해야 할 자리다 — 지금은 자산만 적는다.
- ★ 소설도 같은 자리를 짚었다 — *"세력 함대는 전부 등급 4~5인데 부자가 될수록 더 못 이긴다"*(`story/FACTIONS.md`).

### A-8d. 나포를 **끌고 오는** 장면을 아직 못 봤다 — `표본`
`navy` 회차에서 5전 3승했지만 **셋 다 고철 처분**으로 끝났다 — 선원이 `prizeCrewNeed` 아래로 줄어서다.
문서화된 설계와 맞지만(짧은 선원 → 고철), **나포 1.00 대 격침 0.45가 전투 설계의 축**인데 그 장면이
한 번도 확인된 적이 없다. 선원 여유를 두고 다시 재야 한다.

### A-8e. **한 판으로 아홉 바다 전부** — `러너 준비됨 · 최고 1,044일차 · 미완주`
사용자 지시: *"한반도 주인공을 골라 전 지역 모든 거점을 한 번에 — 각 지역 이벤트·거점 모두 획득·
항구 개발 등 모든 것을 완수하고 엔딩"*. A-8이 **갈래 × 권역을 각각 새 판으로** 돌린 것과 달리,
여기서는 **판을 한 번만 열고** 부산포에서 끝까지 간다.

- 러너 `.playtest/grand-run/grand-run.mjs`(패권 러너의 사본 — 권역이 상수가 아니라 **축**이다) ·
  진행판 `http://localhost:8891/.playtest/grand-run/` · 완주 정의·비용표 `completion.mjs` ·
  발견 `FINDINGS-grand.md` · 원양 진단기 `ocean-probe.mjs`.
- **완주의 정의(코드에서 뽑음)** — `hegemonyAll()` 9바다 × 4조건(265항구 거점·상관 27·등급5 아홉·
  최고선 아홉) + `endingProgress()` + `homelandProgress()`(숨은 항구 마포·의주에 부두) +
  지역 이벤트 19종 × 9 + 해적 명부 40. **자본 상한 추정 1,552,190닢 · 순회 1,135일.**
- **실측**: 동아시아 41/41은 **다섯 회차 모두 통과**(순회 219~292일). 클릭 검증관이 별도 창에서
  **조선의 끝에 실제로 도달**했다(202일차 · 약 190클릭 · `CLICK-FINDINGS.md`).
- **남은 것**: 원양 첫 관문(광저우→믈라카)을 넘는 장면이 아직 실측되지 않았다. 그 앞에서 죽은
  결함 둘은 잡았다(아래 C-17 · 하네스 쪽은 `QUICKMAP-engine.md` §3).
- **밸런스 회차(2026-08-25) 최고 기록 — `.playtest/supremacy-run/`**: 부산포·역관의 서자·치트 없음으로
  **1,044일차**에 거점 **62/265** · 상관 **6/27**(동아시아 3/3) · 명부 **4/40** · 패권 **0/9**,
  1,025일차 청산으로 끝났다. ★ **치트 없는 판이 집 바다를 벗어난 것은 이때가 처음이다**(801일차 믈라카).
  실클릭 회차 여섯(251·448·536·663일)은 전부 조선 안에서 끝났다.
- **완주를 「못 하게」 막는 것은 하나뿐이다** — 아프리카·인도양에 **육로 구간이 0건**이라
  `bandit`·`toll`이 영영 안 나고 「권역마다 19종」이 그 두 바다에서 불가능하다. 나머지는 전부
  *오래 걸리는 것*이다. 판정 정본 `.playtest/supremacy-run/BLOCKERS.md` · 설계 `.playtest/supremacy-balance/DESIGN-growth.md`.

### A-9. 수직계열화 — `1/4단계` (가공장은 ✅ 2026-08-23)
사용자 지시는 *"산지에 농장도 만들고 유통 선단을 만들어 유통라인 확보하고 가공공장 판매샵 등
수직계열화"* + *"그 당시 세력들과 경쟁하고 때로는 전쟁"*. 사양이 값까지 확정돼 있다 —
**`.claude/docs/SPEC-vertical.md`**(열 갈래 · 네 층 · 세력).

- ✅ **1단계 — 직물 세 갈래 · 가공장만 · 세력 0**(2026-08-23). `state.works` 신설 ·
  `CHAIN` 3갈래(방직·날염·주홍) · 새 품목 둘(`chintz` 320 · `scarlet` 510) ·
  항구 「가공장」 카드 · `check-chain.mjs`·`sim-chain.mjs` 신설.
  곡선은 안 움직였다(짝지은 차이 +0.0% · 실제로 지은 판만 140항차 −6.0%).
  → [wiki/vertical-chain.md](wiki/vertical-chain.md)
- `없음` **2단계 — 생산**(농장·광산 · 새 원료 5종 `cane`·`rawsilk`·`silverore`·`rawpepper`·`kaolin` ·
  `GOODS[].kind` 태그 90종 · 사슬 일곱 완성). ★ **곡선이 처음 움직이는 단계다** — 농장이 붙어야
  사슬이 실제로 남는다(원가 −12~24% · 그 몫은 `impact` 면제). `costFor()`가 두 구간을 갖게 되므로
  **원가 구간이 섞여도 단조인가**를 `test-rules.mjs`에 넣는 것이 선결이다.
- `없음` **3단계 — 소매·유통**(판매소 · 위탁 `state.consign` · 정기선 `state.lines`).
  ⚠️ `tickLines(n)`을 `advanceDays`·`waitDays` **양쪽**에 배선해야 한다.
- `없음` **4단계 — 세력**. ⚠️ **`SPEC-factions.md`가 그 자리를 가져갔다** — `state.factions`·`claim`
  세 등급·면허/밀조/전쟁·몰수는 **안 만든다**(전쟁은 패권 조건 ③이 이미 그 자리다). 아래 **A-10**이 정본이고,
  `SPEC-vertical`이 요구하는 것은 셋뿐이다 — 시설 입회비 · 침범 벌 · **막지 않는 것**.

### A-10. 경쟁 세력 — `1/3단계` (관계·관계도는 ✅ 2026-08-23)
사용자 지시는 *"다른 상단, 제노바, 동인도 회사 등 그 당시 세력들과도 경쟁하고 때로는 전쟁"*.
사양은 **`.claude/docs/SPEC-factions.md`**(값까지 확정), 세력의 얼굴은 **`story/FACTIONS.md`**가 정본이다.
★ **전쟁은 새로 만들지 않는다** — 여덟 바다의 등급 5가 나라·회사의 함대이고, 그것을 꺾는 문의 이름이 이미 「패권」이다.

- ✅ **1단계 — 「누가 화났는지가 보인다」**(2026-08-23). `FACTIONS` 열 · `FACTION_TIES` 세 축 ·
  **`state.regard` 하나** · 움직이는 것 둘(덮침은 악명을 흡수해 읽는다 · 일감 완수 +1) ·
  무는 것 둘(눈총이면 일감이 빈다 · 원수면 문서를 안 판다) · 화면 둘(항구 카드 세 줄 · 관계도 모달).
  곡선은 **한 닢도 안 움직였다**(같은 시드 20쌍 paired 0.000%) — 규칙이 전부 뺄셈이라 새 수입원이 없다.
  → 검증 `node tools/check-factions.mjs` (44/44)
- `없음` **2단계 — 경쟁**: grip 웃돈(`costFor` 한 항 · ⚠️ **상수 배율**이라야 `buy()`의 이분 탐색이 안 깨진다) ·
  계약 가로채기 · 정기선단(`SHOCK.events`의 `convoy` · ⚠️ `kind: 'demand'` — 산지에 걸면 새 수입원이 된다) ·
  `BOON.enroll`(한자 520닢 · 카사 `size×900` · **세를 안 깎는다**) · 연대 악명 `bond` ·
  `pickEnemy`의 regard 항(⚠️ **얼굴만 고르고 등급은 안 건드린다** — 안 그러면 `fleeOdds` 전제가 깨진다) ·
  함대 격파 −4와 승리 모달 세 줄.
- `없음` **3단계 — 회사의 벌목**(`sells: 'nothing'` 규칙 셋)과 **세력끼리의 나포**
  (⚠️ 상선 정원을 안 줄인다 · 플레이어에게 아무것도 안 준다).

---

## B. 데이터는 있는데 게임이 안 읽는 것

### B-1. 항구 인물의 `service` — ✅ **해소**(2026-08-22)
인물 **71명**이 항구에 앉아 있고 화면에도 떴는데 **누를 수 없었다**(모달의 단추가 '자리를 뜬다' 하나).
소설 검수와 완주 플레이가 같은 날 **둘 다** 이것을 최상위로 지목했다 —
소설이 이 바다들을 *"제도를 인물로 보여 준다"*로 설계했으므로 그 문지기가 안 눌리면 제도가 안 굴러간다.
- **여덟 갈래 전부 붙였다**(`state.js: buyService` · 값 `data.js: BOON` · 화면 `port.js: talkTo`):

| service | 명 | 무엇을 사나 |
|---|---|---|
| `permit` | 19 | 그 **바다 전체**에서 90일간 세 −35% (감합·카르타스·인표) |
| `price-tip` | 14 | 여기서 싣고 나가면 값이 오르는 곳 넷 — 품목·항구·상승률·일수 |
| `contract` | 11 | 상관 게시판의 일감을 **한 칸 앞당겨** 다른 것으로 갈아 준다 |
| `smuggle` | 8 | 그 **항구에서만** 14일간 세 −30% |
| `route-tip` | 6 | 나가는 길의 조우 확률·요율·일수 |
| `loan` | 6 | 수수료의 9배를 빌린다 → 30일 뒤 **원금의 1.25배**로 갚는다. 급여일에 **선원보다 먼저** 걷힌다 |
| `repair` | 5 | 그 부두 수리값 20일간 −35% |
| `recruit` | 2 | 선원 6명을 계약금 없이 |

- ★ **세를 깎는 것이 둘이라 바닥을 뒀다**(`BOON.tariffFloor 0.25`). 부관(−35%)·문서(−35%)·밀수(−30%)가
  겹치면 세가 0이 되는데, 그러면 **제도가 사라진다** — 이 세계의 이야기가 거기서 나오므로
  피해 갈 수는 있되 없앨 수는 없게 뒀다. 우려했던 시뮬 곡선 충돌은 이 바닥이 막는다.
- ★ **빚은 선원보다 먼저 걷힌다**(`settlePayroll`). 전주는 급여일에 사람을 보낸다 —
  그것이 이 돈이 무이자가 아닌 이유이고, 못 갚으면 이자가 한 번 더 붙어 다음 달로 넘어간다.
- 실클릭 확인: 부산포 「왜관의 통사」 → `이 바다를 다닐 문서 사기 (222닢)` → 금고 3,000→2,778 ·
  `boons.permit.eastasia = 91일차까지`.

### B-2. 계절 창(season window) — ✅ **해소**(2026-08-26 · 사용자 지시 #4 「계절풍은 있어야지」)
항로마다 **여는 철**이 있다 — `geo.js: ROUTE_SEASON`(권역 안) · `OCEAN_LANES[].season`(원양) ·
합성 `map/geo.js: ROUTE_SEASON` · 규칙 `state.js: routeSeason/seasonFactor/seasonRiskMul`.
지금 **41개 항로**에 철이 걸려 있고 `check-routes.mjs`가 코드↔근거와 **배선까지** 대조한다.
- **판정: 막지 않고 값을 물린다**(`data.js: SEASON` — 속력 ×0.55 → 일수 약 1.8배 · 요율 ×1.6).
  근거 셋 — ① 사료의 "통행 불가"는 대개 **법**이었고(한자법은 상인 자신의 금지, 어기면 벌금·보험 거부)
  값으로 옮기는 것이 오히려 정확하다 ② **항구에는 시간이 없다**(`advanceDays`가 항해에서만 불린다) —
  막으면 갇힌 사람이 철이 바뀌기를 **기다릴 방법 자체가 없다**(진짜 데드락) ③ 인도양 서안·동안이
  반대 철에 열어, 둘 다 막으면 반년마다 인도 아대륙의 절반이 사라진다.
- **화면이 말한다** — 항로 줄의 「철 아님」 표 · 툴팁 한 줄 · 「항로」 패널 머리의 현재 철.
- 발트 12구간(한자법 2/22~11/11) · 말라바르 10구간(남서 계절풍에 닫힘) ·
  코로만델·벵골 9구간(북동 계절풍에 닫힘) · 원양 10구간(`monsoon`).

**해소 전에 무엇이 안 됐나**(재발 진단용): `monsoon: true`가 **어떤 규칙에도 안 읽혔다.**
`scenes/map.js`가 *"철을 잘못 잡으면 훨씬 오래 걸린다"*고 적어 두었지만 일수는 한 치도 안 달라졌다 —
`seasonOf()`와 NPC의 `season` 게이트만 돌고 있었다. **표시만 있고 배선이 없던 자리**이고,
그래서 지금 `check-routes.mjs` §4가 *배선*을 따로 검사한다(`routeFactor`에 계절 배율이 곱해졌는지).

### B-3. 해적 `bounty` 지급 경로 — ✅ **해소**(2026-08-25 · 확인 2026-08-26)
*"저 해적을 일부러 찾아갈 방법이 없다"*가 이 항목이었다. 지금은 셋이 다 있다 —
**소식을 산다**(`buyBountyTip` · 값은 현상금에 묶인 하한 `bountyFloorRate`),
**그 구간이 사냥터라고 화면이 말한다**(`huntLegs` · 항로 카드), **이기고 살아 돌아오면 항구에서 받는다**
(`oweBounty` → `state.bountyDue` → `payBounties`). 현상금은 `capSpoils` **밖**이다 —
나포 포상은 옮겨 싣는 물건이 아니라 관이 장부로 치른 돈이기 때문이다.
- 실측: 중반 세기 5 실수령 **3,953 → 11,107닢**. **가난할 때 가장 크게 갈린다.**

### B-4. 카리브·남아메리카 무역상 — ✅ **해소**(2026-08-22)
**여덟씩 채웠다** — 카리브: 서인도 함대(flota·여름) · 포르토벨로 장(feria·여름) · 베라크루스 코치닐 상관 ·
마닐라 갈레온(겨울·원양) · 리오아차 rescate 밀무역 · 마라카이보 카카오 · 쿠마나 진주 잠수 · 산토도밍고 제당소.
남미: 남해 은 수송선 · 우앙카벨리카 수은 운반대 · 아순시온 마테 · 판파 육포·수지 · 헤시피 제당(원양) ·
브라질우드 청부선 · 라플라타 밀무역(은 하나만) · 과야킬 키나. **세계 상인 52 → 68명.**
근거는 각 권역 `-evidence.json`의 `openQuestions`에 적었다(제도·항로는 사료, purse·rank는 밸런스 값).

**해소 전에 무엇이 안 됐나**(재발 진단용): 두 권역의 `TRADERS`가 빈 배열이라
`world.js: makeTrader`가 `ALL_TRADERS`에서 뽑을 때 **두 바다 상단은 애초에 생성되지 않았다.**
그 바다의 `merchant` 조우는 전부 *"멀리 돛 하나가 지나갔다"*로 끝났고, 털 상대가 없어
`raids()`의 나포 여파 충격도 그 두 바다발로는 영원히 안 났다.
- **미루던 이유가 사라졌다.** 두 파일 머리주석은 *"경제가 들어온 다음에 채운다"*고 적어 두었는데,
  `trade.js`에 두 권역 다 **도시 28곳 전부** 경제가 들어와 있다. 지금은 **채우는 것을 잊은 상태**다.
- **실클릭으로 확인한 영향**(2026-08-22 · `.playtest/nine-seas/`): 그 두 바다에서 `merchant` 조우는
  전부 *"멀리 돛 하나가 지나갔다"* 한 줄로 끝난다. `world.js: makeTrader`가 `ALL_TRADERS`에서 뽑으므로
  **두 바다 상단은 애초에 생성되지 않고**, 털 상대가 없어 `raids()`의 나포 여파 충격(`SHOCK.raidMult`)도
  그 두 바다발로는 영원히 안 난다.
- **소설이 기다리는 자리이기도 하다** — 제8해 「부는 호송되어야 도착한다」(플로타)와
  제9해 「사슬의 가장 좁은 고리」(은의 길)를 게임에서 만날 상대가 없다.
- 채울 자리는 머리주석이 이미 적어 두었다: 플로타 · 파나마 지협 · 밀무역 / 은의 길 · 브라질 설탕 ·
  부에노스아이레스 밀무역.

---

## C. 세계가 커지면서 드러난 구멍

### C-1. `ENEMIES`가 전역 고정 — ✅ **해소**(2026-08-16)
중동 테스터 실측: **홍해 한복판에서 "프랑스 순찰 프리깃 팡당"이 나온다.** 동아시아·아프리카
테스터도 같은 것을 잡았다(대만 해협의 바르바리 기함, 기니만의 바르바리 코르세어).
→ 각 권역 `npc-pirates.js`에 `FOES`(티어 1~5)를 두고 `pickEnemy(rand, regionId)`가 얼굴을 입힌다.
**수치는 안 건드렸다** — hp·포·선원·병력·전리품 금액은 `ENEMIES` 등급 그대로.
★ 재발 진단: 권역을 늘릴 때 `npc-pirates.js`에 `FOES`를 안 적으면 그 바다만 지중해 얼굴로 돌아간다.

### C-2. 조선소가 그 바다와 무관한 배를 판다 — ✅ **해소**(2026-08-26 · 사용자 지시 #3)
`sellsShip`이 기술 사다리(`tierNeeded`) 옆에 **교역 사다리(`yardReach`)**를 나란히 본다.
공업력 1=제 바다 · 2=이 항구가 원양으로 직접 잇는 바다 · 3=이 바다가 닿는 바다 · **4=온 세계**
(`YARD.cap`이 꼭대기라 *온 세계의 배*가 A-2 승급의 마지막 보상이 된다). → `QUICKMAP-combat.md` §2·§3
- **좁힌 것이 아니라 계단으로 만든 것이다.** 한 항구에 걸리는 선종: 공업력 1 **47 → 6종**(중앙) ·
  2 **77 → 12** · 3 **92 → 36** · 4 **— → 93**. 100곳 넘는 곳에서 지어지던 선종 **67 → 0종**.
  나가사키의 브라질 카라벨랑이 사라졌고, 중고 매대(`usedListings`)도 교역권을 본다(한 걸음 관대).
- **막다른 길 없음** — 시작 자리 11곳 전부 「여기 또는 이웃」에 첫 배가 선다(`test-rules` + `sim-firstship` 12/12판).
- ★ **PM 실측(병합 뒤) — 효과가 「다음 배」에 그대로 보인다.** 전에는 아홉 시작지가 **전부 코카 1,023닢**이었는데
  이제 부산포 **평두선 651닢** · 캄바트 **잘바 976닢** · 아르갱 **삼북 1,200닢** · 자메이카 **피라구아 1,104닢**이다.
  **그 바다 배가 사다리의 첫 칸이 됐다** — 사용자 지시(*"교역이 잘 이뤄지는 곳의 배를 먼저"*)가 이 표에 있다.
- ⚠️ **부작용 — 사다리 첫 칸이 낮아졌다.** 지역 배가 코카보다 싸서 `sim-firstship`의 첫 배가
  **10항차 → 1항차**가 됐다(12/12판). 40항차 총자산은 시작지마다 갈리되 붕괴는 없다
  (부산포 32,214 → 26,558 · 믈라카 22,652 → **63,123** · 캄바트 17,365 → 10,584).
  *"돈은 어렵게 벌려야 재미가 있다"*에 걸리는 자리라 **다음 회차의 조정 후보**다 —
  값을 올리는 쪽이 아니라 **첫 칸의 폭**(시작배 → 다음 배 사이의 거리)을 보는 쪽으로.

**해소 전에 무엇이 안 됐나**(재발 진단용): 실측 — 선종 **94** · 도시 265에서 코카·갤리·카라벨 등
**10종이 240곳**에서 지어지고 **67종이 100곳을 넘었다**(공업력 1이면 47종이 한꺼번에 걸렸다).
`state.js: yardAllowed` 주석이 *"`sellsShip`은 공업력만 본다 — **그것이 설계다**"*라고 못박아 두었던
자리이고, **사용자가 그 설계를 바꾸라고 했다**(주석도 같은 커밋에서 고쳤다).
⚠️ 이때 `tools/sim-core.mjs: ORDER`가 **유럽 배 열 척짜리 손목록**이라 아시아 시작 항구가 통째로
「다음 배 없다」가 됐다 — 게임이 아니라 **측정기가 유럽만 알고 있었다.** 지금은 세계에서 뽑는다.

### C-3. 대형 주문이 조달 불가능한 것을 낸다 — `절반`
아덴에서 **"말 8개를 고아까지 · 기한 22일"** — 말은 아덴에서 안 팔고 항로만 21일이다.
`contractOffer`가 2홉 이웃에서 목적지를 뽑으면서 **원산지 조달 가능성과 실제 소요일을 안 본다.**

### C-4. 이름 충돌 — ✅ **해소**(2026-08-26)
- **`유향`** 지중해 `mastic` ↔ 중동 `incense` — 2026-08-15 지중해 감수가 고쳤다.
- **`바갈라`** 아프리카 `baghla`(5,200 · 포 4문) ↔ 중동 `baghlah`(10,200 · 포 10문 · 각진 선미).
  **합치지 않았다** — 같은 다우 계열의 **다른 바다 변종**이고 제원이 실제로 다르다. 이름의 본가가
  걸프이므로 그쪽이 「바갈라」를 그대로 쓰고 스와헬리 해안 것을 **「잔지바르 바갈라」**로 갈랐다.
  값·제원·era는 한 자도 안 건드렸다(근거 JSON에 판단 기록).
- **재발 방지** — `check-world.mjs` ⑤가 **교역품·선종·도시**의 표시 이름 중복을 본다.
  ★ 메시지가 *"합치지 말고 지명을 붙여라"*라고 적는 이유: 이 검사가 **콘텐츠를 줄이는 쪽으로 읽히면 안 된다.**
- **왜 여태 안 잡혔나**(재발 진단용): id가 갈려 있어(`baghla`/`baghlah`) `check-*` 열셋이 전부 통과했다.
  사람 눈으로만 잡히던 자리다.

### C-5. 선원 이름 풀에 새 바다가 없다 — ✅ **해소**(2026-08-16)
`CREW_NAME_POOL`에 latin·iberian·greek·levant뿐이라 새 깃발이 전부 `latin`으로 떨어져
**광저우 술집에 이탈리아 이름 무리**가 떴다. 풀을 11개 늘려 나가사키 겐타 패·광저우 허(許)씨 패로
확인됐다.
- 해적 소굴 세 곳(토르투가·쌍서·계롱)은 깃발이 `pirate`라 나라를 알 수 없어 `latin`으로
  떨어졌다 → `PIRATE_NAME_POOL`로 **그 바다**를 보게 했다(해적도 근처에서 태운다).
  토르투가 "시마론 패" · 쌍서 "허(許)씨 패" · 계롱 "임(林)씨의 무리".

### C-6. 대시보드가 아홉 바다를 못 담는다 — ✅ **해소**(2026-08-26)
탭 여섯이 전부 권역 필터를 읽고, **사람 탭이 생겼다**(`dashboard/npc-view.js` — 상단·해적·인물·동료 넷 +
권역/갈래/검색 + 바다×갈래 교차표).
- ⚠️ **"NPC 163명"이 틀렸다 — 230명이다**(2026-08-26 실측). 상단 68 · 해적 40 · 인물 71 · **동료 51**.
  **동료가 아무 데서도 안 세어지고 있었다.** 사람이 선 항구는 **138/265**.
- ⚠️ 아래 "필터를 읽는 탭은 항구·선박 둘"도 낡았다 — **경제 탭도 이미 읽고 있었다.**
  안 읽던 것은 오버뷰·보수물가였고 둘 다 붙였다.

<details><summary>해소 전 기록(재발 진단용)</summary>

공유 권역 필터는 생겼다(`dashboard/region-filter.js` — 선택을 한 곳에 두고 탭이 구독한다.
*"권역을 추가하면 이 바는 저절로 늘어난다"*). **문제는 구독하는 탭이 둘뿐인 것이다.**
- **실측** — 탭 여섯(오버뷰·경제·해적·선박·항구·보수물가) 중 필터를 읽는 것은 **항구·선박 둘**.
  나머지 넷은 아직 전 세계를 뭉뚱그린다. (예전 기록의 "경제·항구"는 낡았다.)
- **NPC 163명(상단 52·해적 40·인물 71)을 한자리에 보여주는 탭은 여전히 없다**(해적 탭은 명부만 본다).
- 배선 자체는 작다 — 필터는 이미 공유물이라 각 뷰가 구독 한 줄을 더하면 된다.
</details>

---

### C-7. 원양이 근거리보다 못 번다 — ✅ **해소**(2026-08-16)
아메리카 테스터: 아카풀코→마닐라 **54일에 −2,847닢**. 아프리카 테스터: 희망봉 35일 +2,813닢 vs
동안 근거리 16일 **+3,327닢**(하루 208닢 vs 80닢).
- **원인은 마찰이다.** 40개를 한 번에 사면 +6%, 팔면 −16%. 120칸을 한 항구에 쏟으면 매각 −24%.
  스프레드가 아무리 커도 대량거래 벌점이 먹는다.
- 사건 판정을 일수에 매단 뒤(2026-08-16) 긴 항해의 나쁜 일이 0.32 → 1.11회로 늘어 **더 불리해졌다.**
  좋은 일도 0.29 → 0.72회로 늘었으나 증가폭이 작다.
- **고친 방법**: `MARKET.gateDepth 2.5` — 원양 항로가 닿는 항구 29곳은 시장 깊이가 그만큼 깊다.
  근거는 `caribbean-evidence.json`이 이미 적어 둔 것이었다 — "페리아에서 팔린 물건은 지협을 넘어
  페루 부왕령 전체로 퍼졌으므로 **소비권이 이 항구 규모보다 훨씬 넓었다**." 깊이를 도시 규모로만
  재는 것이 애초에 틀렸다. 은 항로 실거래 5% → **21%**, 근거리는 15% 그대로(시뮬 12회로 확인).
- ★ **마닐라행은 아직 음수다.** 그건 벌점이 아니라 마닐라도 은 산지(0.46)이기 때문이고,
  갤리온의 이문은 은을 파는 데가 아니라 **그 은으로 비단·도자기를 사 오는 왕복**에 있었다.
  편도만 재면 계속 손해로 보인다 — 왕복 곡선은 아직 안 냈다.

### C-10. 원양 항로가 **출발 권역**의 적 표를 쓴다 — `절반`
아메리카 테스터: 아카풀코→마닐라(태평양 한복판)에서 **부카니에 소선**, 반대 방향에서 **왜구 소선단**.
`pickEnemy(rand, regionId = currentRegion())`이고 항해 중 `state.at`은 출발지다. 바르바리만큼
심하지는 않지만 같은 종류의 구멍이다 → 원양 구간은 양쪽 권역에서 뽑거나 '대양' 표를 따로 둔다.

### C-11. 사다리 꼭대기에 딱지가 없다 — ✅ **해소**(확인 2026-08-26)
딱지가 **순위**가 됐다 — `state.js: tagRank`가 그 바다의 같은 편(산지끼리·수요지끼리)을 줄 세워
`산지 2/6`처럼 내고, 툴팁이 *"이 바다의 산지 6곳 가운데 2번째로 싸다"*라고 말한다(`port.js:201-205`).
- **왜 그래야 했나**(재발 진단용): '산지' 하나로는 포토시(0.44)와 놈브레데디오스(0.74)가 같은 딱지를
  달아 30% 값 차이가 안 보였다. **순위는 플레이어가 실제로 알고 싶은 것을 그대로 답한다.**

### C-12. 지도 두 곳이 아직 어색하다 — `그림`
- **중동**: 카이로~다마스쿠스~바그다드~바스라 줄이 통째로 물길이라 **아라비아가 섬**이 됐다.
  그 도시들을 잇는 것은 육로인데 항로 회랑이 바다를 판다(`auto.js`가 육로를 안 가린다).
- **남아메리카**: 바다 53.4%인데 서·남안이 폭 2~4px 협만 그물이라 "대륙 하나 + 두 대양"으로
  안 읽힌다. 카리브는 섬·유카탄·플로리다가 또렷해 대비가 크다.

### C-8. 급여일에 선택이 없다 — ✅ **해소**(2026-08-26)
*"안 주는 것도 선택이지 회피가 아니다"*라고 화면 주석이 적어 두었는데 **안 주는 단추가 없었다.**
이제 급여일 모달에서 셋을 고른다 — **판다**(C-17과 **같은 표**를 모달 안에서 바로) ·
**빌린다**(그 항구에 대금업자가 있으면) · **이번 달은 미룬다 — 한 푼도 안 준다**.
- ★ **새 규칙을 만들지 않았다.** 이미 있던 선택지(짐·정박선·거점 매각, 대금업)를 그 화면에서
  **닿게** 했을 뿐이다. 규칙 쪽 변경은 `settlePayroll(rand, { pay })` 인자 하나이고
  **인자를 안 주면 예전 그대로**다(검사로 못 박았다). 체불·이탈 벌칙은 손대지 않았다.
- 실측: 급여일 모달에서 「판다」로 금고 300 → 1,015에 모달이 다시 그려지고,
  「미룬다」로 금고 1,015를 지킨 채 체불 1,400이 쌓였다.

### C-9. 상선을 덮치는 데 대가가 없다 — ✅ **해소**(확인 2026-08-26)
*"평판도 소문도 안 남으니 이길 수 있으면 언제나 덮치는 쪽이 옳다"*가 이 항목이었다. 지금은 대가가 둘이다 —
**전리품에 상한**(`capLoot` — 옮겨 실을 수 있는 만큼)과 **악명**(`addInfamy(flag,1)` + *"이 일은 소문이 난다"*).
`regardOf`가 그 값을 읽어 세력 관계로 이어지므로, 상단 명부의 `refuse` 대사
(*"원로원의 화물이오. 손대면 총독이 안다"*)가 이제 실제 예고다.
- ⚠️ **실전 표본은 아직 0이다**(코드로만 확인). 덮친 뒤 관계가 실제로 얼마나 상하는지는 실클릭이 재야 한다.

---

> ★ **2026-08-23 — C-13~C-16을 C절로 되돌렸다.** 그동안 이 넷이 `## E`·`## F` 아래에 흩어져 있어
> "C절을 찾아 스크롤하면 못 찾는" 상태였다(콜드 스타트 점검이 잡아냈다). 내용은 그대로다.

### C-13. 삭은 배를 수리 없이 무한히 몰 수 있다 — `구멍` (부분 해소 2026-08-25)
> ★ **침몰이 아주 없지는 않게 됐다** — 동행선은 포격에서 `spreadDamage`로 **격침된다**(A-4).
> ⓘ **2026-08-26 판정 — 기함이 그 자리에서 안 가라앉는 것은 결함이 아니라 설계다.**
> `state.js: spreadDamage` 주석이 *"기함은 여기서 가라앉지 않는다 — **패배 처리**(`battle.js: finish('lose')`)가
> 따로 있다"*고 적어 두었다. 「기함이 침몰하지 않는다」를 **결함 목록에 적지 말 것**(라우터에 그렇게 적혀 있었다).
> 아래 진단은 *수리 없이 무한히 몬다*는 쪽에 대해서만 유효하다.
> ★ **2026-08-25 — 「대가 없음」은 해소됐다.** 선체가 바닥이면 **느려지고**(`state.js: hullFactor` ×0.85/×0.70)
> **실은 짐이 젖는다**(`soakCargo` — 값싼 것부터). 값은 `data.js: HULL`, 서술은 그 주석이 정본이다.
> C-13의 설계 메모 셋 중 **①과 ②**를 골랐다 — 둘 다 *막지 않고 값을 물리는* 쪽이라
> `oceanReady` 주석의 원칙(*"항구에 갇히는 일이 없어야 한다"*)을 안 깬다.
> **남은 것은 침몰(N4)뿐이다.** 폭풍이 항차당 12%로 나므로(`SEA_EVENTS`) 선체 1에서 치명적으로 만들면
> 실플레이가 실제로 해낸 「금고 0·선체 1에서 1일 항로 열여섯 항차」 탈출이 **87% 확률로 끊긴다**
> (supremacy ISSUES #14). ⇒ 침몰은 우연이 아니라 **플레이어가 고른 자리**(전투)에 붙어야 한다 — 따로 설계할 몫.
> ★ **N4(파산이 없다)는 2026-08-25에 해소됐다** — 못 낸 돈이 빚이 되고, 급여일에 못 갚으면
> 채권자가 집행하며, **배를 넘기면 셈이 끝난다**(`state.js: enforceDebt`·`liquidate` ·
> 근거 `data.js: BANKRUPT` · 서술 [wiki/payroll.md](wiki/payroll.md) §7). **배를 잃는 길이 생겼으므로
> 「기함이 안 가라앉는다」의 대가가 처음으로 실재한다** — 다만 침몰 자체는 여전히 없다.
누수는 `advanceDays`에서 `Math.min(state.hp - 1, …)`이라 **hp를 1 밑으로 깎지 않는다**. 침몰이 없으니
낡은 바사를 고치지 않고 영원히 몰 수 있고, 전투만 피하면 불이익이 사실상 없다.
- **왜 지금 드러났나**: 시뮬이 "60% 미만이면 전액 수리"를 하고 있어 가려져 있었다. 그 전략을 걷어내니
  (실제 플레이어처럼) 초반 압박이 통째로 사라졌다 — 완주 20/20판. → [wiki/playtest-log.md](wiki/playtest-log.md) §4-2
- **왜 값으로는 못 막나**: 시작 자금은 절벽(175↔180)이라 손잡이가 아니다. 180~210이 결과가 같다.
- **고른다면**: ① 선체가 낮으면 속력이 준다(선원 미달과 같은 방식 — 항해 일수 증가) ②
  일정 이하에서 화물이 젖어 상한다 ③ 펌프질에 사람이 묶여 백병전 인원이 준다. **①이 기존 규칙과 가장 닮았다.**

### C-17. **파산하면 판이 잠긴다 — 벌이 없는 게 아니라 출구가 없다** — `구멍` (실측 2026-08-24)
한 판 완주 회차가 960일차에 금고 0이 되자 **항해비를 못 내 출항이 막혔고, 그 뒤 아무것도 할 수
없었다.** 러너는 마지막 2,241닢까지 근해를 왕복하다 멈췄다.
- 규칙상 회복 경로는 **있다** — 짐을 팔거나(`sellAll`) **정박한 배를 판다**(`sellShip` · 재판매가 `SHIP_RESALE`).
- 그러나 **화면이 그것을 안내하지 않는다.** `금화가 모자란다`만 뜨고 *팔 수 있는 것이 있다*는 말은
  어디에도 없다. 실제 플레이어도 같은 자리에서 막힌다.
- ★ **C-13이 적어 둔 "파산이 없다"와 같은 자리인데 뜻이 다르다** — C-13은 *벌이 없다*는 뜻으로 적었고,
  실측은 **출구가 없다**는 쪽이었다. 고칠 것은 파산 벌칙이 아니라 **막다른 골목의 안내**다.
- 참고: 자금이 마른 것 자체는 설계다(`voyageCost` 일곱 갈래 · *"성장할수록만 무거워진다"*).
  큰 선단을 끌고 무역을 안 하면 하루 6,000~10,000닢이 나간다(사냥 구간은 순회의 두 배 — 수리비).

### C-18. **부두 거점과 공업력 승급이 같은 값을 올리는데, 순서로 비용이 갈린다** — `설계` (실측 2026-08-24)
`HOLDINGS.dock.industryUp = 1`과 `YARD_UPGRADE`가 둘 다 `industryOf`를 올린다. 어느 것을 먼저
사느냐로 **남은 비용이 통째로 달라지는데 화면이 그 존재를 말하지 않는다.** 염포(base 1 · 필요 3):

| 순서 | 승급 | 자재 | 공기 | 부두값(`60,000+40,000×지금 공업력`) |
|---|---|---|---|---|
| 부두 먼저 | 2→3 · 96,000닢 | **760칸** | **360일** | 100,000닢 |
| 승급 먼저 | 1→2 · 34,000닢 | **330칸** | **180일** | 140,000닢 |

- 부두값 40,000닢을 더 내는 대신 **자재 430칸과 180일**을 아낀다. **되돌릴 수 없는 선택**이다.
- ★ **조선의 끝 ①이 부산포에서는 설계대로 굴러가지 않는다** — base 2라 조선대+부두 두 클릭(약
  200,000닢)이면 3이 된다. `data.js: ENDING` 주석의 *"돈만으로는 안 된다 — 자재를 실어 오고"*가
  두 부두 중 **염포에서만** 참이다. (같은 계열의 자리를 A-8 회차가 `yardsOnly`로 한 번 막았는데,
  **공업력 조건 자체**는 여전히 부두로 우회된다.)
- 화면이 말하지 않는 것 셋이 더 붙어 있다 — ⓐ 거점 「부두」 설명에 **「공업력」이라는 말이 없다**
  ⓑ **공사 중인 부두는 배를 못 짓는데**(`sellsShip`의 `yardBusy`) 그 대가가 **공사를 건 뒤에만** 뜬다
  ⓒ **배 한 척으로는 자재를 못 싣는다**(필요 330~1,440칸 vs 가장 큰 배 380 + 창고 60 ·
  `storeCap`은 `Math.max`라 창고를 더 지어도 안 는다). 답은 **동행 선단**(`cargoCapTotal`)인데
  승급 카드는 `목재가 N칸 모자란다`만 되풀이하고, 화물칸이 차면 시장 `사기`도 이유 없이 `disabled`가 된다.
- ⚠️ **고치지 않았다** — `HOLDING.industryCap 3`은 설계값이라, 막으려면 `ENDING.yards`를 부두로
  못 닿는 값으로 올리거나 판정을 `yardBoost`(승급으로 올린 칸)로 바꿔야 한다. **밸런스 결정이다.**
- 진단 전문 `.playtest/grand-run/FINDINGS-grand.md` G-F1·G-F5·G-F6 · 클릭 실측 `CLICK-FINDINGS.md`.

### C-14. 권역이 이름만 갈리고 얼굴은 안 갈린다 — `절반`
✅ **선원 스프라이트는 갈렸다** — `unitSprite(…, faceKey)`가 권역별 키를 굽고 씬이 `regionOf(state.at)`를 넘긴다.
✅ **이름 중복도 해소** — `tavernCrews`가 이미 앉은 이름을 비켜 간다(전수 9,975건 중복 0).
**남은 것**: PNG가 있는 병종은 `sailor` 하나라 포수·검병은 아직 지중해 얼굴이고, **술집 배경은 한 장 그대로**다.
그림이 오면 `assets/npc/char-<유닛>-<권역>.png` + manifest 한 줄로 붙는다(코드는 안 고친다).
→ [wiki/playtest-log.md](wiki/playtest-log.md) §4-6 · 키 함정은 [QUICKMAP-art.md](QUICKMAP-art.md) §3

---

### C-15. 상선 조우 열세 번 중 **실제 거래는 두 번** — ✅ **해소**(2026-08-22)
`SEA_EVENTS`에서 `merchant`는 가중치 12로 `storm`과 같은 무게인데, **실효는 그 15%**다.
아홉 바다 126구간 실클릭에서 조우 13회 중 11회가 *"멀리 돛 하나가 지나갔다"*로 끝났다
(상인이 여덟씩 있는 지중해·인도양에서도 그랬다).
- **왜**: `map.js: startVoyage`가 `world.js: npcsOnLeg`로 배를 붙이는데, 그 조건이
  **"그 상인의 `at`·`to`가 정확히 내가 지금 타는 두 항구일 것"**이다. 상인은 권역당 3척
  (`npc/config.js: tradersPerRegion`, 실제 40척 안팎)이고 세계는 264 도시라 겹칠 일이 드물다.
- **밀도를 올리지 않고 찾는 범위를 넓혔다** — NPC를 늘리면 시세 압력(`NPC.pressure`)도 같이 늘어
  벌 곳이 준다. 대신 `world.js`에 세 단을 두었다: ①정확히 그 구간(지금까지의 규칙 — "지도에서 보던 그 배")
  → ②두 항구 중 하나를 오가는 배(`tradersNearLeg`) → ③그래도 없으면 **그 바다 명부에서 한 척을 세운다**
  (`strayTrader` — 짐을 싣고 온다. `state.npcs`에는 안 넣어 세계의 정원을 흔들지 않는다).
  해적은 이미 같은 폴백(`pickEnemy`)을 갖고 있었고 **상선만 없었다.**
- **실측**: 842개 구간에서 상선을 만날 수 있던 비율 **4.5% → 100%**(정확 4.5 + 근처 31.4 + 폴백 64.1).
  조우 확률 자체(12%)는 안 건드렸고, 해상 거래는 그 항구 시세의 **1.12배**로 사는 것이라 공짜 이득이 아니다.

### C-16. `index.html`에 아이콘이 없어 `/favicon.ico` 404 — ✅ **해소**(2026-08-22)
게임 동작에는 지장이 없다. 다만 브라우저가 제 발로 부르고 404를 받으므로
**콘솔 오류를 세는 모든 검증이 이것 하나로 전부 실패한다** — 실제로 아홉 바다 TC-01이
전부 FAIL로 나왔다(→ `wiki/playtest-harness.md` §3). **인라인 SVG(data URI) 한 줄로 넣었다** —
외부 파일을 두지 않는다.

---

## D. 오래된 미구현 (확장 이전부터)

- ~~**세이브/로드**~~ → **A-0에서 ✅ 해소**(2026-08-22 · `js/save.js`). 이 줄을 "없음"으로 읽지 말 것 —
  같은 파일 안에서 사실이 갈려 콜드 스타트 점검이 실제로 걸려 넘어졌다.
- **부관 급여 유예** — 가난할 때 에이미의 급여를 미루는 규칙. 초반 10항차가 예외 없이 손해인 것의 해법으로 적혀 있다.
- **술집 평판** — 체불이 오래가도 어느 항구에서든 똑같이 태울 수 있다.
- **백병전 연출** — 포즈 전환뿐(`pixel.js: blitTinted`가 준비돼 있으나 미사용).
- **포격 페이즈의 바람** — 항해에는 있는데 전투에는 없다. 선회가 거리 조절로만 추상화돼 있다.
- **★ 시뮬이 10항차에서 멈춘다** — 확장 이전부터 있던 문제. 진단은 [`WORLD-INTEGRATION-TODO.md`](WORLD-INTEGRATION-TODO.md) §6(같은 `.claude/docs/` 안이다 — 저장소 루트에서 찾으면 헛걸음한다).

---

## E. always-load에서 옮겨 온 것 (2026-08-16)

`claude-memory.md`의 "다음 후보" 절이 이 문서와 거의 그대로 중복이라 옮겼다.
여기 없던 두 개만 아래에 적는다 — 나머지는 위 A~D에 이미 있다.

### E-1. 관세를 사료 쪽으로 — `판단 보류`
게임 3~6%(`TARIFF`) vs 사료 20%(알렉산드리아 impost). 두 원칙("어렵게 벌어야" + "근거에 충실")이
다 인상을 가리킨다. 다만 게임은 **매각에만** 물어 단순 비교가 안 되므로 **시뮬 곡선을 먼저** 낸다.

### E-2. 그림 발주 — `절반`
항구 배경 16장은 의뢰서만 있다. **지도는 아홉 장이 붙었다**(2026-08-16, `assets/map/*.png` +
`manifest.json`) — 다만 그것은 **코드가 그린 것을 굳힌 것**이지 사람이 그린 그림이 아니다.
발주 의뢰서는 `assets/map-briefs/<권역>.md` 아홉 장이고 기준판이 곧 게임이 쓰는 그림이라
납품본을 같은 이름으로 덮으면 그대로 들어간다. 검수 `python tools/check-map.py --all`.

## F. 그림 파이프라인 — 2026-08-17 실클릭·후처리에서 나온 것

> 이 절은 **에셋 유입**에 딸린 일이다. 코드 구멍(A~D)과 달리 그림이 와야 진행된다.
> 방법은 `QUICKMAP-art.md` §3과 `tools/clean-map-art.py` 머리말이 정본.

### F-0. ✅ 해소 — 재의뢰 자료를 갖췄다 (2026-08-17)
`node tools/gen-map-tracing.mjs`가 권역마다 두 장을 뽑는다 — **4배 확대판**(`-x4.png`, 덧그릴 판)과
**해안선만 남긴 판**(`-coast.png`, 지켜야 할 실루엣). art §3이 "빠진 것은 좌표를 이미 만족하는
실루엣 기준판"이라고 지적한 바로 그 자산이고, 세 번의 반려 끝에 **덧그리기만 성립한다**는 결론에서 나왔다.
의뢰서 §2에 전달 파일 넷으로, §4-B에 이유와 함께 적었다.

### F-1. 권역 지도 9장이 모두 코드판이다 — `대기 · 3차 반려`
사람이 그린 지도는 **세 번 다 반려**됐다. 3차(지중해·AI 생성)는 그림 자체는 훌륭했지만
게임 좌표를 얹으니 도시가 **바다 한가운데** 떠 있었다(알렉산드리아 64px). 되돌렸다.
- **원인은 매번 같다** — 이 지도는 실제 지리의 투영이 아니라 도식이라, 진짜 지리대로 그릴수록 더 틀린다.
- ⇒ 재의뢰 한 벌은 **`assets/BRIEF-MAP.md` + 바다별 `map-briefs/<권역>.md` + 이미지 4장**
  (`<권역>.png` · `-x4.png` 덧그릴 판 · `-coast.png` 지킬 실루엣)이다. **AI에 줄 때는 의뢰서 §1-A**
  (비율 16:9·정수배·img2img·denoise 0.25~0.35)가 정본 — 3차가 그 자리에서 무너졌다.
- ⚠️ **첨부용 이미지 둘이 지금 어긋나 있다**: `map-briefs/<권역>.png`가 08-15판(9장 모두 지금 지도와 다름),
  `-coast.png`가 한 판 낡았다. `-x4.png`만 현재 지도와 픽셀 일치한다. 보내기 전에 다시 뽑아야 한다.
- 받으면 `check-map.py`로 검수하고 **좌표를 그림 위에 찍어 눈으로도 대조한다**(→ `QUICKMAP-art.md` §3).

### F-2. ✅ 해소 — 검수기가 "바다에 뜬 항구"를 못 잡던 것
도시 검사가 "반경 6px에 물이 있나"만 봐서 **바다 한복판이 늘 통과**했고 26곳이 `섬 항구`로 조용히
집계됐다. 뭍까지의 거리를 재는 검사 둘을 넣었다(28px 초과 실패 · 12px 초과가 절반이면 실패).
실측 근거: 코드판 평균 8.5px vs 어긋난 판 29.5px. **진단은 남겨 둔다** — 같은 우회가 또 온다.
같은 검사가 진짜 결함도 잡았다(쌍서가 섬 없이 물 위에 떠 있었다 → `auto.pinIsles`로 해결).

### F-3. 인물 초상이 71명 중 1명뿐 — `대기`
자리는 만들었다(항구 인물 패널에 초상 · 전투 화면 우측 상단에 이름난 해적 초상).
그림은 **시박태감 뇌은**(`eas-ningbo-kanhe`)과 **왕직**(`wangzhi`) 둘뿐이고 나머지는 직업·세기에 맞는
병종 실루엣을 공유한다. 발주 규격·키 규칙은 `assets/BRIEF-NPC.md` §5.
- 우선순위는 그 바다의 제도를 상징하는 인물(감합의 시박태감·왜관 통사 같은)과 바다별 두목 해적.

### F-4. 권역 얼굴의 남은 절반 — `절반` (→ C-14)
선원(`sailor`) PNG는 붙었으나 **포수·검병 등 다른 병종은 지중해 얼굴**이고, **술집 배경도 한 장**이다.
`char-<병종>-<바다>.png` + manifest 한 줄이면 코드 수정 없이 붙는다.

### F-5. 항구 배경 6/16 — `대기`
베네치아·이스탄불·알렉산드리아·로도스·알제·몰타만 사람이 그린 판이고, 남은 10곳은 코드 생성이라
서로 비슷하다. 규격·키는 `assets/PORT-BACKGROUND-BRIEF.md`.

### F-6. `check-map.py`에 남은 우회 구멍 — `절반`
✅ **바다에 뜬 항구는 이제 잡는다**(F-2). ✅ **`mapcheck.html`도 같은 기준이 됐다**(2026-08-18) —
브라우저 판정이 옛 "반경 6px"만 보고 있어 3차를 놓친 그 구멍을 그대로 갖고 있었다. 남은 것은 2차 외주가 쓴 우회다 — 8색·해안선이 전부 직선인
그림이 **전 항목을 통과했다**. 색수 **하한**(≥24색)과 **해안선 직선 구간 길이 상한**이 있으면 막힌다
(지금은 색 수가 *많다*는 경고만 있다). ⇒ 세 번 다른 방식으로 통과당했으므로,
**기계 검수는 최소조건이고 눈 대조를 절차로 못박아 두었다**(`QUICKMAP-art.md` §3).

### F-7. 하네스 — 뷰포트 밖 요소 클릭 — `열림 · 사소`
`click`은 화면에 보이는 좌표만 누른다. 조선소 선박 목록은 세로 **9,000px**이 넘고 항구 인물 패널도
뷰포트 밖(y≈985)에 있어, 지금은 `eval "…scrollIntoView()"`를 먼저 넣어야 한다.
`click`이 대상이 화면 밖이면 스크롤하고 좌표를 다시 재게 하면 사라지는 불편이다.

### F-8. 지도 질감 스크립트가 벽지 무늬를 만든다 — ✅ **해소**(2026-08-26 · 선택지 ②로)
`tools/enhance_map_texture.py`(untracked)가 지도 9장을 덮어 놓은 상태다. **커밋하면 그 판이 들어간다.**
- **증상**: 육지가 **41.9px·17.9px 격자로 반복**된다(세로 42px 자기상관 0.688). 사하라와 사바나의
  남북 구분이 사라지고 산줄기가 없어졌다. `check-map.py`는 **9/9 통과**시킨다.
- **원인**: 주석은 "Perlin-like"인데 실제로는 `sin(x·0.15)·cos(y·0.15)` — 사인파 곱은 주기함수다.
  그리고 `CLIMATE[].zones`(y로 지대를 가르는 함수)를 읽지 않는다.
> ★ **2026-08-26 — 지도 회차가 ②를 골라 실행했다.** 사인파 곱을 걷어내고 렌더를 통째로 다시 짰다
> (거리장 기반 해안 띠 · 부호거리장+3옥타브 `landmass` · 해저 저주파장 수심). `zones`도 살아났다 —
> 아프리카가 사하라→사헬→우림→사바나로 선다.
> **실측(자기상관 국소 봉우리)**: 아홉 장 중 **일곱이 단조 감소**(무늬 없음)이고, 카리브·동아시아의
> 봉우리(30~48px)는 **섬 배치 주기**이지 질감 반복이 아니다 — F-8의 증상이던 *"지대 구분 소실"*은
> 아프리카가 clean인 것으로 반증된다.
> ⚠️ **`git stash@{0}`에 옛 벽지 판이 남아 있다** — 되살리지 말 것. `tools/enhance_map_texture.py`도
> 더 이상 파이프라인에 없다(`gen-map-png.mjs`가 정본).

- **선택**: ① `git checkout assets/map/` 후 `gen-map-png.mjs` 재생성으로 되돌린다 ② 값 노이즈(좌표 해시
  + 보간)로 바꾸고 `zones`·산줄기를 살려 다시 돌린다. **②를 권한다** — 코드판이 밋밋한 것은 사실이고
  망가진 것은 노이즈 함수 하나다. 어느 쪽이든 끝나면 `gen-map-tracing.mjs`로 `-x4`·`-coast`를 다시 뽑는다.

## 손대는 순서 (제안)

> ⚠️ **2026-08-26 재갱신(3회차).** 앞선 판(08-23)의 1번 A-5는 해소됐고, 그 뒤로 **밸런스 회차 P1~P8**과
> **지도 회차**가 통째로 지나갔는데 표에 없었다. 낡은 순서표는 **틀린 지시**라 회차마다 여기를 다시 쓴다.
> **이번 회차 커밋**: 병합 셋(P8-2·3 / P7 / P1·P4·P8-1) · `C-4` · 점검기 넷(`check-imports` 신설 ·
> `check-world` ⑤ · 못 짓는 배 · `region-topology` BY_DESIGN) · 세이브 손실 방지(ISSUES #36).

### ★ 이번 회차가 남긴 가장 큰 교훈 — **측정기를 먼저 의심하라**

한 회차에 **거짓 신호가 넷**이었다. 넷 다 "게임이 틀렸다"로 읽힐 뻔했다:

| 무엇이 거짓말했나 | 진짜 | 고친 것 |
|---|---|---|
| `probe-fair`가 옛 상수를 읽어 **「보험 미해소」** | 이미 전 구간 90%로 고쳐져 있었다 | 프로브가 `insureCover`를 부르게 |
| `check-world`가 정적 공업력만 보고 **「철갑 거북선을 못 짓는다」** | 염포를 두 번 승급하면 열린다(설계대로) | 올릴 수 있는 데까지 올려 놓고 판정 |
| `region-topology`가 남미를 **「닿지 못하는 자리」**로 | 두 대양이 안 만나는 것이 그 권역의 뼈대다 | `BY_DESIGN`으로 갈라 ⓘ 표기 |
| `check-map.py` 요약이 **진짜 반려 사유를 가림**(`head[-1]`이 경고문을 집었다) | 반려는 항로 관통이었다 | 지도 회차가 개별 실행으로 우회 |

★ 그리고 **반대 방향으로 가장 비쌌던 것** — `check-*` 열셋과 규칙 184개가 **전부 통과하는데 게임이 안 떴다**
(`scenes/map.js`가 `riskKey`를 잘못 가져왔다). 그 도구들은 `data.js`·`state.js`를 직접 부르므로
**`scenes/*`를 한 번도 안 거친다.** 지도가 낡은 채 굳어 있던 것이 그 장애의 **증상**이었다
(`gen-map-png.mjs`는 게임을 띄워서 굽는다). ⇒ `tools/check-imports.mjs` 신설.

### 다음 순서

1. **지도 미학 마무리** — 회차 진행 중(`.playtest/map-polish/`). 검수 9/9 통과까지 왔고 남은 것은
   **미학**이다: 기후 버그 둘(아프리카 적도 이남이 통째로 사하라 · 중동에 사막색이 한 픽셀도 없다) ·
   색 예산 역전(바다 250색 vs 육지 7색) · 해안 대옥타브. 진단 정본 `DIAGNOSIS-designer.md`.
2. **`DESIGN-growth.md`의 판단 보류** — P1~P8이 전부 들어왔으므로 이제 **값을 재는 회차**다.
   원양 배율 2.4~2.8 확정 · `perCrew 0.004` 이중 과금 · P3 코멘다 체류 일수 ·
   새 항로 셋 요율의 근거 JSON 기재 · 전투 패배율 0.35(가정) · 육로 통행세가 P8-1을 새게 하는가.
   ★ **첫 원양이 아직 중앙값 293일**이고 합격선은 80~150일이다(`node tools/sim-gate.mjs 20`) — P7이 문을
   셋 늘렸는데도 미달이므로 **여기가 다음 회차의 과녁**이다.
3. **A-8b 패권 아홉 확장** — 1/9(동아시아만). 러너 준비됨(권역당 약 6분). `regionTopShips` 동률 처리 먼저.
4. **A-9 수직계열화 2단계** · **A-10 세력 2단계** — 사양이 값까지 확정돼 있다(`SPEC-vertical.md`·`SPEC-factions.md`).
5. **C-11 수요 딱지** — 작고, 가장 공들인 사다리가 화면에서 거꾸로 읽히는 문제다.
6. **C-2 조선소 목록** — 판단이 먼저다(위 항목 참조). 즉흥으로 좁히면 콘텐츠를 줄인다.
7. **A-3 지역별 주인공의 남은 절반** — 어디서 시작해도 낡은 바사 + 200닢 + 에이미다.

★ **미커밋으로 남던 `assets/map/*.png` 아홉 장은 이번 회차에 본선에 들어왔다**(지도 회차가 다시 뽑았다).
  더 이상 "빼고 커밋"하지 않는다.

**F(그림)는 이 순서와 나란히 간다** — 그림이 오면 그때 처리하는 일이라 코드 작업을 막지 않는다.
다만 **F-1 재의뢰는 빠를수록 좋다**(§4-A를 안 주면 또 후처리가 필요하다).

## 보류 — 알고 안 하는 것 (always-load에서 옮겨 옴, 2026-08-17)

- **사운드** — 없다.
- **라틴세일이 뭉툭하다** — 안 거슬려 방치했다.
- **대포를 현측 포문에 얹기** — 포문 수 ≠ 대포 수라 규칙부터 정해야 한다.

✅ 해소(2026-08-16): **C-1 권역별 적**(깃발까지) · **C-5 이름 풀**(해적 소굴 포함 전부) ·
   **C-7 원양 수익성**. 진단은 각 항목에 남겨 뒀다(재발 대비).
★ **C-2 조선소 60줄**은 대시보드 선박 탭이 숫자로 드러냈다 — 사선·세키부네가 155곳에서
   지을 수 있고 그중 152곳이 전통 조선지 밖이다. 고치지는 않았다.
