# 세계 확장 — 통합자가 처리할 것

> 아홉 권역을 **동시에** 채우면서, 권역 담당자가 손댈 수 없는 **공용 파일**의 문제가 쌓였다.
> 담당자들은 계약서 §9에 따라 "직접 고치지 말고 보고서에 적으라"는 지시를 지켰다.
> 여기가 그 보고들이 모이는 자리이고, 통합자(= 전체를 보는 사람)가 하나씩 지운다.
> **전부 처리하면 이 파일을 지운다.**

---

## 1. ★ 향신료가 뒤집혔다 (경제 정합성 · 가장 급함)

`spice`(범용 향신료)의 **산지가 통과지에만 있다.** 실측:

| 곳 | 값 | 권역 |
|---|---|---|
| 아덴 | supply 0.56 | 중동 (통과지) |
| 지다 | supply 0.60 | 중동 (통과지) |
| 알렉산드리아 | supply 0.62 | 지중해 (통과지) |
| **캘리컷** | **demand 1.26** | 인도양 (**산지인데 사들인다**) |
| **캄바트** | demand 1.28 | 인도양 |

원인은 조율 실패가 아니라 **각자 옳게 판단한 결과가 어긋난 것**이다. 인도양은 `pepper`·`cinnamon`·`ginger`를,
동남아는 `clove`·`nutmeg`를 각자 새 id로 만들고 범용 `spice`는 건드리지 않았다. 그 결과
"동방에서 올라오는 향신료"라는 범용 품목의 **출발점이 사라졌다.**

**처리안** — 둘 중 하나. 지금은 (A)를 권한다.

- **(A) 인도양·동남아에 `spice` supply를 심는다.** 0.44~0.50으로 두면 순서가 완성된다:
  산지(0.46) < 아덴(0.56) < 지다(0.60) < 알렉산드리아(0.62) < 베네치아(demand 1.42).
  범용 `spice`를 "구체 품목으로 안 갈린 잡향(카르다몸·메이스·후추 섞인 것)"으로 읽으면 고증도 성립한다.
  산지 도시의 `spice` **demand는 함께 지운다** — 산지가 사들이면 안 된다.
- (B) 범용 `spice`를 없애고 구체 품목으로 전부 대체한다. 깔끔하지만 지중해·대서양·동아시아의
  기존 `spice` 수요 20여 곳을 전부 다시 짜야 하고, `SHOCK.events`·`ENEMIES[].loot`도 걸린다.

★ **동남아 권역 작업이 끝난 뒤에** 손대야 한다(작업 중 파일을 고치면 덮어쓴다).

---

## 2. `content/goods-evidence.json`의 목표 비율 (검증)

새 교역품 50여 종이 `gameTargets.goodsRatioToGrain`에 없다. 지금은 **경고로 통과**하지만
(2026-08-15에 실패→경고로 내렸다), 값이 있어야 다음 사람이 밸런스를 되돌리지 않는다.
담당자들이 근거와 함께 제출한 값:

| 권역 | 제출값 (곡물 대비 배수) |
|---|---|
| 아프리카 | melegueta 5.5 · palmoil 2.0 · kola 3.5 · copper 6.0 · panos 5.0 · tortoise 9.25 · ambergris 28.0 |
| 대서양 | timber 1.4 · herring 2.0 · tar 2.2 · stockfish 2.9 · iron 3.9 · linen 7.25 · woolcloth 10.5 · wax 13.0 · sugar 14.0 · amber 18.0 |
| 중동 | dates 1.7 · incense 8.5 · carpet 10.5 · rosewater 11.5 · coffee 14.0 · horse 24.0 · pearl 29.0 |
| 인도양 | cowrie 1.7 · teak 3.5 · saltpetre 5.5 · ginger 6.0 · calico 7.25 · pepper 11.5 · indigo 13.0 · cinnamon 15.0 · diamond 39.0 |
| 동아시아 | sulfur 3.2 · tea 5.0 · deerhide 6.0 · lacquer 8.5 · ginseng 18.0 · musk 22.0 · silver 24.0 |
| 지중해·동남아·아메리카 | (보고 대기) |

---

## 3. 원양 항로 추가 제안 (공용 `js/regions/index.js`)

- `cambay~aden` — 구자라트~홍해가 실제 주간선이었는데 지금 아덴은 캘리컷으로만 이어진다 (인도양 담당)
- `chittagong~pegu` — 벵골~버마 쌀·목재 항로 (인도양 담당, 동남아가 `pegu`를 만들었다면)
- `luanda~mocambique`(26일)를 `luanda~cabo`(12) + `cabo~mocambique`(14)로 쪼개는 안 —
  **지금은 일부러 안 쪼갰다.** 쪼개면 "길고 위험한 외길"이라는 아프리카 권역의 긴장이 옅어진다 (아프리카 담당)

---

## 4. 규칙이 없어서 표현 못 한 것 (미구현 · 후보)

- **계절 창(season window).** 한자법이 발트 항해를 2/22~11/11로만 허용했다. 발트는 겨울에
  *위험한* 게 아니라 **통행 불가**였는데 `ROUTE_RISK`로는 표현이 안 된다 (대서양 담당).
  인도양 계절풍도 같은 문제다 — 원양 항로엔 `monsoon: true`가 있지만 권역 안 항로엔 없다 (인도양 담당).
- **허가 문서.** 명의 감합, 포르투갈의 카르타스, 오스만 카피툴레이션. 지금은 관세를 높게 두는 것으로
  대신하고 근거에 제도를 적어 두었다 (동아시아 담당). 항구 인물(`npc-figures`)로 확장될 자리다.

---

## 5. 깃발·화풍·아이콘 (그림 담당에게 넘김 · 진행 중)

권역 근거 JSON의 `art.iconTodo`·`hullTodo`·`flagTodo`·`styleTodo`에 **136건**이 쌓여 있다
(아이콘 49 · 선형 32 · 깃발 33 · 화풍 22). 그림 담당 둘에게 넘겼다.

★ **깃발을 바꿀 때의 함정**: `check-evidence.mjs`가 도시의 `flag`를 근거 JSON의 `flag.value`와
대조한다. 그림 담당이 깃발을 바꾸면 근거도 같이 고쳐야 하는데 근거 파일은 권역 담당자 소유다 —
그래서 그림 담당에게는 "바꾸지 말고 목록으로 보고하라"고 했다. **그 목록을 통합자가 처리한다.**

---

## 6. ★ 시뮬이 열 항차에서 멈춘다 — **콘텐츠 확장 이전부터 있던 문제**

`node tools/sim-trade.mjs`가 10항차 언저리에서 급여 체불 → 선원 이탈 → 배가 묶여 끝난다.

**이것은 세계 확장이 만든 회귀가 아니다.** 권역 분할 직전 커밋(`d84b971`)을 worktree로 꺼내
같은 시뮬을 돌린 결과가 이렇다:

| | 5항차 자산 | 어디까지 |
|---|---|---|
| 권역 분할 전(`d84b971`) | 347닢 | 5항차에서 멈춤 |
| 아홉 바다(지금) | **648닢** | 10항차에서 멈춤 |

**확장 뒤가 오히려 낫다.** 그러니 이 문제는 확장 이전 어느 시점에 들어온 것이다.
`changelog`의 마지막 측정은 급여 발생주의를 넣을 때(`be3af74`)의 "시작 자금 200 →
완주 8/12 · 체불 2/12"이고, 그 뒤 입항세 항구별 오버라이드(`ada85c2`)가 들어왔다 —
관세가 오르면 매각 수익이 줄므로 **그 커밋을 첫 용의자로 본다.**

진단해 둔 것:
- 항차당 순이익이 90닢 남짓인데 **낡은 바사 수리비가 5항차마다 약 400닢** 나간다
  (leak 2 × 항해 일수 → maxHp 55의 60% 아래로 떨어지면 REPAIR_UNIT 14닢/HP).
- 코카가 1,100닢이라 그 속도로는 영영 못 산다. **시작배를 벗어나는 길이 막혀 있다.**
- 자본이 200닢이라 화물칸 45칸을 못 채우는 것이 병목이다 — 칸이 아니라 **돈이 모자란다.**

고칠 때 볼 후보(하나만 만지지 말 것 — 서로 물려 있다):
`REPAIR_UNIT`(14) · `SHIPS.hulk.leak`(2) · `START_GOLD`(200) · `SPREAD`(0.62) ·
`TARIFF`/`CITY_TARIFF` · 코카 가격(1,100).
**반드시 `node tools/sim-trade.mjs`를 여러 시드로 다시 돌려 확인한다**(메모리: 시뮬 수치는 한 판으로 판단하지 않는다).

## 7. 자잘한 것

- `silk`의 곡물 대비 21배가 사료(1566년 페르시아 시세표: 생사는 밀의 300~500배)에 견주면
  14분의 1로 눌려 있다. **알고 눌러 둔 것**이지만(게임 화폐 규모) 새 품목들이 그 눌린 사다리에
  맞춰졌다는 사실을 근거에 남겨야 한다 (중동 담당).
- `cairo`가 중동 권역에 있다. 나일 대상로가 홍해 체계의 일부라는 판단인데,
  알렉산드리아(지중해)와 원양 항로로 이어져 있어 통합 시 어색하면 조정 (중동 담당).
- 지중해 `iskenderun`(알렉산드레타)이 실제 위치(키프로스 북동)가 아니라 한 칸 서쪽에 앉아 있다.
  `SEA_SPANS` 격자가 그 일대를 육지로 잡고 있어서다 — 해안선을 손볼 때 북동으로 옮긴다 (지중해 담당).
