# changelog — tradeship

> 변경 이력 정본(lazy·거의 안 읽음). `claude-memory.md`에는 **현재 상태만** 두고,
> "무엇을 어떻게 고쳤다"는 여기에 쌓는다.
> 상한 40KB — 넘으면 시점 경계로 분할한다. 과거분:
> [changelog-2026-08-25-am.md](changelog-2026-08-25-am.md)(08-25 앞부분) ·
> [changelog-2026-08-23-24.md](changelog-2026-08-23-24.md)(08-23 ~ 08-24) ·
> [changelog-2026-08-22.md](changelog-2026-08-22.md)(08-22 하루) ·
> [changelog-2026-08-17-21.md](changelog-2026-08-17-21.md)(08-17 ~ 08-21) ·
> [changelog-2026-08-16.md](changelog-2026-08-16.md)(08-16 하루) ·
> [changelog-2026-08-15.md](changelog-2026-08-15.md)(08-15 하루) ·
> [changelog-2026-08-early.md](changelog-2026-08-early.md)(08-08 ~ 08-15 앞부분).

## 2026-08

- **[2026-08-25] #world #trade #balance** — **명부 사냥이 벌금에서 사다리가 됐다. 현상금은 한 닢도 안 올렸다.**
  5차 실플레이(663일차)가 낸 결함 둘을 먼저 고치고, `DESIGN-growth.md` P6을 **순서대로** 넣었다
  (P6-1 → 재고 → P6-2·4·5. 한꺼번에 넣으면 무엇이 고쳤는지 모른다).

  - **★ 파산 청산이 코멘다를 정리하지 않았다**(ISSUES #29 · 큼). *"셈이 끝났다 — 빚은 없다"* 뒤에도
    동료가 갑판에 남아 50%를 떼고, 내리는 순간 **밑천 1,800닢이 빚으로 부활**했다.
    ★3이 세운 「끝이 있는 실패」를 **가장 무거운 부채 한 줄이 넘어오고 있었다.**
    - **판정 — 코멘다는 대차가 아니라 공동 위험 인수다.** 쌍무 콜레간자에서 항해자가 댄 1/3은
      *그도 그 항해에 건 자본*이라 배와 짐이 사라지면 **양쪽이 함께 잃는다**(그가 물어낼 것이 없다).
      **해상대차(배가 사라지면 채무 소멸)와 같은 논리의 다른 얼굴**이다.
      ⇒ 청산이 `state.mates`를 비우고 **밑천은 안 돌려준다**. 전략적 파산도 안 된다 —
      거기 닿으려면 금고·정박선·창고 짐·배를 먼저 다 잃어야 한다.
  - **「이익의 50%」의 밑절미가 매매차익이었다**(ISSUES #30). 쌍무로 태운 지 19일에 그의 몫이 3,263닢.
    **근거 JSON(`commendaSplit`)에 「경비를 누가 물었는지」가 없다** — 그래서 비율을 안 건드리고
    (없는 근거로 수치를 만들지 않는다는 규약) **화면이 실제와 같은 말을 하게** 했다:
    *"**매매차익**의 N% (항해비·급여·보험은 내가 문다)"*. 그리고 **여태 그가 가져간 액수**를 카드에 적었다 —
    19일에 3,263닢이었는데 화면 어디에도 그 수가 없어 「언제 내릴 것인가」를 판단할 재료가 없었다.

  - **P6-1 소식이 그 구간의 시세를 함께 판다 — 수치는 하나도 안 바꿨다.**
    실플레이의 −301닢은 **빈 배로 순찰했기 때문**이지 그 구간이 나빠서가 아니었다.
    명부 40명의 `hunt` 구간을 무역으로 뛰면 **적자가 0/40**이고 중앙이 기준선의 1.16배다 —
    **해적은 털 것이 지나가는 곳에 앉아 있다.** 명부를 만든 사람이 이미 그렇게 앉혔고,
    게임이 그 사실을 화면에서 말하지 않았을 뿐이다. 사료도 한 사람이다: 팩토리아의 편지에는
    *"어느 항로에 코르세어가 있다"*와 *"그 항구의 후추 값이 얼마다"*가 **같은 장에** 적혔다.
    ⇒ `buyBountyTip`이 그 `hunt` 구간 양끝 항구를 `state.scouted`에 넣고, 어느 구간인지도 말해 준다.
  - **P6-2 현상금을 `capSpoils` 밖으로 — 액수는 그대로, 상한만 뗐다.**
    `capLoot`의 정당화는 *"낡은 바사 갑판에 여섯이 서서 갤리엇의 금고를 통째로 옮길 수는 없다"* —
    **옮겨 싣는 것**이라 상한이 있는 것이다. 그런데 **현상금은 옮겨 싣는 물건이 아니다**:
    나포 포상(prize money)은 나포심판소 판결 뒤 **항구에서 관이 장부로 치른 돈**이고 갑판 인원과 무관했다.
    ⇒ `pirateEnemy`가 `bounty`를 따로 들려 보내고, 이기면 `state.bountyDue`에 적었다가
    **다음 입항에 지급**한다(`oweBounty`·`payBounties`). ⇒ **이기고 살아 돌아와야 받는다**는 대가가 하나 생긴다.
    실측: 중반(브리간틴·금고 8,000닢) 세기 5 실수령 **3,953 → 11,107닢(+181%)** —
    **가난할 때 가장 크게 갈린다.** 큰 현상금이 가장 필요한 때가 바로 그때다.
  - **P6-4 소식값 하한을 현상금에 묶었다.** 고정 200닢이 **세기 1의 현상금 하한(180닢)보다 비쌌고**
    명부 40명 중 여섯이 그랬다 — 사다리의 첫 칸이 마이너스로 시작했다.
    ⇒ `max(tipFloorAbs 40, bounty[0]×0.35, bounty[1]×0.05)`. 잔챙이 소식 **200 → 63닢** ·
    거물 소식 **600 → 1,750닢**. **소식이 현상금보다 비싼 자 6명 → 0명.**
  - **P6-5 항로 카드가 사냥터를 짚는다 — 규칙 변경 0.** `huntedOnLeg`는 `riskKey`가 `def.hunt`와
    **정확히 일치**해야 열리므로, 어느 줄이 그 구간인지 화면이 말해 주지 않으면 그 규칙은 없는 것과 같다.
    **이 표시가 실제 성공률을 정한다.** 함께 *"짐이 값나갈수록 그자가 붙는다"*를 적었다 —
    `cargoLure`는 **이미 있는 규칙**이고, 순찰하며 무역하는 것이 사냥을 **돕는다**는 사실의 통역이다.
  - 곁에서 하나 — **초행 툴팁이 아직 `state.known`을 보고 있었다**(배지는 `priceKnown`인데).
    지난 회차 P5에서 놓친 자리라 *"시세는 닿아야 안다"*가 거점을 세운 뒤에도 그대로 떴다. 고쳤다.

  - **사다리 전/후** (`node tools/sim-roster.mjs` 신설 · 갈레온 단독 · 금고 60,000닢 · 순찰 10일)

    | 세기 | 소식값 | 실수령 | **사냥 수지** | 빈배로 순찰 | 사냥터 무역(10일) | 적자 사냥터 |
    |---|---|---|---|---|---|---|
    | 1 | 200 → **63** | 625 | 425 → **562** | −2,504 | +29,093 | **0/8** |
    | 2 | 200 → **158** | 1,545 | 1,345 → **1,387** | −1,679 | +22,377 | **0/9** |
    | 3 | 200 → **385** | 3,930 | 3,730 → **3,545** | +479 | +21,032 | **0/6** |
    | 4 | 280 → **910** | 8,060 | 7,780 → **7,150** | +4,084 | +18,032 | **0/12** |
    | 5 | 600 → **1,750** | 14,380 → **15,700** | 13,780 → **13,950** | +10,884 | +21,840 | **0/5** |

    **빈배로 순찰하면 세기 1~2가 적자인데, 그 구간을 무역하며 돌면 전 세기가 흑자다.**
    ⇒ 고칠 것은 현상금이 아니라 **정보**였다는 것이 이 표의 결론이다.

  - 파일: `js/data.js`(`ROSTER.bountyFloorRate/tipFloorAbs`) · `js/state.js`(`huntLegs`·`oweBounty`·
    `payBounties`·`state.bountyDue` · 청산이 코멘다 정리 · 소식값 하한) · `js/world.js`(현상금 분리) ·
    `js/scenes/battle.js`(현상금은 항구에서) · `js/scenes/map.js`(입항 지급 · 사냥터 표시 · 툴팁 정정) ·
    `js/scenes/port.js`(밑절미 문구 · 여태 가져간 액수 · 사냥터 안내) · `tools/sim-roster.mjs` 신설 ·
    `tools/test-rules.mjs`(+15검사) · `dashboard/architecture.mjs`(도구·state 등재).
  - 검증: **규칙 165/165**(150 → 165) · `check-*` 12종 전부 통과 ·
    `sim-stat 20` **4회: 완주 16~20/20 · 첫 배 10~11항차 · 10항차 금고 899닢(네 번 다 같다) · 30항차 8,379~14,230닢**.
    ★ **초반이 네 번 다 같은 값**인 것이 이 회차의 회귀 증거다 — 사냥·코멘다·소식이 전부
    **고르는 자리**에 붙어 있어 안 고르면 아무것도 안 바뀐다.


- **[2026-08-25] #trade #world #combat #balance** — **자본이 늘 때 버는 「방식」이 달라진다 — 초행 정보 · 선단의 입장권 · 동료 코멘다.**
  사용자가 방향을 직접 정했다(*"많이 벌어도 유지비용도 같이 상승하고 동료를 모으고 여러배를 같이 운용하고 …
  게임레벨 에이전트가 적절히 벌게 해야 하고 그래야 버는 재미 새로운 무역로를 뚫는 재미도"*).
  게임레벨 담당의 설계문 `.playtest/supremacy-balance/DESIGN-growth.md`에서 **P5·P2·P3**을 맡았다
  (P1 원양 데이터·P4 비용 축은 다른 개발자가 같은 시각에 맡았다).
  ★ 설계문의 진단 한 줄이 이 회차의 전제다 — *"없는 것은 **자본이 늘었을 때 열리는 새 벌이 방식**이다.
  지금은 2만닢인 상인과 30만닢인 상인이 똑같이 2일짜리 근해를 왕복한다."*

  - **P5 초행이 손해가 아니게 — 「보너스가 아니라 정보」.**
    임차창고는 `tariffCut 0 · depthUp 0`이라 **수입 효과가 정확히 0인데** 30일마다 유지비만 나갔다
    (265곳을 사라는 패권 목표와 정면으로 어긋났고, 투자의 14%가 순수 지출이었다). 사료가 그 자리를 채운다 —
    **상관망(팩토리아)의 첫 값어치가 가격 정보**였고 팩터의 편지가 곧 상품이었다.
    - `HOLDINGS.rental.priceTip` — 임차창고가 선 항구는 **그 값이 내게 온다**(문을 닫으면 소식도 끊긴다).
    - `HOLDING.scoutNeighbors 3` — 닿은 항구가 **가까운 직항 이웃 셋**의 값을 함께 연다(부두의 소문).
    - 지도 「값이 흔들리는 곳」이 **내 거점이 선 바다를 먼저** 세운다 — 거점이 늘수록 회로를 넓힐 수 있게 된다.
    - ★ **`known`과 `scouted`를 갈랐다.** 처음엔 소문도 `known`에 넣었는데 `metFactions()`가 그것을 세어
      **가 본 적 없는 세력을 만난 것으로** 셌다(`check-factions`가 잡았다). 들른 곳은 `known`, 소문은 `scouted`다.
    - ⚠️ 차익은 한 톨도 안 바뀐다 — **못 보던 시절의 손해**(초행 벌금 · conquest ISSUES #16: 8항차 23일에
      6,661 → 5,448닢)를 없앤 것이지 값을 올린 것이 아니다.

  - **P2 선단이 「한 척으로는 못 하는 일」을 갖는다 — 적재 배수는 주지 않는다.**
    동행 8척은 하루 +733닢을 먹고 적재 200→1,588칸이 **전부 무용**이었다(2일 항차 −1,466닢).
    사용자가 명시적으로 요구한 플레이인데 게임이 벌하고 있었다. **입장권 셋**을 준다.
    - **ⓐ 원양은 혼자 못 건넌다**(`FLEET.escortAt` · `oceanReady(to)`) — 요율 8.5 초과면 동행 2척,
      6.0 초과면 1척. 사료: 1561년 이후 **카레라 데 인디아스는 함대(flota) 편성이 의무**였고 무장 호위
      없는 상선은 출항 허가가 안 났다(`lossCause` — **손실 원인의 60%가 코르세어**).
      ★ 항로 데이터에 필드를 더하지 않고 **요율에서 뽑았다** — `OCEAN_LANES`는 권역 담당이 늘리는 자리라
      손으로 적게 하면 새 항로에서 조용히 빠진다. **근해는 그대로 열려 있다**(막으면 항구에 갇힌다).
    - **ⓑ 계약 수량이 선복을 따라 큰다**(`CONTRACT.consortHold`) — 선금 상한은 이미 `fleetCollateral()`
      (선단 전체)에 묶여 있는데 수량만 기함 기준이라 **반쪽만 배선돼 있었다**.
      ★★ **설계문이 제안한 0.6은 실측에서 기각했다.** 지켜야 하는 선은 *"보수 배수 < 적재 배수"*인데
      0.6은 적재 ×2.60에 보수 **×4.10**(동행 1척) · 적재 ×8.04에 보수 **×10.63**(8척)으로 **배수를 넘겼다.**
      설계문이 못 본 것은 **수량 상한과의 맞물림**이다 — 값싼 부피화물은 단독일 때 `qtyCap`에 잘려 보수가
      목표에 못 미치는데 동행이 그 자물쇠까지 풀어 주므로 **보수가 두 번 오른다.**
      120장×2세트를 쓸어 **0.25**를 골랐다(0.60 ×4.10/×10.63 · 0.35 ×2.88/×8.22 · **0.25 ×2.38/×6.33** ·
      0.15 ×1.85/×4.34) — 0.25에서 처음으로 둘 다 적재 배수 아래로 들어온다.
    - **ⓒ 선단이 보험료를 깎는다**(`FLEET.insureOffPer 0.08` · `insureOffCap 0.40`) — 함대 항해분 요율이
      단독보다 뚜렷이 낮았다(같은 `lossCause` · `insurancePremium`의 p10 0.75% ↔ p90 22%라는 30배 스프레드).
      실측(세비야→아바나·와인 150칸): 32닢 → 동행 1척 −9% · 3척 −25% · 5척 **−41%(상한)**.
    - ⚠️ **적재 배수는 안 생겼다** — `sim-fleet 20 40`에서 채운 칸이 여전히 **160→110**이고 순무역
      항차 순이익은 **−355닢**이다(설계 합격선 −300~0의 언저리). 선단의 값어치는 **원양(P1)과 대량 계약**에서
      나오도록 설계돼 있고, 그중 원양 수입은 다른 개발자의 P1이 든 뒤에 확인된다.

  - **P3 동료 = 코멘다 — 같은 사람이 초반엔 자본이고 후반엔 비용이다** (`UNIMPLEMENTED` A-5 · 데이터 51명 배선).
    아홉 권역 `npc-mates.js`가 다 있는데 **`regions/index.js` 배선이 없어 게임에 아무 영향이 없었다.**
    계약 모양은 사료 그대로다(`commendaSplit` — 1199년 계약 원문 · 381건 데이터셋):
    **편무 75/25 · 쌍무 50/50(항해자 자본 1/3)**. 게임에서는 플레이어가 투자자(stans)다.
    - **쌍무로 태우면 자본이 늘어난다** — 계약금 800닢 내고 밑천 2,400닢을 받는다(`COMMENDA.stakeMul 3.0`).
      초반은 **칸이 남고 돈이 없다**(계약 96건 중 자금부족 74) — 그 자리를 정확히 채운다.
    - **그 대신 이익의 25~50%가 그의 몫이다.** 후반에는 그것이 순손실이고, 밑천은 **내릴 때 돌려준다**
      (코멘다는 출자이지 증여가 아니다) — 그래서 **후반에 반드시 비싸진다.**
    - **`FLEET.requireCaptain`을 켰다** — 동행선에는 선장이 있어야 하고 배 하나에 선장 하나다.
      ⇒ **선단 규모가 동료 수에 묶인다.** 사용자가 *"동료를 모으고 여러배를 같이 운용하고"*를
      한 문장에 붙여 쓴 구조가 이것이다.
    - 특전은 부관·갈래와 **같은 자리에서** 더해진다(`matePerk` — 새 계산 경로를 안 판다).
      명부가 쓰는 열 키 중 여덟을 배선했다: `impactOff`·`tariffOff`·`repairOff`·`sailDaysOff`·`hullUp`·
      `upkeepOff`·`fleeUp`·`meleeUp`. **남은 둘은 `gunUp`·`crewLossOff`**(전투 씬 안쪽이라 따로 봐야 한다).
    - ⚠️ **강제되지 않는다.** 태우지 않으면 아무것도 안 바뀐다 — 너프가 아니라 **능력에 붙은 값**이다.

  - **함께 고친 실플레이 결함 셋**(4차 회차 · 넷은 화면에서 확인됐다 — 소식·초무·거점 유예·삭은 배):
    - **ⓐ 초무한 자가 39일 뒤 항로를 막았다**(ISSUES #26 · 큼) — **처방을 두 번 썼고 두 번째가 맞다.**
      920닢을 치르고 *"이제 우리 배는 건드리지 않는다"*를 받았는데 그자와 싸워 나포했고
      `tamed`와 `slain`이 **둘 다 박혔다.** 코드로 확인한 원인: `pickDef`는 **새로 만들 때만**
      닫힌 명부를 거르고 **이미 떠 있는 배**는 그대로 둔다(테스터의 추정이 맞았다).
      ⇒ **1차 처방(폐기): 그 배를 지운다.** `retireRosterShip()`으로 지웠는데, 그 사이 밸런스 담당이
      낸 `DESIGN-growth.md` P6-4를 읽고 **되돌렸다.** 지워 봐야 **정원이 안 줄어** `standIn`이
      얼굴 없는 배로 그 자리를 채운다 — **바다가 하나도 안 안전해지고** 24,000닢을 내고
      체크리스트에서 이름 하나가 지워질 뿐이다(코드로 확인).
      ⇒ **2차 처방(지금): 고칠 자리는 「그자가 내 배를 안 건드리는 것」이었다.**
      그리고 **사료가 그 답을 명부 `blurb`에 이미 적어 두었다** — 정지룡은 1628년 초무로
      해방유격이 되어 **다른 해적을 소탕했고**, 무라카미 수군은 **과소기(過所旗)**로 안전 통행을
      보장했다. **둘 다 사라진 사람이 아니라 일해 준 사람이다.** 그래서 값(`tameMult 2.0`)은
      안 건드리고 **효과를 붙였다**: **과소기**(그 권역 조우 −12%/명·상한 −40%) +
      **토벌 협조**(그 권역 남은 명부의 소식값 −20%/명·상한 −60%). 효과는 **권역에 매이고**
      그 바다에 거점이 없으면 **180일 뒤 식는다**(`ROSTER.tameGraceDays` — 과소기는 갱신하는 것이었다).
      실측: 사카이~히라도 조우 **18.0% → 15.8%** · 왕직 소식값 **600 → 480닢** · 지중해는 그대로 ·
      반년 뒤 0으로 식고 거점을 세우면 되살아난다.
      ⇒ **초무가 처음으로 선택지가 된다** — 한 번 지나갈 사람에게는 여전히 비싸고,
      그 바다를 오래 쓸 사람에게는 남는 장사다. **`retireRosterShip`은 격파 전용으로 남겼다**(죽은 자는 죽었다).
      옛 세이브를 위해 `scenes/map.js`의 조우 갈래가 한 번 더 거른다.
    - **ⓑ 밀린 유지비를 그 자리에서 낼 단추가 없었다**(ISSUES #22). 규칙은 *"내면 그 자리에서 다시
      연다"*인데 화면에 낼 방법이 없어 **금고가 7,105닢인데도 나갔다 와야(6일)** 문이 열렸다 —
      「한 번 더 밀리면 압류」라 **항로가 긴 자리에서는 돈이 있어도 넘어간다**(막으려던 바로 그 사고다).
      거점 카드에 「밀린 유지비 N닢 — 낸다」 한 줄을 넣었다.
    - **ⓒ 「청구 3닢 · 납부 1닢」은 결함이 아니었다**(ISSUES #23). 문을 닫은 동안에는
      `HOLDING.idleRate`(절반)만 문다 — 그리고 **반올림 전 값에 곱한다**(2.95×0.5=1.475→1,
      3×0.5=2가 아니다). 설계대로이므로 값은 안 건드리고 **화면 문구가 그 이유를 적게** 했고,
      검사로 박아 두었다(같은 보고가 다시 오지 않게).

  - 파일: `js/data.js`(`HOLDINGS.rental.priceTip`·`HOLDING.scoutNeighbors`·`FLEET.escortAt/insureOff*`·
    `CONTRACT.consortHold`·`COMMENDA` 신설·`FLEET.requireCaptain` 켜기) · `js/state.js`(`knowPort`·`priceKnown`·
    `holdingTip`·`escortNeed`·`oceanReady(to)`·`convoyInsureOff`·동료 규칙 일곱·`state.scouted`·`state.mates`) ·
    `js/regions/index.js`(`ALL_MATES` 합치기 — 아홉 권역 `mates` 배선) · `js/scenes/map.js`(초행 표시·시세 한 줄·
    소문 정렬·호위 안내) · `js/scenes/port.js`(동료 카드) · `js/scenes/battle.js`(`meleeUp`) ·
    `tools/test-rules.mjs`(+22검사) · `dashboard/architecture.mjs`(state 둘 등재).
  - 검증: **규칙 150/150**(117 → 150) · `check-*` 12종 전부 통과 · `check-factions` **45/45** ·
    `sim-stat 20` **7회: 완주 17~20/20 · 첫 배 10~13항차 · 10항차 금고 854~899닢 · 30항차 9,509~17,147닢**
    (고치기 전 17~20/20 · 첫 배 10~14항차 · 10항차 639~963 · 30항차 10,543~17,364 — **설계문 회귀 기준
    「10항차 금고·첫 배 항차 ±10%」 안**). ★ **초반은 한 톨도 안 움직였다**(10항차 898~899닢) —
    P5·P2·P3·P6-4가 전부 **고르는 자리**에 붙어 있어 안 고르면 아무것도 안 바뀌기 때문이다. `sim-fleet`에서 **적재 배수가 안 생긴 것**을 확인했다.
  - ★ `changelog.md`가 상한(40,960)을 채워 **시점 경계로 무손실 분할**했다 —
    08-23~24분을 [changelog-2026-08-23-24.md](changelog-2026-08-23-24.md)로 옮겼다(한 줄도 안 고쳤다).


- **[2026-08-25] #trade #world #engine #balance** — **명부를 찾아갈 수 있게 하고, 삭은 배에 값을 물리고, 시작 항구가 곧 아는 항구가 됐다. 그리고 「첫 배를 못 산다」는 회귀가 아니었다.**
  실플레이 3차 회차(`.playtest/supremacy/ISSUES.md` 18건 · 251일차 청산까지)가 낸 넷을 받았다.

  - **★6 「계약 사다리의 첫 칸이 안 밟힌다」 — 회귀가 아니다. 코드를 안 고쳤다.**
    보고는 *"여수의 다음 배가 조운선 1,150닢인데 여수↔강진만 왕복하면 자본이 800닢에서 멈춘다"*였다.
    **판정을 위해 `tools/sim-firstship.mjs`를 새로 만들어 열한 시작 항구를 전부 쟀다**(12시드 × 40항차):
    - **계약 없이 무역만으로 12판 중 12판이 첫 배를 산다** — 부산포 **10항차/21일** · 여수 **8항차/17일** ·
      가장 느린 호르무즈도 18항차/42일. 곧 **계약은 지름길이지 유일한 길이 아니다.**
    - 같은 도구로 이웃을 **둘로 잠그면**(`runSim`에 `only` 자물쇠 신설) 자산 천장이
      부산포 46,926 → **6,650닢**, 여수 53,625 → **6,872닢**으로 무너진다. **벽을 만든 것은 밸런스가 아니라
      두 항구만 왕복한 플레이 방식**이고, 그것은 `wiki/playtest-harness.md` §3이 이미 적어 둔 함정이다
      (`sim-core: bestRun`이 왕복에 15% 벌점을 두는 이유이기도 하다).
    - 참고로 여수의 가장 싼 다음 배는 조운선(1,150)이 아니라 **코카 1,023닢**이다.
    - ⇒ **선금 하한을 손대지 않았다.** 손대면 없앤 「1일차에 자산의 2.45배」가 되돌아온다.
      `sim-contract` 판정선도 그대로다(1일차 선금/자산 **0.35배** · 자금이닿나 13%).

  - **★7 명부 해적을 만날 수가 없었다 — `bounty-tip`과 초무를 붙였다**(SPEC-supremacy §1-3 (b)·(c)).
    **984 게임일에 명부 조우 0회.** 원인은 `npcsOnLeg`가 *"그 배의 `at`·`to`가 정확히 내 두 항구일 때만"*
    잡고 아니면 `pickEnemy()`가 **이름 없는 적**을 내는 것이다 — 264 도시에서 그 일치는 사실상 안 난다.
    상선이 겪던 것과 같은 문제인데(C-15) **폴백을 상선만 받았다.**
    - **소식을 산다**(`buyBountyTip` · 값 = `max(200, 현상금×5%)` · `tipDays` 20일) → 그자의 `hunt`·`base`에
      닿는 항로로 나가면 `world.js: huntedOnLeg()`가 **그자를 세운다**(명부의 배·세기·현상금 그대로).
    - **초무**(`tamePirate` · `bounty[1] × 2.0` · **소굴 항구에서만**) → `state.tamed[id]`로 명부가 닫힌다.
      격파하면 현상금을 **받고** 초무하면 그 두 배를 **낸다** — 못 이길 상대도 길이 막히지 않는다.
    - ⚠️ **해적을 약하게 만들지 않았고 밀도도 안 올렸다.** 조우 확률은 그대로이고 **누가 오는가**만 바뀐다 —
      *"사람은 이길 수 있는 상대만 싸운다"*를 깨는 것이 아니라 **살리는** 쪽이다(찾아갈 수 있으면 고를 수 있다).
    - `state.tamed`를 **명시 선언**하고 `resetGame`이 비우게 했다 — `??=`로만 만들면 새 판에 옛 판의 초무가
      살아남는다(`slain`이 같은 이유로 그렇게 돼 있다). `dashboard/architecture.mjs`에 등재.
    - 화면은 **패권 카드의 명부 줄**에 얹었다 — 사이드패널은 이미 카드 열둘이라 열셋째가 넘으면
      출항 단추가 화면 밖으로 밀린다(`port.js` 947행 주석의 사고).

  - **★8 선체 1/55로 열여섯 항차를 뛰어도 잃는 것이 없었다 — 막지 않고 값을 물렸다**(C-13).
    C-13의 설계 메모 셋 중 **①(느려진다)·②(짐이 젖는다)**를 골랐다(`data.js: HULL`).
    실측: 선체 55/55 → 1/55에서 베네치아~이스탄불이 **10일 → 14일**(삯·보급·보험이 그만큼 는다).
    침수는 **값싼 것부터**(폭풍 투하와 같은 규약)라 **성장에 비례**한다 — 부자일수록 수리한다.
    - ⛔ **출항 금지·강제 정박을 안 고른 이유**: 실플레이가 금고 0·선체 1에서 **1일 항로 열여섯 항차로
      0 → 1,720닢**을 벌어 빠져나왔다(ISSUES #14). 막으면 3일 항로뿐인 항구에서 영영 못 나가는
      **새 데드락**이 된다 — `oceanReady` 주석의 *"항구에 갇히는 일이 없어야 한다"*와 같은 원칙이다.
      실제로 짧은 항로는 `voyageDays`의 `max(1, …)`에 걸려 그대로 1일이고 침수도 0이라 **그 탈출구는 온전하다.**
    - ⛔ **침몰(N4)도 아직 아니다**: 폭풍은 항차당 12%(`SEA_EVENTS`)라 선체 1에서 치명적으로 만들면
      그 열여섯 항차가 **87% 확률로 끊긴다.** 침몰은 우연이 아니라 플레이어가 고른 자리에 붙어야 한다.

  - **★9 어느 항구에서 시작하든 첫 줄이 「베네치아 부두」였다** — `resetGame`의 `known: new Set(['venezia'])`와
    로그 문구가 지중해 시절 하드코딩 그대로였다. 이제 **시작 항구가 곧 아는 항구**다.
    ★ 곁가지 하나가 드러났다 — `check-factions`의 *"새 판에서 만난 세력은 하나(베네치아)"*가 **그 하드코딩에
    기대고 있었다.** 부산포에서 시작하면 조선의 바다에는 세력이 없으므로 **0이 맞고**, 그 검사가
    「베네치아에서 시작하면 하나가 된다」로 정정됐다(45/45).

  - 파일: `js/data.js`(`HULL` 신설 · `ALL_PIRATES` 재수출) · `js/state.js`(`hullFactor`·`soakCargo` ·
    `rosterOpenIn`·`bountyTipPrice`·`buyBountyTip`·`tamePrice`·`tamePirate`·`activeBounty` · `state.tamed` 선언 ·
    `known`/시작 로그) · `js/world.js`(`huntedOnLeg`) · `js/scenes/map.js`(조우 배선 · 침수 로그) ·
    `js/scenes/port.js`(명부 줄에 소식·초무) · `tools/sim-core.mjs`(`only` 자물쇠) ·
    `tools/sim-firstship.mjs` 신설 · `tools/test-rules.mjs`(+20검사) · `tools/check-factions.mjs`(정정) ·
    문서 `wiki/world-npc.md`(명부 사냥) · `UNIMPLEMENTED.md` C-13 · `QUICKMAP-{trade,world,art}.md`.
  - 검증: **규칙 117/117**(97 → 117) · `check-factions` **45/45** · `check-*` 12종 전부 통과 ·
    `sim-stat 20` **4회 실행: 완주 17~20/20 · 체불 0~5% · 첫 배 10~14항차 · 10항차 금고 639~963닢 · 30항차 10,543~17,364닢**(고치기 전 16~19/20 · 10항차 898~899 · 30항차 8,970~10,543).
    ★ **초반은 그대로거나 눌렸고**(10항차 금고 899 → 639~963 — 삭은 배 벌점이 무는 자리가 거기다), 후반 폭이 넓어졌다. 선체 상태가 판을 가르기 시작했다는 뜻이라 **분산이 커진 것 자체가 의도**다 — 다만 30항차 중앙값이 위로 밀린 것은 「느려진 만큼 시장이 더 회복한다」(`MARKET.decay`)는 곁효과일 수 있다. **1회 실행으로 판단하지 않는다는 규약대로 네 번을 적어 둔다** — 다음 회차가 다시 잴 자리다.
  - ★ **`check-memory-routing`이 실패하고 있었다**(`claude-memory.md` 16,025B / 상한 15,360). 무손실로
    **15,327B**까지 줄였다 — 지도 PNG 절차를 `QUICKMAP-art.md` §3으로 **이관**하고(실존 확인 후),
    나머지는 상세가 이미 도메인 파일에 있는 줄을 **결론+포인터**로 접었다. 트립와이어 전 항목 ✅.


