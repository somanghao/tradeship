# changelog — tradeship

> 변경 이력 정본(lazy·거의 안 읽음). `claude-memory.md`에는 **현재 상태만** 두고,
> "무엇을 어떻게 고쳤다"는 여기에 쌓는다.
> 상한 40KB — 넘으면 시점 경계로 분할한다. 과거분:
> [changelog-2026-08-30.md](changelog-2026-08-30.md)(08-30 · 회차 26·27·28) ·
> [changelog-2026-08-29-pm.md](changelog-2026-08-29-pm.md)(08-29 뒷부분 · 회차 24·25) ·
> [changelog-2026-08-29-am.md](changelog-2026-08-29-am.md)(08-29 앞부분 · 회차 22·23) ·
> [changelog-2026-08-25-pm.md](changelog-2026-08-25-pm.md)(08-25 오후·저녁) ·
> [changelog-2026-08-25-am.md](changelog-2026-08-25-am.md)(08-25 앞부분) ·
> [changelog-2026-08-23-24.md](changelog-2026-08-23-24.md)(08-23 ~ 08-24) ·
> [changelog-2026-08-22.md](changelog-2026-08-22.md)(08-22 하루) ·
> [changelog-2026-08-17-21.md](changelog-2026-08-17-21.md)(08-17 ~ 08-21) ·
> [changelog-2026-08-16.md](changelog-2026-08-16.md)(08-16 하루) ·
> [changelog-2026-08-15.md](changelog-2026-08-15.md)(08-15 하루) ·
> [changelog-2026-08-early.md](changelog-2026-08-early.md)(08-08 ~ 08-15 앞부분).

## 2026-09

- **[2026-09-04] #world — 회차 31 이어서, 「동아시아 세 문」 조사·판정.** 사용자 지시 셋을 한 묶음으로 받았다 —
  조선 *"왕에게 미션 받고 개항하고 작위 받아서 공업력 올리고 … 최소 몇개지역 패자되면"* ·
  명 *"뇌물로 각 지방 항구 관리자 이용해서 수익"* · 일본 *"강제 개항 이벤트 해군력이 일본 전역보다 세지면"*.
  **조사만 했다 — 코드는 한 줄도 안 고쳤다**(A-8e 러너가 `js/`를 HTTP로 실시간으로 읽는 중).
  결론 정본은 **`UNIMPLEMENTED.md` §A-11**, 상세는 `.playtest/round-31/`의 넷.
  - ★★★ **조사 셋이 독립적으로 같은 결론에 닿았다 — 세 나라 다 이미 열려 있다.**
    조선 9 · 명 13 · 일본·류큐 13이 **전부** 항로·거래·관세를 갖고 있고 어느 층에도 잠금이 없다.
    ⇒ **세 기능 다 「여는 일」이 아니라 「먼저 닫는 일」**이고, 그것이 이 묶음의 진짜 규모다.
    ⛔ 항로를 **지워서** 닫으면 안 된다(`check-world` ①이 265항구 도달성을 정적으로 본다) —
    선례는 `oceanReady`/`escortNeed`의 *"막지 않고 조건을 붙인다"*이다.
  - ★★★ **판정 다섯을 닫았다**(상위가 전부 판정 함수에 직접 물어 확인) — 상세는 §A-11의 표.
    ㄱ 패권은 **막지 말고 값을 물린다**(동아시아 41항구 중 **26곳 63%**가 명+일본+류큐다) ·
    ㄴ 조정은 **세력이 아니라 인물** · ㄷ 명↔일본 연쇄는 **의도로 두되 화면이 말하게** ·
    ㄹ 동아시아 쏠림은 **진행** · ㅁ 미션 N은 **3, 동아시아 필수 금지**(고리가 생긴다).
  - ★★ **조사가 「구멍」이라 낸 것이 실은 「의도」였다.** 조선 9항구에 세력이 0곳인 것을 조사가
    구멍으로 보고했는데, `check-factions`가 **`busanpo: null`을 명시적으로 잠갔고** 주석이
    *"집의 바다에만 세력이 없다 … 뒤집히면 **소설 28·64·68장의 뼈대가 무너진다**"*라고 적어 두었다.
    ⇒ **상위가 보고를 액면 그대로 받지 않는다**는 규약이 또 값을 했다(회차 29·30에 이어 세 번째).
  - ★★ **자를 잘못 대 두 번 뒤집혔고, 둘 다 상위 쪽이었다.**
    ⓐ **공업력 4는 「기술」이 아니라 「교역권」을 연다.** 상위가 `tierNeeded`만 보고
      *"3→4에서 선종이 안 는다(124→124)"*고 적었는데 틀렸다 — 사다리가 둘이라 둘 다 보면
      부산포 **1→24 · 2→35 · 3→63 · 4→124종**이고, 딴 바다 배 61종이 전부 `yardReach 4`다.
      ⇒ **공업력 4 = 조선이 세계의 배를 짓게 되는 것**이고 그것이 곧 개항의 뜻이라 설계가 맞아떨어졌다.
      ⚠️ **회차 29가 이 함정을 적어 뒀는데 그대로 밟았다** ⇒ `QUICKMAP-combat.md` §3으로 내렸다.
    ⓑ **「얇은 바다」도 자가 틀렸다**(앞 기록 참조) — 같은 회차에 두 번이다.
  - ⛔ **뇌물로 세를 깎는 설계는 이미 죽어 있다** — `BOON.tariffFloor 0.25`라 광저우는
    감합+밀수+부관으로 **이미 바닥 1.875%**다. 넷째 감면은 한 자리도 안 움직인다
    ⇒ `QUICKMAP-trade.md` §3으로 내렸다.
  - ⚠️ **`FIGURES`를 보는 검사가 하나도 없다**(확인). 세 문이 **전부 인물에 앉는 설계**라
    `service` 철자 하나가 틀리면 조용히 아무 일도 안 난다 ⇒ **`check-figures`가 구현 1번**이다.
  - **고증이 셋 다 시대에 맞게 착지했다** — 조선은 근대 개항이 아니라 **삼포개항**(내이포·염포·부산포가
    게임에 이미 다 있다)이고 작위는 **공명첩·납속**(서얼허통이 `interpreter`·`bastard` 갈래의 대가를 푼다) ·
    명은 *"뇌물이 아니라 관례(陋規)였다"*(1553~54 왕백 — 지방관 한 명에게 값을 치른 것이 마카오가 됐다) ·
    일본은 *"이 시대에 무력이 항구를 연 사례는 없다"*(나가사키는 1571년 오무라가 **내줬다**)
    ⇒ 사용자의 「밀어서 연다」는 살리되 **미는 대상을 옮겼다**(해역의 치안을 쥐면 다이묘가 연다 · 1588 해적정지령).
  - **문서 드리프트 둘 더 정정** — A-3의 *"열린 부두가 하나뿐(항로가 마포·하카타 둘뿐)"*은
    **코드가 강제하지 않고** 부산포 이웃은 둘이 아니라 **다섯**이다 · B-1의 `permit` **90일 → 45일**(`BOON.permitDays`).
  - **메모리** — 라우터에 §A-11을 최종상태로 얹고 **도메인 전용 함정 둘은 도메인 파일 §3으로 내렸다**
    (라우터 15,223 → **14,796** · 여유 137 → 564). `QUICKMAP-world`에 세 문 라우팅 행과 함정 셋 추가.
    `check-memory-routing` 전 항목 통과 · dead pointer 0 · 고아 0.
  - **A-8e가 집 바다를 벗어났다** — 동아시아를 돌고 **대서양 라스팔마스 13/28 · 금고 1,154만**까지 갔다.
    아직 도는 중이라 결론은 다음 기록에. ⚠️ 로그에 하네스 결함 하나 — `✗ 수리가 안 됐다 · NO_BTN`.

- **[2026-09-03] #world #trade — 회차 31, 「해협·근거·선종」.** 사용자 지시 *"남은일 확인"* →
  *"이미지 지도 에셋들 빼고 나머지 리스트업 추천하는 방안"* → *"추천대로 진행하고 남미 북미는
  파나마 운하가 있으면 가면 되고 없으면 반대로 갈수 밖에 없지 않아?"*.
  - ★★★ **사용자가 빠진 항로 하나를 짚었고 그것이 남미의 구조 문제였다.** 파나마 운하는 1914년이라
    이 세계에 없다. 이 시대에 남미를 넘는 길은 둘뿐이었는데 **하나만 있었다** —
    지협 노새길(`panama|portobelo` + `panama~callao`)은 있고 **마젤란 해협이 없었다.**
    그래서 남아메리카는 **권역 안에서 두 조각**이었고(대서양안 15 ↔ 태평양안 13),
    브라질의 배가 카야오로 가려면 **카리브를 거쳐야** 했다. 패권 ①이 15/28에서 멈춘 자리다.
    ⇒ `sanjulian~valdivia` · **34일 · 요율 10.5 · `season: 'summer'`**.
    **권역 안 BFS 못 닿는 곳 13 → 0**(아홉 바다 전부 0).
    · **원양 항로로 그었다** — 산줄리안(248,212)→발디비아(104,214) 직선은 파타고니아를 관통한다.
      실제 길은 지도 아래로 내려가 해협을 지나는 길이라 **좌표로 못 잰다**. 같은 모양이 이미 넷 있었고
      그중 `luanda~mocambique`(희망봉 26일)가 정확한 선례다 — **권역 안인데 대륙 끝을 도는 길.**
    · **요율은 이 권역이 이미 쓰던 규칙 그대로** — `buenosaires|sanjulian`(8.0)이 *"무기항 앵커 9~11에
      가깝되 노리는 자가 없어 반 칸 낮췄다"*고 적었고, 해협은 그 최상단(11)에서 같은 이유로 반 칸 내렸다.
      ★ 희망봉(11.0)을 **안 넘긴 것은 의도** — 해협엔 노릴 자가 아예 없다(항구도 사람도 없다).
      위험의 성격이 해적이 아니라 날씨와 굶주림이라 그 몫은 `season`과 34일이 문다.
    · **동행 2척이 붙어야 넘는다**(요율 10.5 > `FLEET.escortAt` 8.5). 사료와 맞고,
      그래서 **지협은 초반 길·해협은 후반 길**이 된다.
    · 검증 `.playtest/round-31/m1-probe-strait.mjs`(신설) **10/10** — 브라우저 없이
      `escortNeed`·`oceanReady`·`ALL_ROUTES`에 직접 묻는다.
  - ★★ **자를 두 번 고쳐 대니 「얇은 바다」가 바뀌었다.** 처음에 «카리브가 교역품 8종으로 가장 얇다»고
    적었는데 **틀렸다** — 카리브 `goods.js`가 *"새 id를 만들면 이 바다 안에서만 도는 물건이 하나 늘 뿐"*이라며
    **일부러** id를 안 늘리고 다른 바다 품목에 산지를 붙이는 쪽을 택했다고 적어 두었다.
    ⇒ **「새로 정의한 id」가 아니라 「그 바다에서 실제 거래되는 품목」**으로 다시 재니
    인도양 27 ~ 중동 38이고 **항구당으로는 5.4~6.4로 고르다 — 얇은 바다가 없다.**
    ★ 진짜 얇은 것은 **선종**이었다(남미 9 · 중동 9 ↔ 지중해 17).
  - **남미 선종 9 → 12**(t1 5 · t2 3 · t3 3). 셋을 이 바다의 **서로 다른 조각**에 붙였다 —
    **차타**(과야스 강 평저 거룻배 · t1 900) · **피나사 칠로타**(칠로에 낙엽송 · t2 5,600 ·
    **hp 168로 이 권역 t2 최고** — 남쪽 물길에서는 안 깨지는 것이 곧 성능이다) ·
    **카피타나**(과야킬 무장 갈레온 · t3 24,000 · **포 26**).
    ★ **카피타나는 드레이크가 만든 배다** — 태평양이 *"지협 하나로만 들어오는 스페인의 호수"*라
      무장 안 한 배가 다니던 바다에 1579년 드레이크가 나타났고, 그 뒤로 과야킬이 포를 얹기 시작했다.
      같은 t3인 남해 나비오(250칸·포 12)와 **정확히 반대**다 — 그쪽이 *"안전한 바다에서는 배가
      상선이 된다"*면 이쪽은 그 안전이 끝난 뒤의 배다. `requires: 'galeota'`로 **사슬을 이 바다 안에서 닫았다.**
    ⇒ `regionTopShips('southamerica')`가 이제 셋을 돌려주고(`navio`·`capitana`·`naudobrasil`)
      회차 30의 고침이 **포·선체로 고르므로** 카피타나가 걸린다 — 남미 ③이 163일 걸린 자리다.
  - ✅ **근거 경고 24 → 0.** `WORLD-INTEGRATION-TODO §2`가 *"지중해·동남아·아메리카는 보고 대기"*로
    비워 둔 칸을 채웠다. `check-prices`가 **«전역 목표 대조 102종 · 권역 근거만 있는 것 0종 ·
    아직 근거 없는 것 0종»**을 내고 *"(경고는 위에)"* 문구 자체가 사라졌다.
    ★ **새로 정한 값은 하나도 없다** — 스물넷 전부 권역 근거에 base와 서술이 이미 있고 코드와
      일치하던 값이라, 한 일은 **「합의된 값」으로 못박은 것**이다(다음 사람이 흔들면 오차 12%로 잡힌다).
    ★ 여섯(원면·우지·육포·코이어·사르사파리야·안식향)은 권역 근거조차 없어 «미조사»였는데
      **근거는 코드 주석에 이미 다 적혀 있었다** — JSON으로 옮기지 않은 것뿐이었다.
  - **문서가 세 군데 낡아 있었다.**
    ⓐ ★ **패권 표가 스스로 어긋났다** — 지중해·동남아·카리브 세 줄이 ①②③④ **네 칸 다 찼는데
      종합만 3/4**였다(회차 30이 ③을 채우고 종합 열을 안 고쳤다). 종합은 그 줄의 네 칸을 세는 값이라
      **다른 측정 없이 판정된다.** ⇒ **4/4가 넷 → 일곱.** 이 표가 다음 회차의 목표를 정하는 자리라
      틀린 채로 두면 **이미 채운 바다를 또 돈다.**
    ⓑ 순서표 7번 「죽은 코드 13건」 — 회차 30에 이미 0이었다(`check-orphan-rules`가 «구멍 없음»).
    ⓒ `WORLD-INTEGRATION-TODO §1` 「향신료가 뒤집혔다(가장 급함)」 — 처리안 (A)가 2026-08-15에
      들어가 주석까지 남아 있는데 **1년 가까이 «가장 급함»으로 남아 있었다.**
  - **검증** — `test-rules` 486/486 · `check-world` 문제없음 · `check-routes` 문제없음 ·
    `check-prices` PASS(경고 0) · `check-map` 9/9(경고 8건 불변 — 해협은 원양 항로라 권역 지도
    항로검사에 안 걸린다) · `check-memory-routing` 경고 0 ·
    **`sim-stat 20` 3회 10항차 752닢 전부 동일** — 바다 하나를 잇고 선종 셋을 넣었는데 **곡선이 안 움직였다.**
  - ★★ **A-8e(한 판으로 아홉 바다)를 걸어 두고 그 위에서 회차를 돌렸다.** 러너가 `js/`를
    HTTP(`serve.py` 8891)로 **실시간으로 읽으므로** 도는 동안 게임 코드를 고치면 그 측정이
    조용히 낡는다(회차 29가 겪은 바로 그 모양). ⇒ **무엇을 언제 넣을지를 순서로 갈랐다**:
    ⓐ 해협은 러너를 **띄우기 전에** 커밋했다(`eb55c01`) — 그래서 이 판은 **해협이 있는 세계**를 잰다.
    ⓑ 근거 JSON(`content/**`)은 **`js/`가 안 읽는다**(검사기만 읽는다) ⇒ 도는 중에 넣어도 안전하다.
    ⓒ 선종 셋은 `js/`라 **워크트리 브랜치 `r31-content`에 두고 A-8e가 끝난 뒤 합친다.**
    ⚠️ **A-8e 진행 중 · 결과는 다음 기록에.** 로그 `.playtest/round-31/a8e-grand.log`.
  - **`changelog.md` 분할** — 35.8KB라 2026-08-30분(회차 26·27·28)을 `changelog-2026-08-30.md`로 옮겼다.

- **[2026-09-02] #engine #world #trade — 회차 30, 「R5·근거·규칙감사」.** 사용자 지시
  *"커밋 푸시하고 지도 이미지 캐릭터들은 빼고 나머지 진행"* → MAP과 그림 넷을 빼고 나머지를 밟았다.
  회차 29가 워크트리에 미커밋으로 남아 있어 먼저 검사 열여덟·`test-rules` 486/486·스모크 0을
  다시 돌려 세 갈래로 올렸다(`cfd45db`·`f0c9a3c`·`185d97d`).
  - ★★★ **패권 ③이 안 채워지던 원인은 배가 아니라 선단이었다(R5).** 회차 29는 「만나서 싸웠고
    다 졌다」로 적고 *"카리브엔 t3가 있으니 배가 원인이 아니다"*까지 갈랐는데, **그 다음 칸**이
    이것이었다 — 로그를 다시 보니 사냥 내내 **동행 0척**이었다. `r5-probe-power.mjs`가 게임의
    피해 식으로 직접 풀었다: **동행 0척이면 어떤 t3 기함으로도 지고 2척이면 이긴다**
    (포 `gunShare` · 피격 `shieldPer` · 갑판 `meleePerHand`가 한꺼번에 걸린다).
    ⇒ **카리브 ③ 달성**(스페인 은함대 호위기함 · 18항해째 89일차 · **7전 7승 · 진 것 0건**)에
    이어 **중동 ③도 달성**(포르투갈 인도 함대 · **12일차** · 4전 4승 · 등급5를 두 번 이겼다).
    회차 29엔 카리브가 3전 3패, 중동이 **9전 9패 · 금고 −1.63M**이었다.
    ★ **한 고침이 두 바다를 함께 풀었다** — 회차 29가 「같은 원인」이라 본 것이 맞았다.
    ★ **게임 규칙은 한 줄도 안 바꿨다** — 러너가 갖고 들어간 것만 달라졌다.
    ⇒ **패권 표: 중동 4/4 · 카리브 3/4**.
  - ★★★ **한 겹을 벗기면 다음 겹이 나왔다 — 여섯 번.** ① 사전 채비가 `hegemony-run`엔 아예 없었다
    (「고침이 한 러너에만」 **네 번째 재발** — 회차 22가 `grand-run22`에 넣은 것을 회차 27이
    `__r22_hunt` 한 줄만 이식했다) → ② 배를 먼저 사고 동료를 나중에 구해 **배와 선장이 다른 항구에
    갈렸다** → ③ `ensureMate`가 「사람」 탭을 열어 둔 채라 **「조선소로 간다」가 레이아웃에 없어**
    조용히 실패했다(`industry: 3`인 아바나에서도) → ④ `anchored`가 **이미 동행 중인 배까지 세어**
    새 배를 안 샀다 → ⑤ 동행선을 **가장 싼 것**으로 골라 얹힌 포가 **1문**이었다 → ⑥ 사냥 루프의
    보충이 배를 **사냥터 밖으로** 데려가 34초에 12항해가 「지나가고」 조우가 0건이었다.
    ★ **증상이 바뀌면 고쳐진 것이 아니라 다음 겹이다**(졌다 → 안 만난다).
  - ★★ **로그 없는 루프는 죽은 것과 구별되지 않는다.** `ensureFleetPower`의 `say`가 루프 밖에
    하나뿐이라 열 바퀴가 침묵으로 지나갔고 **두 판을 「죽었다」고 오판해 껐다**. 바퀴마다 한 줄을
    내자 그 자리에서 ③이 드러났다. 실패에도 **왜**를 적는다(「조선소를 못 열었다」와 「살 배가 없다」는
    부르는 쪽에서 똑같이 빈손이다). 고침은 전부 **공통 층 `.playtest/_shared/mates.mjs`**에 뒀다.
  - ✅ **항로 근거 108 → 0.** 경고 문구 자체가 사라졌다(「문제 없음 — 요율과 근거가 맞물려 있다」).
    동아시아 40 · 중동 20 · 아프리카 19 · 동남아 19 · 인도양 10. `ROUTE_RISK`는 한 줄도 안 바꿨고
    불일치는 계속 0건 — 왜 그 값인지만 밖으로 꺼내 적었다. 뭍길·내수로는 `null`에 「배가 아니라
    짐이 간다」를 근거로 남겼다(양쯔강·의주대로·세토내해·비와호·크라 지협·베냉 라군).
  - ✅ **규칙 전수 감사가 도구가 됐다** — `tools/check-orphan-rules.mjs`(검사 열아홉 번째 · 경고만).
    `state.js` export 425개를 전수로 훑는다. ★ **이 도구가 두 번 거짓말했다**: ⓐ `''`의 백슬래시가
    셸을 지나며 반으로 줄어 **백스페이스**가 돼 **424/425가 「안 쓰인다」**로 나왔고(→ 식별자 토큰
    Set으로 방식 교체) ⓑ 주석의 단어까지 세어 오탐이 무더기였다(→ 주석 제거). 91→119 · 30→**14**.
  - ★ 그 목록이 **진짜 부채 하나**를 찾아냈다 — **`cargoLure`**. 값나가는 짐이 해적을 부른다는 규칙은
    `encounterOdds`가 `lure`를 안 받으면 그것을 그대로 먹으므로 **위험 칸 숫자엔 이미 있었는데**
    화면이 그 몫을 한 번도 말한 적이 없었다(회차 27 술집 평판과 같은 모양). 항로 카드 툴팁에
    「그중 실은 짐이 부른 몫 +N%p」과 **천장에 닿으면 더 실어도 안 오른다**를 적었다. 규칙은 그대로다.
  - ★★★ **아홉 바다 전부에 왕이 섰다 — 해적왕 9/9 · 상인왕 9/9.** 먼저 세어 보니 해적왕 6/9 ·
    상인왕 5/9였고 빈 자리가 일곱이었다. ⚠️ **첫 후보가 이미 들어 있는 경우가 많았다** —
    미르 알리 베이(s3)·셀만 레이스(s4)·피리 레이스(s4)·조호르 오랑카야(r3)·식량형제단(s2).
    확인 없이 넣었으면 중복이 났다.
    · 해적왕 — 중동 **하디임 술레이만 파샤**(1538년 갤리 일흔 척으로 아덴 접수·디우 포위) ·
      대서양 **바다거지**(1572년 브릴 점령 · 깃발이 `pirate`인 것은 **아직 나라가 없어서**다) ·
      아프리카 **오바의 삯함대**.
    · 상인왕 — 중동 **카리미 상인단** · 아프리카 **상투메 제당 농장주단** ·
      지중해 **그리말디 가문** · 동남아 **마카사르 왕실 무역단**.
    ★ **아프리카가 마지막까지 비어 있었고 그 이유가 사료였다** — 서아프리카에서 포르투갈
      독점을 깬 것은 대개 익명의 인터로퍼 함대라 이름을 못 붙였다. 미르 알리 베이를 올릴까
      했으나 **코드 주석이 이미 정확했다**(*"갤리 한두 척만 끌고"* · 1585년 1척 · 1588년 5척).
      ⇒ 사용자 설계로 풀었다: **뱃사람이 아니라 왕이 두목인 함대.** 기니만의 오바가 금·상아와
      사람을 팔아 유럽 뱃사람을 사들였고, **배는 삯꾼이 몰되 갑판에 오르는 것은 오바의 병사**다.
      `flag: 'benin'`은 이미 있던 키다 — 이 자리를 위해 만든 것이 아니다.
      ⚠️ 이 함대 자체는 **사료에 없다**(`gameplay`). 베냉의 부와 상비군은 사실이다.
    ★ 아프리카 상단의 천장이 `rank 3`이던 이유도 드러났다 — 전부 **옮기는 자본**이고
      **만드는 자본**이 없었다(상투메가 그 자리다).
  - ★★ **감사기가 두 겹 더 정확해졌다 — 죽은 코드 117 → 13 → 6 → 0.**
    ⓐ `state.js`의 **자기 호출**을 안 세어 `priceTips`·`routeTips`(정보상이 파는 그것)와
      `backerSlain`이 죽은 코드로 보고됐다 — 셋 다 살아 있었다(117→13).
    ⓑ `tools/`를 안 봐서 **검사기가 재고 있는 7건**이 죽은 코드로 떴다(13→6).
      ★ 검사기가 쓴다는 것은 **규칙이 지켜지는지 재고 있다**는 뜻이지 게임이 안 쓴다는 뜻이 아니다.
    ⇒ 남은 여섯은 전부 **한 줄 래퍼**였고 외부 참조 0건이라 다섯을 지웠다
      (`refitPrice`·`fleetRecord`·`worksOf`·`hasWork`·`originOf` · export 425→**420**).
      `setCaptain`만 남겼다 — **동행선의 선장을 바꾸는 유일한 안전한 문**이라 지우면
      나중에 `state.consorts`를 직접 만지게 된다. 출력이 **「구멍 없음」**이 됐다.
  - ★★★ **패권 ③이 아홉 바다 전부에서 채워졌다.** 마지막이 인도양이었고 원인은 **게임이 아니라
    러너가 「최고선」을 고르는 방식**이었다 — `regionTopShips`는 tier가 같으면 **전부** 돌려주는데
    러너가 `keys[0]`을 집었다(**`SHIPS` 순회 순서에 좌우되는 임의값**). 인도양은
    `['ghanjah','naodegoa']`라 간자(205/12)가 걸렸고, 같은 t3인 **고아 나우(245/18)**를 두고
    그 배로 등급5에게 갔다. 포·선체 점수로 고르게 하니 **5전 5패 → 5전 5승**(7항해째 76일차).
    ⇒ 회차 30의 ③ 성적: 카리브 89일 · 중동 12일 · 남미 163일 · 아프리카 109일 · **인도양 76일**.
  - ★★ **자를 두 번째로 잘못 댔다(자기정정)** — 인도양 ③을 *"간자가 포 12로 t3 최하이니
    전투형 t3를 새로 넣어야 한다"*고 적었는데 **틀렸다.** 인도양엔 **고아 나우(245/18)**가
    이미 t3로 있고, 러너가 `requires: 'kotia'` 사슬을 안 밟아 못 샀을 뿐이다.
    그 판의 전적은 **12전 12승 · 진 것 0**이었다(등급5가 안 나왔을 뿐 · 표본 부족).
    ⇒ **제원표를 보기 전에 러너 로그만으로 결론을 냈다.** 회차 29의 「서브PM 둘이 정반대로
      보고했다」와 같은 모양이 이 회차에 두 번 났다.
  - **메모리 정비**(`memory-optimizer` §1 라우터 감사 + §2 중복·stale 감사 · §3 다이어트는
    판정해서 제외 — `gotchas.md` 3,937 < 4,096). ★ **리프가 조용히 낡아 있었다** —
    상단 수 **49 → 53**이 `wiki/economy-trade.md`·`file-map.md`·`world-npc.md`(3곳)에 남아 있었고,
    `QUICKMAP-world.md`의 **「이름난 해적 40」**(지금 58)도 그랬다.
    ⇒ 리프 셋은 값을 고치고, **라우터의 count 둘은 아예 뺐다**(brittle count는 SoT에 위임 —
    「이름난 해적(아홉 바다 · 바다마다 해적왕 하나)」·「적자가 한 곳도 없다」).
    라우터에 최종상태 둘을 얹었다 — **아홉 바다마다 왕이 둘**(⚠️ 패권 ③의 두목은 이것이 아니라
    `FOES`의 다섯째다) · **같은 tier 안에서도 배가 갈린다**(`regionTopShips`가 전부 돌려준다).
    `A-8b` 헤더의 「2/9 완주」도 「4/4가 넷 · ③은 아홉 바다 전부」로 고쳤다.
  - **재집계** — `art.hullTodo` **71종**(선종 122→133이라 회차 28의 60에서 늘었다).
    ⚠️ `sprites/ship.js: HULLS`는 10종이고 133 선종이 나눠 쓰는 구조다 — 버그가 아니다.

- **[2026-09-01] #trade #world #engine — 회차 29, 「조사 → 빠른수정 → 콘텐츠」 3파(서브PM 12).** 사용자 지시
  *"남은일 리스트업 각 서브pm이 보고"* → *"빠르게 할수 있는것들부터"* → *"이미지 지도 캐릭터 빼고 나머지 진행"*.
  **완주했다** — `test-rules` 486/486 · 검사 열여덟 전부 · 스모크 0 · 곡선 **10항차 752닢 불변**(3트랙 × 3회 = 9회 전부 동일).
  - ★★★ **서브PM 둘이 같은 항목에 정반대로 보고했고 셋 다 자가 달랐다.** 「아프리카·남미의 최고선을 제 바다에서 못 짓는다」를
    RULE은 *"이미 열려 있다"*, RUN은 *"안 열린다"*고 냈다. 상위 PM이 판정 함수에 직접 물어 갈랐다 —
    ⓐ 초기 `sellsShip`은 `everOwned`가 비어 **0/28**이 나오고 ⓑ `yardReach`는 참/거짓이 아니라 **필요 공업력 수치**이며
    ⓒ `sellsShip`은 `yardAllowed`(조선지 명부)도 본다. **정본은 `requires` 사슬**이었고 **참인 것은 아프리카 하나뿐**이었다.
    ⇒ 회차 28의 「자를 바꾸니 판정이 뒤집힌다」가 **조사 단계에서 재현**됐다.
  - ★★★ **회차 28의 헤드라인이 흔들렸다** — `sim-guild`가 회차 28엔 「항차 **+5.6%** ↔ 날 −16~−21%」였는데 오늘은
    **항차 −29.0% · 날 −31.5%로 둘 다 음수**라 방향이 안 갈린다. 도구는 그대로다(`677492e` 하나뿐).
    **RUN이 측정을 마친 뒤에 RULE의 선종 102→122(`0327b2b`)가 워크트리에 합쳐졌다** — 두 트랙이 **다른 시점의 세계**를 쟀다.
    ⇒ 규약 신설: **측정값에는 「어느 커밋에서 쟀나」를 함께 적는다**(`wiki/sim-measurement.md`).
    ★ 결론 자체는 살았다 — 「상단은 이문을 깎는 게 아니라 오래 걸리게 한다」는 두 자 모두 음수인 지금도 성립한다.
  - ★★ **「③이 안 채워진다」가 카리브에서도 「만나서 싸웠고 다 졌다」였다**(등급5 3건 · 회피 0 · 3전 3패).
    ★ **카리브엔 t3가 있다**(갈리사브라) ⇒ **t3가 있어도 진다.** 「그 바다에 큰 배가 없다」가 원인이 아니라
    **러너가 전력을 안 갖추고 두목에게 간다**는 쪽이다. 다음 회차의 1번(R5).
  - ★★ **조용한 사고를 하나 막았다** — 남미 해적을 넣다가 카리브의 `drake`와 **id까지 완전히 충돌**했는데
    `ALL_PIRATES`가 **id 유일성을 안 봤다**. 들어갔으면 `state.slain`이 두 바다를 섞어 **패권 ③이 거짓 통과**한다.
    `check-world` **⑦ 명부id충돌** 신설(실패 조건 · `ALL_MATES`도 함께) — **일부러 중복을 만들어 exit 1을 확인**했다.
  - **콘텐츠 — 아홉 바다의 천장이 나란해졌다.** 선종 122 → **133**(카리브·동남아·인도양 각 8→11 · 중동 +1 · 아프리카 +1).
    ★ **중동이 유일하게 t2였다** → `kalyon`(카륜 · 오스만 홍해 함대 · 수에즈). `originFlag` 홈 할인이 `tierNeeded`를 내려
    **공업력 상한 2를 안 건드리고** t3가 열린다(수에즈·아덴·바스라). 이제 여덟 바다가 t3, 동아시아만 t4.
  - **아프리카 사슬을 닫았다** — `naudamina ← guineiro ← caravel(**지중해**)`이라 **아프리카만** 제 바다에서 최고선을 못 지었다.
    ⛔ `caravel`을 아프리카에 뿌리면 **C-2(그 바다와 무관한 배를 판다)가 되살아난다** ⇒ 아프리카 고유 t1 **`barca`** 신설로 대신했다
    (제원은 `caravel`과 동일 — 사다리의 그 칸이므로). ★ **`shipLockedBy`에 `requiresAlt` 신설** — `caravel`만 몰아 본 옛 판도 통과한다.
  - **교역품** 아프리카 7→13 · 중동 7→13 · **해적 명부** 51→**55**(중동·남미 각 4→7).
    ★ **바다끼리 물렸다**(사용자 지시 *"서로 영향을 받고"*) — 알레포 `rawsilk` 신설로 **중동에서 캔 것이 지중해에서 짜인다**
    (지중해 실크 사슬은 이미 돌고 있었는데 **원료 출처가 빈칸**이었다). 상투메 사탕수수는 이미 있었고 **주석이 코드와 반대로** 적혀 있었다.
  - **화면** — 회차 28 규칙 셋(`backerSlain`·`stirSeen`·`guildBackers`)이 **UI 트랙 종료 직후 커밋돼 아무도 안 본 자리**였다.
    ★ 셋에 각각 붙이지 않고 **관계를 움직이는 유일한 문**(`addRegard`)에 얹었다(`state.regardWhy` · 세력당 최근 한 건).
    이월 부채 넷도 닫았다 — **급여일 모달 넘침 487→0px**(수치가 아니라 `fold()` 규약으로) · 술집 `advance` 문 ·
    부관 유예 누계(코드가 *"누계(화면용)"*라 적어 두고 **한 번도 안 쓰던 값**) · 평판 **남은 반감기**.
  - **하네스** — `.playtest/_shared/` 신설(`graph`·`travel-fail`·`mates`). 「고침이 한 러너에만」이 회차 22·27·28 **세 번 반복**된 자리다.
    합칠 수 없는 자리(원양 진입 지점)는 **왜 못 합쳤는지 주석으로** 남겼다.
  - **근거** — `check-routes` 근거없음 141 → **108**(카리브 18·남미 15). ⚠️ **「카리브·중미에 몰렸다」는 착시였다**
    (콘솔이 표본 6개만 보여준다) — **진짜 무게중심은 동아시아 40**이다. `ROUTE_RISK`는 한 줄도 안 바꿨다(불일치 0건).
  - **문서** — 상한 셋을 한꺼번에 풀었다: 라우터 **37→3,540** · `QUICKMAP-trade` 52→3,884 · `QUICKMAP-world` 663→5,105바이트
    (`wiki/holdings-facilities.md`·`world-data-sync.md` 신설 · 무손실 이동). **`C-17`은 이미 해소돼 있었는데 「구멍」으로 남아 있었다.**
