# changelog — tradeship

> 변경 이력 정본(lazy·거의 안 읽음). `claude-memory.md`에는 **현재 상태만** 두고,
> "무엇을 어떻게 고쳤다"는 여기에 쌓는다.
> 상한 40KB — 넘으면 시점 경계로 분할한다. 과거분:
> [changelog-2026-08-25-pm.md](changelog-2026-08-25-pm.md)(08-25 오후·저녁) ·
> [changelog-2026-08-25-am.md](changelog-2026-08-25-am.md)(08-25 앞부분) ·
> [changelog-2026-08-23-24.md](changelog-2026-08-23-24.md)(08-23 ~ 08-24) ·
> [changelog-2026-08-22.md](changelog-2026-08-22.md)(08-22 하루) ·
> [changelog-2026-08-17-21.md](changelog-2026-08-17-21.md)(08-17 ~ 08-21) ·
> [changelog-2026-08-16.md](changelog-2026-08-16.md)(08-16 하루) ·
> [changelog-2026-08-15.md](changelog-2026-08-15.md)(08-15 하루) ·
> [changelog-2026-08-early.md](changelog-2026-08-early.md)(08-08 ~ 08-15 앞부분).

## 2026-08

- **[2026-08-26] #trade #world #combat #art #engine** — **사용자가 방향을 다섯 개 정했고, 다섯 팀이 그것을 나눠 지었다.**
  PM이 남은 20건을 표로 올리자 사용자가 ★5건에 방향을 직접 정하고 *"나머지는 진행"*이라고 했다.
  `data.js`·`state.js`를 여럿이 동시에 만지므로 **워크트리로 격리**해 병렬로 돌리고 PM이 병합·판정했다.
  지시 정본은 `.playtest/round-20/DIRECTIVE.md`(사용자 원문 그대로), 팀별 로그는 같은 폴더의 `WORK-*.md`.

  - **#3 조선소 — 좁힌 것이 아니라 계단으로 만들었다.** `sellsShip`이 기술(`tierNeeded`) 옆에
    **교역권**(`yardReach` 0~4)을 나란히 본다. 공업력 1=제 바다 · 2=원양으로 직접 잇는 바다 ·
    3=이 바다가 닿는 바다 · **4=온 세계**(A-2 승급의 마지막 보상이 됐다). 걸음은 **이미 있는 데이터로만** 잰다.
    한 항구에 걸리는 선종 **47 → 6종**(공업력 1) · **100곳 넘게 지어지던 선종 67 → 0**.
    ★ **효과가 「다음 배」에 그대로 보인다** — 전에는 아홉 시작지가 **전부 코카 1,023닢**이었는데
    이제 부산포 **평두선 651닢** · 캄바트 **잘바 976닢** · 자메이카 **피라구아 1,104닢**이다.
    ⚠️ 부작용으로 **첫 배가 10항차 → 1항차**(지역 배가 더 싸다) — 다음 회차 조정 후보로 적었다.
  - **#4 계절풍 — 막지 않고 값을 문다.** 41구간에 여는 철(발트 12 · 말라바르 10 · 원양 10).
    ★ 판정 근거 셋 중 둘째가 이 게임의 구조였다 — **항구에는 시간이 없다**(`advanceDays`가 항해에서만 불린다).
    막으면 갇힌 사람이 **철이 바뀌기를 기다릴 방법 자체가 없다.** 겨울 뤼베크 `4일 222닢` → **`7일 389닢`**.
    ⚠️ `routeRisk()` 자체는 안 건드렸다 — `escortNeed`가 그것을 먹어 **같은 원양 문이 날짜에 따라 열렸다 닫혔다** 한다.
  - **#5 거점이 부동산이 됐다** — **가게**(노점→가게→도매상점) · **여관**(선술집→여관→고급 여관).
    등급마다 수익↑·**공실↑**(여관 16% → **28%**)이고 **승급의 문이 그 항구 공업력**이라 부두가 두 번째 쓸모를 얻었다.
    공실은 `size`와 **그 깃발의 악명**으로 정하고 **결정론적**이라 드나들며 다시 못 굴린다.
    수익률은 당대 안전자산 이율(4~7%)에 맞췄다 — 열흘에 10%인 무역과 자릿수가 다르다.
  - **#6 관세가 후반 브레이크가 됐다** — 같은 짐인데 자산이 크면 더 뜯긴다(**3.90% → 9.36%**).
    **초반 ×1.00**(30항차까지 안 켜진다) · 60항차 ×2.40 · 끝값 총자산 **−15~−22%**.
    **관세 폭탄**은 `state.shocks`의 품목 없는 충격, **무역품 몰수**는 `SEA_EVENTS`의 calm 한 장을 바꿔 붙였다
    (**weight 합 100 규약 무손상**).
  - **#7 시작배가 바다마다 갈렸다** — 전에는 아홉 어디서 시작해도 `hulk` 하나였다.
    ★ **한 번 무너뜨리고 되돌렸다**: 지시대로 *가장 싼 정품 tier 1 배*를 줬더니 `leak`가 없고 속력이 1.7배라
    **첫 배 10~16항차 → 1항차 · 10항차 금고 899 → 2,144닢**. 되돌리고 **「삭은」 배 여덟 종을 새로 만들었다**
    (선종 94 → **102**). **제원은 `hulk`에 맞춘다 — 바다마다 다른 것은 얼굴이지 난이도가 아니다.**
    ⚠️ `crewMin`을 4로 뒀더니 **그것만으로 10항차 금고가 +41%**였다.
  - **화면 다섯 자리** — C-17 바닥 출구(**규칙 0줄 추가** · 경보와 안내를 두 얼굴로) · A-8c 패권 표시 ·
    C-8 급여일 선택(판다·빌린다·미룬다) · C-6 대시보드 6탭 + **사람 탭**(230명) · 사소 넷.
  - **지도 7라운드 — 검수 1/9 → 9/9.** 절반은 미학이 아니었다(→ 위 항목). 기후 버그 둘,
    색 예산 역전(바다 250색 → 5), **두 번 반려**(섬마다 동심원 21 → 0 · 지중해가 직전 판과 구분 안 됨),
    그리고 그 반려가 진짜 원인을 냈다 — **지중해만 바다가 29.6%였다**(다음이 47.7%). → **48.0%**.

  - **★ 이 회차 최대 교훈 — 「분산이 크다」는 관찰 자체가 측정기를 의심하라는 신호였다.**
    낮에 30항차 금고가 ±30% 흔들리길래 PM이 **밴드를 넓혔다**(참고 지표로 강등).
    저녁에 조선소 팀이 `sim-core`의 버그 둘을 잡자 — **하향 갈아타기** · **선원을 안 보는 매입** —
    3회가 **완전히 같은 값**을 냈다. **흔들린 것은 게임이 아니라 시뮬의 전략이었다.**
    ⇒ 그 버그 이전의 기준선 숫자는 전부 못 믿는다. 정본 `wiki/sim-measurement.md`(이 회차에 신설).
  - **같은 모양의 구멍이 셋 더** — `check-*` 열넷이 전부 통과하는데 ① 게임이 안 뜨고(`riskKey`)
    ② 새 시작배 여덟 종이 **없는 선체**를 가리키고(그리는 순간 죽는다) ③ 검사가 **여덟 줄 내내 거짓말**했다
    (`state.at`을 안 세워 조선 항구에서 지중해 카라벨을 사면서 베네치아라 적었다).
    ⇒ `check-imports`(신설) · `check-world` ⑤이름충돌 ⑥선체등록 · `region-topology` BY_DESIGN.
  - **격리된 팀이 서로의 결함을 잡았다** — 화면 담당이 다른 포트에서 스모크를 돌리다 PM의 선체 버그를 보고했고,
    지도 담당은 **서버가 남의 워크트리를 서빙하는** 함정으로 30분을 태우고 그 진단법을 남겼다
    (`curl | wc -c`를 디스크와 비교 · `QUICKMAP-engine.md` §3).

  - 검증: 규칙 **184 → 284** · `check-*` 열넷 전부 · 지도 9/9 · `sim-stat 20` 3회 완주 19~20/20.
    메모리: `QUICKMAP-trade` 29,457B → **23,729B**(계측 함정 13개를 `wiki/sim-measurement.md`로 무손실 이동) ·
    `changelog` 38,917B → **12,466B**(시점 분할).

- **[2026-08-26] #combat #world** — **조선소가 계단이 되고, 계절풍이 화면의 글자에서 규칙이 됐다.**
  사용자 지시 둘 — *"조선소는 공업력이 올라가면서 점점 지어지는 배가 많아지고 교역이 잘이뤄지는 곳의 배를
  먼저 만들수있개해"* · *"계절풍은 있어야지"*(`.playtest/round-20/DIRECTIVE.md` #3·#4).

  - **#3 조선소 — `sellsShip`이 사다리를 둘 본다.** 기술(`tierNeeded`) 옆에 교역(`yardReach` 0~4)을 나란히 놓았다.
    걸음은 **이미 있는 데이터로만** 재고(`yards`·`originFlag`·`home`·`ROUTES`·`OCEAN_LANES`·권역) 새 값은 한 줄도 안 만들었다.
    — 실측 전/후(한 항구에 걸리는 선종 중앙): 공업력 1 **47→6** · 2 **77→12** · 3 **92→36** · 4 **—→93**.
      100곳 넘는 곳에서 지어지던 선종 **67→0종**. 나가사키의 브라질 카라벨랑이 사라졌다.
    — **좁힌 것이 아니라 계단이다** — 꼭대기(`YARD.cap` 4)에서는 지금처럼 전부(93/93) 열린다.
      걸음을 0~3으로 접어 공업력 3에서 온 세계를 여는 안도 재 봤으나, 그러면 ind3 항구 스물넷이 지금과 똑같아
      **꼭대기에서 규칙이 사라진다**(베네치아에서 평두선). 그래서 4단이고, *온 세계의 배*가 A-2 승급의 마지막 보상이 됐다.
    — 문이 셋이라 셋 다 같은 판정을 지난다 — 신조·중고(한 걸음 관대)·나포(원래 권역 한정). 하나만 막으면 나머지로 샌다.
    — `state.js: yardAllowed` 주석이 *"`sellsShip`은 공업력만 본다 — **그것이 설계다**"*라고 못박아 둔 자리다.
      **사용자가 그 설계를 바꾸라고 했으므로 주석도 같은 커밋에서 고쳤다.**
    — `tier 0`(아홉 바다의 삭은 시작배)은 그대로 **시중에 안 나온다** — `tierNeeded`가 `Infinity`라 문 셋 어디에도 안 걸린다.

  - **★ 그 과정에서 측정기가 유럽만 알고 있다는 것이 드러났다.** `tools/sim-core.mjs: ORDER`가 **손으로 적은 유럽 배 열 척**이라,
    교역권이 붙자 아시아 시작 항구가 통째로 「다음 배 없다」가 됐다(`sim-firstship` 0/12판). 게임이 아니라 도구였다.
    — `ORDER`를 **세계에서 뽑는다**(`tier > 0`을 화물칸 오름차순). 권역이 배를 더해도 저절로 따라온다.
    — 그 목록이 넓어지면서 **시뮬 버그 둘이 드러났다**: ① `curRank`가 루프 안에서 안 갱신돼 **큰 배를 사고 나서
      더 작은 배를 이어 사며 하향 갈아타고** 있었다 ② 매입 판정이 **배값만 봐** 선원 34명·유지비 21닢/일짜리
      통신사선을 사고 파산했다(부산포 40항차 자산 23,694 → 1,067닢). 둘 다 고쳤다 — **태울 사람 값까지 셈에 넣는다.**

  - **#4 계절풍 — 항로마다 「여는 철」이 생겼다**(`geo.js: ROUTE_SEASON` · `OCEAN_LANES[].season` · 합성 `map/geo.js` · 규칙 `state.js`).
    41구간 — 발트 12(한자법 2/22~11/11) · 말라바르 10(남서 계절풍에 닫힌다) · 코로만델·벵골 9(북동 계절풍에 닫힌다) · 원양 10.
    — **판정: 막지 않고 값을 물린다**(`data.js: SEASON` — 속력 ×0.55 → 일수 약 1.8배 · 요율 ×1.6).
      PM의 권고와 같고, 근거 셋을 남겼다 — ① 사료의 "통행 불가"는 대개 **법**이었고(한자법은 상인 자신의 금지로,
      어기면 벌금·보험 거부였다 = 값이다) ② **항구에는 시간이 없다** — `advanceDays`가 항해에서만 불려
      막으면 갇힌 사람이 철을 기다릴 방법 자체가 없다(진짜 데드락) ③ 인도양 서안·동안이 반대 철이라 둘 다 막으면
      반년마다 인도 아대륙의 절반이 사라진다.
    — ⚠️ **`routeRisk()` 자체는 안 건드렸다.** 그 함수는 `escortNeed`(원양 동행 의무)도 먹이므로 철에 따라 입장권이
      흔들리면 **같은 문이 날짜에 따라 열렸다 닫혔다** 한다 — 「막지 않는다」를 뒷문으로 깨는 짓이다.
      배율은 `encounterOdds`와 `insuranceFor` 안에서만 곱한다. 제철에 상은 주지 않는다(배율 1).
    — **화면이 말한다** — 항로 줄의 「철 아님」 표 · 툴팁 한 줄 · 「항로」 패널 머리의 **현재 철**.
      전엔 `monsoon: true`가 있었고 항로 카드가 *"철을 잘못 잡으면 훨씬 오래 걸린다"*고 적었는데
      **그 값을 읽는 규칙이 하나도 없어 일수가 한 치도 안 달라졌다** — 화면이 거짓말을 하고 있었다.

  - **검증** — 규칙 212 → **241**(#3 12개 · #4 17개). 일부러 깨뜨려 exit 1을 확인했고, 그때
    **「겨울이 여름보다 오래 걸린다」만 보는 검사는 배선을 떼어내도 통과한다**는 것을 발견해(바람도 날짜로 바뀐다)
    곱해진 것을 식으로 맞춰 보는 검사로 바꿔 다시 깨뜨렸다.
    `check-routes.mjs`가 철을 **요율과 같은 규약**으로 대조하고(§1) 배선까지 본다(§4).
    근거는 `atlantic`·`indian`·`ocean-lanes` evidence의 `season`·`seasonBasis`에 같은 커밋으로 적었고,
    철을 건 항로 여덟은 원래 「근거없음」 목록에 있던 것이라 항목을 세웠다(요율 근거는 `geo.js` 주석 그대로).
    ⇒ `check-routes` 경고 149 → 141건.
  - **시뮬**(3회) — 완주 20/20/20 · 첫 배 **8항차**(전 10) · 10항차 금고 665닢(전 749~824) · 30항차 4,622~5,317닢.
    금고가 준 것은 **더 싼 지역 배가 먼저 열려 둘째 배를 두 항차 미리 사기 때문**이다(금고가 선단으로 옮겨간 것).
    `sim-firstship`은 시작 자리 11곳 중 10곳이 **12/12판**이다(호르무즈 0/12는 이 변경 전부터 그러했다).

- **[2026-08-26] #engine #art #world #trade** — **측정기가 넷을 거짓말했고, 검사기 열셋이 통과하는데 게임은 안 떴다.**
  사용자 지시 둘로 시작한 회차다 — *"이슈 전체 수정 가능한 것부터 바로"* · *"지도 제대로 다듬어봐 지금 너무 형편없어"*.

  - **밸런스 회차 워크트리 셋을 전부 본선에 넣었다** — `aa2e31c`(P8-2·3) → `a1915c8`(P7) → `a79d749`(P1·P4·P8-1).
    마지막 것은 `main`을 먼저 태워 **어제의 `activeBounty` 중복이 그 병합으로 지워졌다.**
    `africa-evidence.json` 충돌 9곳은 **P1 쪽을 채택** — P1의 판단이 P7의 원양 관문 신설을 이미 전제로
    값을 냈기 때문이다(아르갱 0.74 → 0.70: *"관문이 되면서 배율을 내렸지만 사다리는 지킨다"*). 규칙 165 → **184**.

  - **★ 게임이 안 뜨고 있었다. 그런데 `check-*` 열셋과 규칙 184개가 전부 통과했다.**
    `js/scenes/map.js`가 `riskKey`를 `state.js`에서 가져오는데 그 이름이 없다 → 브라우저가 모듈 그래프를
    통째로 거부 → 게임이 아예 안 뜸. 그 도구들은 `data.js`·`state.js`를 **직접** 부르므로 `scenes/*`를
    한 번도 안 거친다. ⇒ **`tools/check-imports.mjs` 신설**(104파일 · import 이름 726개 전수).
    거짓 경고 8건(`export {}` 안의 주석 · `export const VW = 400, VH = 225` 한 줄 다중 선언)을 고쳐
    **애매하면 "있다"로** 본다 — 검사기가 없는 결함을 만들면 아무도 안 쓴다.
  - ⇒ **그 장애의 증상이 지도였다.** `gen-map-png.mjs`는 게임을 띄워서 굽는다. 사용자가 본 *"형편없는 지도"*의
    **절반은 미학이 아니라 낡은 산출물**이었다 — 한 줄 고치고 다시 뽑으니 검수 **1/9 → 9/9**.

  - **★ 측정기가 거짓 신호를 낸 것이 한 회차에 넷.** 넷 다 "게임이 틀렸다"로 읽힐 뻔했다:
    `probe-fair`가 옛 상수를 읽어 **「보험 미해소」**(실제로는 이미 전 구간 90%) ·
    `check-world`가 정적 공업력만 보고 **「철갑 거북선을 못 짓는다」**(염포 두 번 승급하면 열린다 · 설계대로) ·
    `region-topology`가 남미를 **「닿지 못하는 자리」**(두 대양이 안 만나는 것이 그 권역의 뼈대다 —
    1599년 해협 통과 넉 달 · 1584년 '기아항') · `check-map.py`의 `head[-1]`이 **진짜 반려 사유를 가림**.
    앞의 셋은 측정기를 고쳤고(`BY_DESIGN`·승급 반영·프로브 갱신), 넷째는 지도 회차가 개별 실행으로 우회했다.

  - **지도 아홉 장 — 검수→수정→재검수를 PM이 눈으로 판정하며 돌렸다.**
    미학이 아니라 **버그였던 것 둘**: 아프리카 `forest` 분기가 죽은 코드라 **적도 이남 전체가 사하라**였고
    (사하라 80% → 사바나 38%/사하라 28%/우림 22%), 중동은 `scrubY: () => -1`이라 **사막색이 0%**였다(→ 84%).
    색 예산도 거꾸로였다(바다 250색 vs 육지 7색) — `outline()×4 + 그라데이션`을 **거리장 한 번**으로 바꿔
    바다 고유색 **5**, 해안 띠 σ **0 → 11~15**, `landmass`를 부호거리장+3옥타브로 바꿔 **반도가 생겼다**.
    ★ **1차 결과를 반려했다** — 수심 다섯 단을 전부 거리로 끊어 **섬마다 동심원**이 둘렸다(21/156).
    깊은 세 단을 해저 저주파장으로 옮겨 **0**. 검수기에 §7로 그 판정을 박았다.
    합격선 하나는 **기준 쪽을 고쳤다**(고유색 하한 26 → 16) — **숫자를 맞추려고 그림을 나쁘게 만들지 않는다.**

  - **고친 결함** — **C-4** 세계에 남은 마지막 이름 충돌(바갈라 둘 → 「잔지바르 바갈라」. **합치지 않았다** —
    같은 다우 계열의 다른 바다 변종이고 제원이 실제로 다르다. `check-world` ⑤에 이름 중복 검사) ·
    **ISSUES #36** 저장된 판이 *"지워질 자리에 다가간다"*가 아니라 **이미 지워지고 있었다**(바다·갈래를 고르면
    입항 자동저장이 덮는다). 규칙은 안 바꾸고 **덮기 직전에 한 장 미는** 방식으로 막았다(`stashSave`·+6검사.
    ★ `localStorage` 폴리필을 함께 넣었다 — 없으면 `save.js`가 조용히 실패해 **전부 통과해 버린다**) ·
    **C-6** 해적 명부가 아홉 바다를 뭉뚱그리던 것(대시보드 탭 여섯 중 필터를 읽는 것이 둘뿐이었다).

  - **문서 — P6(반증가능성) 회차.** always-load의 **틀린 결론 넷**을 정정했다: 「보험 미해소」(고쳐져 있었다) ·
    「미완성 셋이 워크트리에」(다 들어왔다) · 「**기함이 침몰하지 않는다**」(**설계다** — `spreadDamage` 주석이
    *"패배 처리가 따로 있다"*고 이미 적어 두었다) · C-2 「59종」(실측 94선종 · 한 곳에 64줄).
    실측으로 **B-3·C-11도 이미 해소**돼 있었다. 「손대는 순서」를 3회차로 다시 쓰고 그 앞에
    **「측정기를 먼저 의심하라」** 표를 넣었다. `gotchas` #6을 넓히고 상한을 넘겨 #3(CDP 타임아웃)을
    `QUICKMAP-engine.md` §3으로 이관 — **번호는 다시 매기지 않았다**(다른 문서가 "#6"으로 가리킨다).

  - 검증: `check-*` **14종**(신설 1 포함) · 규칙 **184/184** · 세계 10/10 · 지도 **9/9** ·
    `sim-stat 20` ×4회 회귀 없음(완주 18~19/20 · 첫 배 10~14항차 · 10항차 899닢).
    ⚠️ **첫 원양은 중앙값 293일**로 합격선(80~150일) 미달이다 — P7이 문을 셋 늘렸는데도 그렇다(`sim-gate`).
