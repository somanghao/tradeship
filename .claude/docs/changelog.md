# changelog — tradeship

> 변경 이력 정본(lazy·거의 안 읽음). `claude-memory.md`에는 **현재 상태만** 두고, "무엇을 어떻게 고쳤다"는 여기에 쌓는다.
> 상한 40KB — 넘으면 시점 경계로 분할. 과거분(2026-08-08 ~ 08-13)은 [changelog-2026-08-early.md](changelog-2026-08-early.md).

## 2026-08

- **[2026-08-15] #memory #evidence** — **근거 정본을 카테고리별로 갈랐다.** 사용자 지적("물가·교역품·월급·부동산은 카테고리가 다르잖아").
  - `price-evidence.json` 하나에 교역품 물가·임금·부동산·유지비가 섞여 있었고 임금은 `wage-evidence.json`과 **중복**이었다. 무엇을 고쳐야 하는지가 흐려지는 구조다.
  - 넷으로 분할: **`goods-evidence.json`**(교역품 물가·화물 1칸의 실물 정의·대조 2축의 정본) · **`wage-evidence.json`**(임금 — 사료 임금 5종을 흡수해 한곳으로) · **`asset-evidence.json`**(선박가·집세·주택값 + "선원 연봉의 몇 배" 지표) · **`upkeep-evidence.json`**(선체·무장 유지·적하보험·화물 유인). 도시·항로까지 합쳐 근거 파일은 6개가 됐고 **파일 이름만 봐도 어디를 고칠지 정해진다.**
  - `check-prices.mjs`가 세 파일을 읽도록 고쳤고(임금 목표는 `check-wages.mjs` 소관으로 넘겨 경계를 지켰다), 대시보드 물가 절은 `goods-evidence.json`만 fetch한다. 데이터 유실 0(leaf 152개 대조) · 잔여 참조 0 · 검증 6종 PASS.

- **[2026-08-15] #memory #router** — **QUICKMAP을 접근패턴으로 재분할하고 라우팅 행에 코드 경로를 붙였다.** 사용자 지적("퀵맵은 파일 경로와 토픽문서 연결, 특정 토픽을 빠르고 정확하게 탑재하게")에서 출발.
  - **`QUICKMAP-game.md`가 20.3KB·92행**으로 경고선(8KB·30행)의 2.5배까지 자라 있었다 — 세션마다 한두 행씩 더한 것이 조용히 누적된 결과다. 경제 세션이 전투·조선소 라우팅까지 통째로 읽고 있었다.
  - co-access로 갈랐다: **`QUICKMAP-trade.md`**(경제·물가·NPC·계약·고증 4종·대시보드) / **`QUICKMAP-combat.md`**(전투·선박·조선소·무장). 도메인은 art/trade/combat/engine 4개.
  - **§1을 `키워드 | 읽을 문서 | 코드 정본` 3열로** 바꿨다. 전에는 문서만 가리켜 "어느 파일을 열지"는 §2를 다시 훑어야 하는 2-hop이었다.
  - 1세션 라우팅 로드 40.2KB → **trade 34.1KB(−15%) · combat 26.6KB(−33%)**. 무손실(사실토큰 대조 — 실제 유실 3건은 복구) · dead pointer 0 · `claude-memory.md` 14.3→13.7KB.
  - 이번에 드러난 실수 4건을 **글로벌 `memory-optimizer` 스킬에 반영**했다: ①행에 코드 정본 병기 ②행을 추가한 세션은 그 자리에서 도메인 파일 재측정(누적 방치 방지) ③감사 때 리프(토픽 문서) stale도 grep으로 함께 점검 ④파생값을 서술 문서에 박지 말고 검증 스크립트가 출력하게. 무손실 대조 시 경로 토큰 basename 정규화도 SKILL.md에 추가.

- **[2026-08-15] #game #balance #evidence #research** — **물가·임금을 당대 사료 비율로 재조정.** 사용자 지적("부관 한 달 월급이 너무 많은 거 아냐? 선원 일당 2.4닢도 그 시세에 맞나")에서 출발해 15~16세기 지중해·이베리아의 밀·식료품·부동산·임금·선박가를 조사하고 경제 전체를 다시 맞췄다.
  - **지적이 정확했다.** 조사 결과 임금이 물가·선박가 대비 3~10배 과중했다 — "선원 연봉으로 배를 몇 척 사나"로 재면 게임 11배 : 사료 30배(스페인 캐랙 건조비 610두카트 ÷ 선원 연봉 20두카트). 부관은 거기에 직위 배율 3배가 더 얹혀 있었다(6.67배 vs c.1500의 2.0~2.3배). 부관 월급 480닢은 시작 금화 900닢으로 두 달이면 거덜나는 값이었다.
  - **보수**: `CREW_WAGE` 2.4→**1.2**, `OFFICER.wage` 16→**2.6**(배율 2.17배로 사료 구간 안). 결과적으로 부관 수입의 **96%가 성과급**이 되어 오히려 고증(사관의 실수입은 봉급이 아니라 사무역 quintalada)에 더 가까워졌다. 예전에 세웠던 "수치 말고 직함을 고치자"는 처방은 **폐기**했다.
  - **물가**: 교역품 12종 기준가를 사료 비율로 재조정(곡물 20 고정 · 소금 ×2.4 · 향신료 ×16.5 · 비단 ×21 · 금괴 ×35). 후추는 무게당 밀의 30배(자루 하나가 장인 연봉)지만 그대로 넣으면 향신료 항로가 경제를 삼켜 **절반쯤만 반영**했다 — 방향은 사료, 폭은 게임. 와인은 반대로 사료가 훨씬 싸서(벌크 0.3배) 최저선까지 내리지 않았다. 계약 보수(`CONTRACT.value`)도 평균 물가 상승(×1.23)에 맞춰 함께 올렸다.
  - **줄어든 압박을 성장 비용으로 옮겼다** — 임금만 내리면 후반이 풍족해지므로(실측 90항차 +65%) 세 갈래를 신설했다: **선체 유지**(기함도 `upkeep`을 문다 — 배가 클수록) · **무장 유지**(대포 1문당 경0.5·중0.9·장1.6 — 무장이 공짜가 아니어야 "어디까지 무장할까"가 선택이 된다) · **적하보험**(화물가치 × 항로요율 × 0.3). 보험 요율은 `ROUTE_RISK`를 그대로 쓴다 — **그 숫자의 본래 뜻이 보험료**인데 지금껏 조우 확률에만 쓰고 있었다.
  - **값나가는 짐은 두 번 대가를 치른다** — 사용자 구상("돈 되는 곳엔 해적이 꼬인다")을 규칙으로 옮겨 `cargoLure`를 넣었다(화물 9,000닢당 조우 +5%p·상한 +14%p). 초반엔 곡물·소금을 안전한 이웃으로 나르니 0에 가깝고, 향신료·비단을 싣기 시작하면 같은 항로도 표적이 된다.
  - **곡선**: 1항차 1,366 · 5항차 824 · 10항차 1,714 · 30항차 8,002 · 60항차 25,505 · 90항차 32,245닢. 초반은 여전히 빠듯하고 후반 증가폭은 완만해졌다(유지비 7갈래가 함께 늘기 때문). 90항차 누적 항해비 구성은 일당 20% · 보급 22% · 선단 21% · 보험 18% · 선체 11% · 무장 6% · 부관 1.6%.
  - **시뮬 현실화 1건** — 배 구입 시 운영자금을 금화의 8%만 남기던 것(`0.92`)을 30%로 고쳤다. 화물 매입 자본이 커진 뒤로는 배를 사면 실을 것을 못 사 **절반이 파산**했다(실측). 배는 화물을 나르는 수단이지 목적이 아니다.
  - 근거 계층 `content/price-evidence.json`(사료 앵커·목표 비율·화물 1칸의 실물 정의)과 기계검증 `tools/check-prices.mjs` 신설 — 절대액이 아니라 **비율**을 지킨다. 대시보드 탭을 **보수·물가**로 넓히고 교역품 상대가격을 사료 원값과 겹쳐 그렸다.
  - 곁다리로 시작 화면 문구가 **"1500년, 카라벨 한 척과 3,200닢"**이었던 것을 고쳤다 — 연도 고정은 최상위 지침 위반이고 시작 조건도 사실이 아니었다(낡은 바사 + 900닢).
  - 검증: `test-rules` 65 · `test-world` 10 · `check-evidence`·`check-routes`·`check-wages`·`check-prices` 전부 PASS.
  - **문서 품질 패스에서 stale 4건을 더 잡았다** — ①`economy-trade.md`의 무역 곡선 표가 옛 배 순서(카라벨 4항차·라구사 50항차)여서 실측으로 교체(코카 2 · 카라벨 12 · 브리간틴 24 · 캐랙/플류트 36 · 갈레온 55 · 아르고시 74) ②`dev-workflow.md` 검증 목록에 `check-wages`·`check-prices`·`check-map`이 빠져 있었다 ③`world-npc.md`의 계약 보수 공식이 코드와 어긋나 있었다(×1.30~1.55 → 실제 `payMul` 1.22~1.42 · 선금 30% → 25%)이고 "보수를 먼저 정하고 수량을 역산한다"는 핵심 구조가 그 절에 없었다 ④`QUICKMAP-game.md`(현 `QUICKMAP-trade.md`) §3에 이번 교훈 4개 추가(임금을 내리면 후반이 풍족해진다 · `ROUTE_RISK`는 조우확률과 보험료 두 곳에서 쓰인다 · 교역품 값을 올리면 매입 자본이 커져 시뮬이 파산한다 · `GOODS[].base`를 고치면 근거도 같은 커밋에서).
- **[2026-08-15] #dashboard #evidence #balance** — **대시보드에 보수 탭 신설**(경제·해적 아래 세 번째). 최근 15~16세기 급여 조사를 화면으로 옮겼다.
  - 화면 6절: 하루치 보수 사다리(부관 16닢 : 선원 2.4닢 = ×6.67) · **사료 대조**(c.1500 ×2.0~2.3 / 1634-35 ×5.7 구간 위에 게임 값 마커) · 에이미 수입 구성(급여·성과급 누적 면적 + 성과급이 급여를 앞지르는 항차) · 항해비 중 부관 몫(45/24/21/**10**%) · **부관 유무 짝지어 비교** · 근거표.
  - **대조는 배율로만** 한다는 것을 구조로 못 박았다 — 게임 화폐 ‘닢’은 실화폐가 아니라 절대액 비교가 성립하지 않는다. 근거 JSON에도 caveat로 적었다.
  - 근거 계층을 도시·항로와 같은 3층으로 깔았다: `content/wage-evidence.json`(앵커 2·발견 3) + `tools/check-wages.mjs`(코드 값·파생 배율·판정 형식 검사). 급여만 고치고 근거를 안 고치면 사료 그래프가 옛 배율 위에 새 값을 찍는다.
  - 계측 `dashboard/wages.mjs`는 **Math.random을 시드 RNG로 잠시 갈아 끼워** 같은 세계를 부관 있음/없음으로 두 번 돌린다(`state.officer = null`). officer.md가 기록한 "그냥 두 번 돌리면 부호가 뒤집힌다"를 코드로 막은 것.
  - **재측정이 stale을 잡았다** — 40쌍 실측에서 20항차 판정이 **+1,421닢(24/35승) → −1,220닢(10/40승)**으로 뒤집혀 있었다. 몰타 추가·항로 위험도 개편이 중반 구간을 내려앉힌 것. 초반 3·5항차 값(−178·−322)은 그대로라 초반은 결정적이고 중반이 콘텐츠에 민감하다는 뜻이다. officer.md 표를 정정했다.
  - 판정문(warnbox)을 고정 문구에서 **실측 생성**으로 바꿨다 — "20항차부터 뒤집힌다" 같은 문장은 콘텐츠가 바뀌면 조용히 거짓이 된다.
  - 그리는 김에 공용 결함 하나: `.legend`가 색 견본용 flex라 문장을 넣으면 낱말마다 14px씩 벌어졌다 → `.legend.para`(block) 신설.
- **[2026-08-15] #art #brief #review** — **지도 1차 납품 검수 → 반려.** 실제 지중해를 정확하고 아름답게 그렸는데 게임에는 못 쓴다.
  - **원인은 그림이 아니라 의뢰서였다.** 게임 지도는 실제 투영이 아니라 도식인데(위경도 최소제곱 잔차 **최대 55px**) 1차 의뢰서에 그 경고가 없었다. 그림쟁이는 진짜 지중해를 그렸고, 그래서 도시 15/16곳이 해안선 밖(나폴리·아테네·로도스·알렉산드리아는 바다 한복판, 팔레르모·튀니스·몰타는 사하라 내륙), 항로 25/28개가 육지를 관통했다. 육지 비율 70%(화면 아래 25%가 통짜 사하라).
  - 그 위에 **손실 WebP** — PNG는 48색으로 정확한데 webp가 10,862색으로 번졌다(15% 픽셀이 8단계 이상 차이). 게임이 4배 확대하므로 안티에일리어싱을 다시 넣은 것과 같다.
  - 처방 셋: ① 의뢰서 **§0-A 신설**("좌표를 먼저 놓고 지형을 맞춰라" + 1차 검수 결과표 + 무손실 webp 저장법) ② **기준판 3종**(`assets/map-reference/coord-target.png`·`-4x.png`·`rejected-overlay.png`) ③ **기계 검수** `tools/check-map.py` — 규격·색수·도시 해안선·항로·바다 소음을 판정하고 좌표는 `geo.js`에서 직접 읽는다(의뢰서와 어긋날 일이 없다).
  - 납품물은 `assets/map/`에 보관하되 `manifest.json`에서 비활성화했다 — 붙여 두면 항구 점이 사막에 찍힌다.
- **[2026-08-15] #memory #principle** — **최상위 지침 신설: 연도를 고정하지 않는다.** 몰타를 넣으면서 "기사단이 로도스를 잃고 몰타로 갔으니 로도스는 오스만이어야 한다"고 연대를 1535년으로 못 박았는데, 사용자가 뒤집었다 — 연도에 매몰되면 수정 범위가 커지고 콘텐츠가 준다. 로도스를 기사단령으로 되돌려 **둘 다 기사단령**으로 두고, 근거 JSON에 `verdict: "gameplay"`로 표시했다. `era`를 "대항해시대(15세기말~16세기)"로 느슨하게 바꾸고 두 근거 파일(city·route)에 같은 취지를 적었다. 테스트도 "로도스=오스만"에서 "로도스도 기사단령(의도적 예외)"으로 뒤집어 누가 고증을 이유로 되돌리면 걸리게 했다.
- **[2026-08-15] #game #content** — **몰타 추가(16번째 도시).** 시칠리아 해협 병목의 기사단 요새섬. `prizeYard`(나포선 경매항)이고 곡물 수요가 16곳 중 최고(1.44), 특산은 고조 염전 소금뿐. 팔레르모·튀니스와만 잇고 직항 palermo~tunis는 남겨 "몰타를 들르는 건 선택"으로 뒀다. **나포품 재판매를 `supply`로 넣지 않은 것은 의도적** — 향신료·비단을 싸게 풀면 산지보다 싼 창구가 생겨 경제가 통째로 쏠린다. 지도에서 시칠리아와 **0px(맞붙음)**이던 섬 좌표를 실제 거리(90km≈9px)만큼 떼어 `[169,161]`로 옮겼다.
- **[2026-08-15] #game #balance #evidence** — **항로별 해적 위험도 배선.** 정의만 되고 호출부가 0곳이던 `pirateThreat()`를 드디어 살렸다. `rollSeaEvent`가 항로를 받고, 확률은 **당대 해상보험 요율**(`ROUTE_RISK`)에서 나온다 — 팔레르모 7~8%·튀니스 9%·아드리아 2% 같은 제노바 장부 값이 앵커. 환산식 `0.05 + 요율×0.026 + 해적수×0.04`로 10.2~28.4%가 되고 **평균은 종전 고정값 18%와 같다**(난이도 총량은 두고 갈랐다). weight 합 100은 calm에서 덜어내 유지. 근거 계층 `content/route-evidence.json`(28항로)과 기계검증 `tools/check-routes.mjs`를 도시 특산품과 같은 구조로 깔았다. 출항 카드에 위험 라벨을 띄워 읽히게 했다.
  - **재측정이 부작용을 잡았다** — `sim-core`는 해상 이벤트를 모델링하지 않아 자산 곡선으로는 안 잡히므로 `tools/sim-risk.mjs`(교통량 가중 실효 조우율)를 새로 만들었다. 결과 18%→**10.3%**: 최적 플레이 90항차 중 33항차가 아나톨리아 내해(위험 0)라 게임이 더 쉬워졌다. 내해에서 해적을 뺀 것 자체는 옳으므로(마르마라해·육로 80km에 코르세어는 오류) 대체 이벤트(육로 도적·통행세)를 후보로 남겼다.
- **[2026-08-15] #game #character** — **에이미를 자동 동행으로.** 등용하는 인물이 아니라 첫날부터 타고 있는 부주인공이 됐다 — 계약금(1,800)·퇴직금·면접·해고를 전부 걷어내고 `resetGame()`이 0일차부터 승선시킨다. 머리는 금발(`PAL.sand` 3톤 — `goldM`은 금속용이라 투구처럼 보인다), 금비녀는 금발에 묻혀 밑동을 의상색으로 받쳤다. 항구 카드는 버튼 없는 살림 창이 되고 모든 항구에서 뜬다. **밸런스: 같은 시드 40쌍으로 재측정** — 급여가 1일차부터 나가 10항차 −1,204닢(0/40 승률), 20항차부터 뒤집혀 최종 +11,497닢(33/40). "같이 굶다가 같이 번다"가 지금 설계다.
- **[2026-08-15] #dashboard** — **경제 대시보드를 통합 대시보드로.** 왼쪽 사이드바 + 탭(경제/해적). 기존 경제 화면은 그대로 두고 해적 탭을 더했다 — 조우 빈도(20만회 실측)·등급별 발생확률(자산 12구간 × `pickEnemy` 8천회)·현상금·**NPC 위치 재생**(지도 위 프레임 스크럽)·항로별 위험도·지도와 조우의 연결(11%/89%). 그리기 헬퍼를 `shared.mjs`로 뽑아 두 탭이 같은 색을 쓰게 했다. 전리품 계산 `pirateEnemy()`가 `scenes/map.js`에 갇혀 있어 대시보드가 재구현할 뻔한 것을 **규칙이므로 `world.js`로 이동**시켰다.
  - 발견: NPC 해적은 턴 만큼 부유해져 **현상금에 상한이 없다**(전과 6건 해적이 38,802~64,670닢 — 최상위 등급의 9배).
- **[2026-08-15] #art #brief** — **그림 발주 의뢰서 2건.** `assets/PORT-BACKGROUND-BRIEF.md`(항구 16장)와 `assets/WORLD-MAP-BRIEF.md`(지도 1장). 다른 AI에 넘길 수 있게 규격·화면구조·도시별 지시·검수기준까지. 핵심은 **"어디에 이미지가 들어가는지"** — 수평선 y=150·흘수선 y=168·부두 y=186 고정, 플레이어 배가 x132~308·y64~192를 가리므로 **랜드마크는 하늘(y<64)로 세워야** 안 가려진다. 생성형 AI 후처리(최근접이웃 축소→양자화→1px 손보정)를 필수로 명시했다.
- **[2026-08-14] #game #balance** — **부관 보수를 "성과급만"에서 "급여 + 성과 배분"으로.** 동업자이지 하인이 아니다.
  - 성과급만 두면 **못 버는 달에 한 푼도 안 나가** 고용인만도 못한 대우가 된다. 고정 급여 `OFFICER.wage`(16닢/일)를 넣고 성과급을 12%→11%로 내렸다. 선원 일당이 2.4닢이니 선원 예닐곱 몫 — 선장의 오른팔값으로 무겁지만 터무니없지 않다.
  - 급여는 `voyageCost()`에 **별도 항목(`officer`)으로** 세웠다. 선원 일당에 뭉쳐 넣으면 "부관을 데리고 있는 값"이 얼마인지 플레이어가 읽을 수 없다. 항해 비용 툴팁·입항 로그·부관 카드 세 곳에 따로 나온다. `state.officer.paid`(급여)와 `earned`(성과급)도 나눠 센다.
  - **총액을 맞췄는데 균형이 안 맞았다.** 성과급만 12%였을 때(90항차 12,451닢)와 부담이 비슷하도록 8%+급여16으로 짰더니 순효과가 +14.0%→**+21.1%로 올라갔다.** 성과급은 잘 벌 때 더 떼므로 성장기 재투자 자본을 깎아 **복리로** 아프고, 급여는 고정이라 규모가 커질수록 가벼워지기 때문이다. 성과급을 11%로 되올려 **+15.7%(27/40승)**에 안착시켰다.
  - 최종 보수: 계약금 1,800 + 90항차 급여 4,448 + 성과급 11,401 = **15,849닢**(이전 12,451닢 대비 +27%). 받는 몫은 늘었는데 플레이어 순효과는 유지됐다.
  - 대사도 동업 쪽으로 — *"급료는 정확히 받겠습니다. 대신 그보다 많이 벌어 드리죠"* · *"동업이라면 셈이 분명해야 오래 갑니다."*
  - 검증: `test-rules` 55 PASS(급여 2개 신설) · `test-world` 10 · `check-evidence` PASS.

- **[2026-08-14] #game #art #char** — **부관 에이미 추가.** 이 바다에 이름을 가진 두 번째 사람.
  - **한 명뿐인 인물**로 못 박았다 — 목록(`OFFICERS`)이 아니라 상수(`data.js: OFFICER`). 갈아 끼우는 부품이 아니라는 것을 자료구조로 표현한 것.
  - 설계 축은 **"쓸모와 대가가 같은 화폐로 매겨진다"** — 항해술도 검술도 손대지 않고 **오직 돈만** 만진다(입항세 −35% · 시장압력 −22% · 계약보수 +12% · 표류물 +50% · 해상흥정 −15%). 대신 매각 이익의 12%를 떼 간다. 거래가 작으면 손해고 커질수록 남으므로 "후반 금화가 갈 곳이 없다"와 "부관을 언제 쓰나"가 한 장치로 풀린다.
  - **효과는 전부 기존 파생 함수에 계수로 곱했다**(`tariffRate`·`impactFactor`·`contractOffer`). 새 계산 경로를 팠다면 부관 유무로 값이 두 갈래가 돼 어느 쪽이 정답인지 알 수 없게 된다. 징수도 `sell()` 한 곳뿐.
  - **`SEA_EVENTS`에 새 항목을 추가하지 않았다.** weight 합이 이미 조율된 값이라 늘리면 조우 빈도가 통째로 흔들린다. 대신 **있던 이벤트가 다르게 풀리게** 했다 — 표류물을 더 건지고, 상선 흥정을 더 깎고, 모달 끝에 대사 한 줄(`officerAside`).
  - **등장 조건이 곧 성격이다** — 베네치아에서만 만나고(`OFFICER.home`), **물 새는 배를 모는 선장에게는 오지 않는다**(`officerOffer().poor`). 결과적으로 초반 목표("제대로 된 배 사기") 직후의 보상 자리에 놓인다. 갑판 슬롯은 먹지 않는다(`TROOPS`에 없음) — 먹였다면 배치 칸이 5→4로 줄어 초반이 빡빡해졌다.
  - **밸런스는 세 번 갈아엎었다.** 처음 성과급 10%·압력감면 25%가 총자산 +62%로 "고용 안 할 이유가 없는" 장치였다. 조이려고 18%·15%로 갔더니 이번엔 0.0%(7/20승)로 "고용할 이유가 없는" 것이 됐다. **12%·22%**에서 +14.0%(24/40승)로 안착 — 승률 6할이 노림수다.
  - 그 과정에서 **측정 방법 자체가 틀렸던 것**을 잡았다. 그냥 두 번 돌려 비교했더니 기준선이 41,377↔51,519로 25%씩 튀어 **효과의 부호가 뒤집혔다**(같은 설정에서 −20%와 +62%가 나왔다). 원인은 시뮬이 "돈이 모이면 즉시 큰 배를 사는" 탓 — 자산 스냅샷이 "방금 배를 샀는가"에 지배된다. **같은 시드로 짝지어(paired)** 돌리고 지표를 **총자산(금화+선단 매각가)**으로 바꾸고서야 안정됐다.
  - **스프라이트**: 병종은 전부 같은 코트 실루엣이라 색만 바꾸면 "또 다른 병종"으로 읽힌다. `UNITS[].body:'fem'` 분기로 여성 바디(좁은 어깨·조인 허리·A라인 치마)를 넣고 `longhair`·`ledger`(장부) 파츠와 `teal` 스킴을 더했다. 렌더 확인에서 세 번 고쳤다 — ①치마가 원통형이라 뭉툭 → A라인으로 ②장부가 펼친 흰 지면이라 얼굴보다 눈에 띔 → 덮은 가죽 표지로 ③깃펜이 몸에서 떨어져 허공에 뜬 막대 → 손에 붙임. 얼굴도 병종의 1px 눈으로는 표정이 없어 눈 2px + 속눈썹 + 눈동자 하이라이트를 얹었다.
  - 검증: `test-rules` 53 PASS(부관 15개 신설) · `test-world` 10 · `check-evidence` PASS · 브라우저 실동작(미고용/고용 두 상태) 확인.

- **[2026-08-14] #game #ship** — **조선소를 "국적별 판매 목록"에서 "도시 공업력"으로 바꾸고, 선종을 늘렸다.**
  - 조사 결과 베네치아 아르세날레는 배를 **팔지 않았다** — 국가 소유였고 무다마다 인칸토 경매로 **운항권만 귀족에게 임대**했다. 바르셀로나·나폴리·팔레르모 조선소도 전부 왕실 군용 갤리 시설이다. 반면 캐랙·카라벨 같은 **원양 상선은 민간이 자유롭게 건조·매매**하던 영역이라, 국영 갤리 프레임을 상선에까지 씌운 것이 과했다.
  - `map/geo.js: industry`(0~3) ≥ `SHIPS[].tier`면 짓는다. **제 나라 배는 한 등급 쉽다**(`originFlag`가 그 항구 깃발이면 −1). `yards`는 판매처가 아니라 **값이 싸지는 전통 조선지**로 의미가 바뀌었다. 공업력 여유·전통 조선지에 따라 같은 배도 항구마다 값이 다르다(`shipPriceAt`).
  - 공업력: 베네치아·제노바·이스탄불 3(아르세날레·캐랙 발상지·테르사네이 아미레) / 마르세유·바르셀로나·나폴리·팔레르모·알렉산드리아 2 / 튀니스·알제·아테네·로도스·베이루트 1 / 부르사·이즈니크 0(내륙).
  - **선종 9 → 11종.** 재래선을 살려 뒀다 — **코카**(1,100닢 원형 상선, 첫 갈아탈 자리)·**갤리**(노와 라틴세일, 바람이 죽어도 가지만 짐이 적다). 시대를 벗어난 이름도 정리 — **블랙월 프리깃(1837년!) → 갈레아스** · **인디아맨 → 라구사 아르고시** · 슈퍼 프리깃 → 대형 갈레온. 브리간틴 국적을 영국→지중해, 캐랙을 베네치아→제노바로 바로잡았다.
  - **해금**: `requires`로 "그 배를 몰아 본 선주에게만 내놓는다"를 넣었다(`state.everOwned`). 갈레온은 캐랙을, 갈레아스는 갤리를 거쳐야 한다. **나포한 배도 몰아 본 것으로 친다** — 해금 경로가 하나 더 생긴다.
  - **중고선 시장**(`usedListings`/`buyUsed`) — 실제로 즉시 손에 넣을 수 있는 배는 신조가 아니라 중고·나포선이었다. 항구마다 사흘 주기로 갈리고, 정가의 52~74%에 선체가 45~85%만 남아 있다. **튀니스·알제는 `prizeYard`** — 자체 건조가 약해 나포 상선 개조가 주된 함대 확보 수단이었다는 고증 그대로, 매물이 더 자주 더 싸게 걸린다. 조선소에 **중고 탭** 신설.
  - **계약 보수가 너무 컸다.** 수량(30~90)을 먼저 뽑고 단가를 곱하니 비단·금괴가 걸리면 계약 하나가 5만 닢을 넘었다(시작 자금 900닢). 이제 목표 보수(`CONTRACT.value`, 도시 size로 스케일)를 정하고 **수량을 역산**한다 — 786~5,859닢, 평균 2,863닢.
  - 시뮬 `ORDER`를 화물칸 오름차순으로 바꾸고 **지금 타는 배보다 나은 것만** 사게 고쳤다(카라벨에서 코카로 하향 갈아타고 있었다).
  - 검증: `test-rules` 38 PASS(중고선·공업력·해금 테스트 신설) · `test-world` 10 · `check-evidence` PASS · 자산 곡선 90항차 3,244~21,162닢.

- **[2026-08-14] #game #map #tool** — **부르사·이즈니크 추가 + 도시 데이터 3계층화.**
  - 도자기·비단 산지가 전부 **이즈니크의 그림자**였던 문제를 실제 도시를 넣어 풀었다. 위계가 정리됐다 — 비단 **부르사(0.46) < 이스탄불(0.62)**, 도자기 **이즈니크(0.44) < 이스탄불(0.70) < 로도스(0.74)**. 산지 → 수도 재유통 → 중계항 순이다.
  - 둘 다 실제로는 **내륙**이다(부르사 외항은 뮈단야, 이즈니크는 호수 동안). 항구 게임이라 마르마라 남안에 두고 **이스탄불에서만 들어가는 막다른 주머니**로 만들었다 — "안쪽까지 들어가면 원가에 산다"는 구조가 생긴다. 측정 결과 NPC가 실제로 들어가 물동량 2,628·1,882.
  - **도시 데이터를 3계층으로 확정** — ①`map/geo.js`(UI: 좌표·깃발) ②`data.js: CITY_TRADE`(테이블: 수치) ③`content/city-evidence.json`(**근거 정본**: 항목별 판정·사료·출처). 마크다운 서술본은 ③을 풀어쓴 것.
  - `tools/check-evidence.mjs` 신설 — 코드와 근거가 어긋나거나(값·방향), 근거가 없거나, 근거에만 있는 유령 항목이면 **exit 1**. 수치만 고치고 근거를 안 고치는 것을 구조적으로 막는다. 현재 75항목 전부 통과(확인 28·개연 26·바로잡음 17·게임성 예외 4).
  - 대시보드 매트릭스에 **근거** 모드 추가 — 칸마다 판정 배지(●확인 ◆바로잡음 ○개연 ▲게임성)와 툴팁 출처.
  - 좌표는 처음 (233,68)·(246,66)에 뒀다가 이스탄불과 **이름표가 4건 겹쳐**(라벨↔라벨 1 · 라벨↔마커 3) (222,66)·(242,76)으로 옮겼다. 클릭 판정이 반경 6px이고 이름표가 도시 위에 배경 박스째 그려지기 때문이다.
  - 검증: `test-rules` 25 · `test-world` 10 · `check-evidence` PASS · 셀 180 중 거래0은 2~3칸 · 라벨/클릭 충돌 0.
  - 자산 곡선(7회 중앙값): 10항차 4,866 · 30항차 11,722 · 90항차 21,213닢. 항로 그래프가 넓어져 **초반이 다소 후해졌다** — 더 조이려면 `SPREAD`(현재 0.62)를 내린다.

- **[2026-08-14] #game #research** — **도시 특산품 고증 반영.** 15~16세기 지중해 교역을 세 갈래(서지중해/남부이탈리아·마그레브/동지중해)로 나눠 웹 사료를 대조하고 `CITY_TRADE`·깃발을 전면 개정했다. 근거·출처·의도적 예외는 `wiki/city-goods-history.md`가 정본.
  - **깃발 3건이 사실오류였다** — 로도스 `venice`→`hospitaller`(1522년까지 성 요한 기사단령, 베네치아령이었던 적 없음) · 마르세유 `genoa`→`france`(1481년 프로방스 병합, 게다가 제노바와 적대였고 소개문은 이미 "프랑스의 관문"이라 자기모순) · 튀니스 `ottoman`→`hafsid`(오스만 확정은 1574년). `FLAGS`에 도안 2종 추가.
  - **방향이 뒤집혀 있던 것** — 알제·튀니스를 곡물 최고 수요지로 뒀는데 둘 다 곡물 **수출**지였다(미티드자 평원="알제의 빵바구니", 하프스 튀니스의 곡물·올리브유 수출). 제노바 비단은 수요가 아니라 조알리 벨벳 **산지**, 팔레르모 와인은 자체 산지(이슬람 지배기 양조의 화학적 증거)라 수요에서 뺐다.
  - **근거가 약하던 공급재** — 아테네 도자기 제거(고대 아티카 도기와 **시대 혼동**, 근세 산지는 이즈니크) · 이스탄불 향신료 제거(산지 아닌 최종 소비지) · 베이루트 향신료를 알렉산드리아 아래 위계로(본선은 홍해→알렉산드리아, 알레포→트리폴리) · 제노바 무기 약화(본산은 밀라노·브레시아) · 알제 모피→곡물.
  - **죽은 칸을 살렸다** — 모피는 지중해에 산지가 없어(흑해·발트산) 수요 5곳이 죽을 뻔했다. 보스포루스 관문인 이스탄불을 산지로 세워 해결(고증에도 맞다). 이스탄불에 곡물 수요를 신설해 마그레브에서 뺀 곡물 항로를 대체.
  - **대시보드가 실제로 일을 했다** — 이스탄불 곡물 수요를 넣었는데 유입이 **1**로 측정됐다(NPC가 이웃 한 칸만 보므로 로도스·아테네가 먼저 흡수). `alexandria↔istanbul` 직항을 놓자 193~428로 흘렀다. 오스만의 이집트 밀 직송로라 고증상으로도 정확하다.
  - 검증: `test-rules` 25 PASS · `test-world` 10 PASS · 거래 0인 칸 4→2/156 · 수요칸 유입0 평균 0.3~0.7/36. 자산 곡선은 기존 범위 유지(초반 1항차 1,116→1,510닢으로 소폭 상승).

- **[2026-08-14] #collab #infra** — GitHub 공개(`somanghao/tradeship`, public/main) + 협업 가능하도록 **세 영역 분리**. 다른 개발자가 남의 파일을 열지 않고 일할 수 있게 경계를 그었다.
  - **그림**: `bake(key,…)`가 모든 스프라이트의 단일 통로라는 점을 이용해 여기에 오버라이드 훅을 걸었다(`js/assets.js`). `assets/manifest.json`에 `"키": "파일.png"`만 적으면 코드 수정 없이 그 스프라이트가 PNG로 대체된다. 키를 알 수 있게 `pixel.js: keyOf/knownKeys` 역맵을 두고 `preview.html`이 그림마다 키를 표시(+ 전체 복사 버튼). 규격 불일치는 콘솔 경고.
  - **NPC**: `world.js`를 3겹으로 갈랐다 — 숫자만 있는 `npc/config.js`, 판단만 하는 `npc/behavior.js`(게임 모듈을 import하지 않고 `ctx`로만 받는다), 집행하는 `world.js`. 행동 규칙을 통째로 갈아 끼워도 `world.js`는 그대로다.
  - **지도**: 좌표·항로·해류를 `js/map/geo.js: CITY_GEO/ROUTES/CURRENTS`로 분리하고, `data.js`는 경제(`CITY_TRADE`)만 갖는다. `CITIES`는 둘을 id로 합성한 결과라 읽는 쪽 코드는 그대로. 한쪽에만 도시를 추가하면 콘솔 경고가 뜬다.
  - 문서: 루트 `CONTRIBUTING.md`(영역 표·검증 절차) · `assets/README.md`(그림 교체 절차) · `.github/CODEOWNERS` · `README.md`. 협업자 `yoush28` write 초대.
  - 회귀: `test-rules` 25 PASS · `test-world` 10 PASS · `sim-trade` 곡선 유지 · 전 모듈 import 링크 확인.

- **[2026-08-14] #game #tool** — **경제 대시보드** `/dashboard/` 신설. "NPC가 실제로 무역을 하는가 · 가격이 정말 움직이는가 · 물자가 부족한 항구까지 닿는가"를 눈이 아니라 수치로 본다.
  - 규칙을 재구현하지 않는다 — 게임 모듈(`state.js`·`world.js`)을 그대로 import해 돌린다. 재구현했다면 "대시보드에서는 맞는데 게임에서는 다른" 지표가 됐을 것이다.
  - 이를 위해 `tools/sim-trade.mjs`를 몸통(`tools/sim-core.mjs`, 출력 없음 + `hooks.onVoyage`)과 CLI 출력으로 갈랐다. 계측은 `dashboard/measure.mjs`에 DOM 없이 두어 node로도 검증된다.
  - 화면: 요약 카드 · 현금흐름(주인공 자산 vs NPC 상인 자산 띠 + 배 갈아탄 시점) · 항차별 수지 막대 · 도시×품목 매트릭스 5모드(가격/배율/변동폭/물동량/압력) · **부족한데 아무도 안 나르는 곳** · 가격이 안 움직이는 칸 · 선박표 · NPC 목록 · 거래·습격 로그.
  - `serve.py`에 `.mjs` MIME 등록(파이썬 `mimetypes`에 없어 `text/plain`으로 나가면 브라우저가 모듈 로드를 거부).
  - 첫 관측(90항차): 수요 33칸 중 유입 0인 칸은 없으나 베이루트 유리 9·베네치아 곡물 12는 사실상 미도달. 156칸 중 3칸은 거래가 아예 0.
