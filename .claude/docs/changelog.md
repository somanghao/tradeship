# changelog — tradeship

> 변경 이력 정본(lazy·거의 안 읽음). `claude-memory.md`에는 **현재 상태만** 두고,
> "무엇을 어떻게 고쳤다"는 여기에 쌓는다.
> 상한 40KB — 넘으면 시점 경계로 분할한다. 과거분:
> [changelog-2026-08-29-pm.md](changelog-2026-08-29-pm.md)(08-29 뒷부분 · 회차 24·25) ·
> [changelog-2026-08-29-am.md](changelog-2026-08-29-am.md)(08-29 앞부분 · 회차 22·23) ·
> [changelog-2026-08-25-pm.md](changelog-2026-08-25-pm.md)(08-25 오후·저녁) ·
> [changelog-2026-08-25-am.md](changelog-2026-08-25-am.md)(08-25 앞부분) ·
> [changelog-2026-08-23-24.md](changelog-2026-08-23-24.md)(08-23 ~ 08-24) ·
> [changelog-2026-08-22.md](changelog-2026-08-22.md)(08-22 하루) ·
> [changelog-2026-08-17-21.md](changelog-2026-08-17-21.md)(08-17 ~ 08-21) ·
> [changelog-2026-08-16.md](changelog-2026-08-16.md)(08-16 하루) ·
> [changelog-2026-08-15.md](changelog-2026-08-15.md)(08-15 하루) ·
> [changelog-2026-08-early.md](changelog-2026-08-early.md)(08-08 ~ 08-15 앞부분).

## 2026-09

- **[2026-09-02] #engine #world #trade — 회차 30, 「R5·근거·규칙감사」.** 사용자 지시
  *"커밋 푸시하고 지도 이미지 캐릭터들은 빼고 나머지 진행"* → MAP과 그림 넷을 빼고 나머지를 밟았다.
  회차 29가 워크트리에 미커밋으로 남아 있어 먼저 검사 열여덟·`test-rules` 486/486·스모크 0을
  다시 돌려 세 갈래로 올렸다(`cfd45db`·`f0c9a3c`·`185d97d`).
  - ★★★ **패권 ③이 안 채워지던 원인은 배가 아니라 선단이었다(R5).** 회차 29는 「만나서 싸웠고
    다 졌다」로 적고 *"카리브엔 t3가 있으니 배가 원인이 아니다"*까지 갈랐는데, **그 다음 칸**이
    이것이었다 — 로그를 다시 보니 사냥 내내 **동행 0척**이었다. `r5-probe-power.mjs`가 게임의
    피해 식으로 직접 풀었다: **동행 0척이면 어떤 t3 기함으로도 지고 2척이면 이긴다**
    (포 `gunShare` · 피격 `shieldPer` · 갑판 `meleePerHand`가 한꺼번에 걸린다).
    ⇒ **카리브 ③ 달성**(스페인 은함대 호위기함 · 18항해째 89일차 · **7전 7승 · 진 것 0건**)에
    이어 **중동 ③도 달성**(포르투갈 인도 함대 · **12일차** · 4전 4승 · 등급5를 두 번 이겼다).
    회차 29엔 카리브가 3전 3패, 중동이 **9전 9패 · 금고 −1.63M**이었다.
    ★ **한 고침이 두 바다를 함께 풀었다** — 회차 29가 「같은 원인」이라 본 것이 맞았다.
    ★ **게임 규칙은 한 줄도 안 바꿨다** — 러너가 갖고 들어간 것만 달라졌다.
    ⇒ **패권 표: 중동 4/4 · 카리브 3/4**.
  - ★★★ **한 겹을 벗기면 다음 겹이 나왔다 — 여섯 번.** ① 사전 채비가 `hegemony-run`엔 아예 없었다
    (「고침이 한 러너에만」 **네 번째 재발** — 회차 22가 `grand-run22`에 넣은 것을 회차 27이
    `__r22_hunt` 한 줄만 이식했다) → ② 배를 먼저 사고 동료를 나중에 구해 **배와 선장이 다른 항구에
    갈렸다** → ③ `ensureMate`가 「사람」 탭을 열어 둔 채라 **「조선소로 간다」가 레이아웃에 없어**
    조용히 실패했다(`industry: 3`인 아바나에서도) → ④ `anchored`가 **이미 동행 중인 배까지 세어**
    새 배를 안 샀다 → ⑤ 동행선을 **가장 싼 것**으로 골라 얹힌 포가 **1문**이었다 → ⑥ 사냥 루프의
    보충이 배를 **사냥터 밖으로** 데려가 34초에 12항해가 「지나가고」 조우가 0건이었다.
    ★ **증상이 바뀌면 고쳐진 것이 아니라 다음 겹이다**(졌다 → 안 만난다).
  - ★★ **로그 없는 루프는 죽은 것과 구별되지 않는다.** `ensureFleetPower`의 `say`가 루프 밖에
    하나뿐이라 열 바퀴가 침묵으로 지나갔고 **두 판을 「죽었다」고 오판해 껐다**. 바퀴마다 한 줄을
    내자 그 자리에서 ③이 드러났다. 실패에도 **왜**를 적는다(「조선소를 못 열었다」와 「살 배가 없다」는
    부르는 쪽에서 똑같이 빈손이다). 고침은 전부 **공통 층 `.playtest/_shared/mates.mjs`**에 뒀다.
  - ✅ **항로 근거 108 → 0.** 경고 문구 자체가 사라졌다(「문제 없음 — 요율과 근거가 맞물려 있다」).
    동아시아 40 · 중동 20 · 아프리카 19 · 동남아 19 · 인도양 10. `ROUTE_RISK`는 한 줄도 안 바꿨고
    불일치는 계속 0건 — 왜 그 값인지만 밖으로 꺼내 적었다. 뭍길·내수로는 `null`에 「배가 아니라
    짐이 간다」를 근거로 남겼다(양쯔강·의주대로·세토내해·비와호·크라 지협·베냉 라군).
  - ✅ **규칙 전수 감사가 도구가 됐다** — `tools/check-orphan-rules.mjs`(검사 열아홉 번째 · 경고만).
    `state.js` export 425개를 전수로 훑는다. ★ **이 도구가 두 번 거짓말했다**: ⓐ `''`의 백슬래시가
    셸을 지나며 반으로 줄어 **백스페이스**가 돼 **424/425가 「안 쓰인다」**로 나왔고(→ 식별자 토큰
    Set으로 방식 교체) ⓑ 주석의 단어까지 세어 오탐이 무더기였다(→ 주석 제거). 91→119 · 30→**14**.
  - ★ 그 목록이 **진짜 부채 하나**를 찾아냈다 — **`cargoLure`**. 값나가는 짐이 해적을 부른다는 규칙은
    `encounterOdds`가 `lure`를 안 받으면 그것을 그대로 먹으므로 **위험 칸 숫자엔 이미 있었는데**
    화면이 그 몫을 한 번도 말한 적이 없었다(회차 27 술집 평판과 같은 모양). 항로 카드 툴팁에
    「그중 실은 짐이 부른 몫 +N%p」과 **천장에 닿으면 더 실어도 안 오른다**를 적었다. 규칙은 그대로다.
  - **재집계** — `art.hullTodo` **71종**(선종 122→133이라 회차 28의 60에서 늘었다).
    ⚠️ `sprites/ship.js: HULLS`는 10종이고 133 선종이 나눠 쓰는 구조다 — 버그가 아니다.

- **[2026-09-01] #trade #world #engine — 회차 29, 「조사 → 빠른수정 → 콘텐츠」 3파(서브PM 12).** 사용자 지시
  *"남은일 리스트업 각 서브pm이 보고"* → *"빠르게 할수 있는것들부터"* → *"이미지 지도 캐릭터 빼고 나머지 진행"*.
  **완주했다** — `test-rules` 486/486 · 검사 열여덟 전부 · 스모크 0 · 곡선 **10항차 752닢 불변**(3트랙 × 3회 = 9회 전부 동일).
  - ★★★ **서브PM 둘이 같은 항목에 정반대로 보고했고 셋 다 자가 달랐다.** 「아프리카·남미의 최고선을 제 바다에서 못 짓는다」를
    RULE은 *"이미 열려 있다"*, RUN은 *"안 열린다"*고 냈다. 상위 PM이 판정 함수에 직접 물어 갈랐다 —
    ⓐ 초기 `sellsShip`은 `everOwned`가 비어 **0/28**이 나오고 ⓑ `yardReach`는 참/거짓이 아니라 **필요 공업력 수치**이며
    ⓒ `sellsShip`은 `yardAllowed`(조선지 명부)도 본다. **정본은 `requires` 사슬**이었고 **참인 것은 아프리카 하나뿐**이었다.
    ⇒ 회차 28의 「자를 바꾸니 판정이 뒤집힌다」가 **조사 단계에서 재현**됐다.
  - ★★★ **회차 28의 헤드라인이 흔들렸다** — `sim-guild`가 회차 28엔 「항차 **+5.6%** ↔ 날 −16~−21%」였는데 오늘은
    **항차 −29.0% · 날 −31.5%로 둘 다 음수**라 방향이 안 갈린다. 도구는 그대로다(`677492e` 하나뿐).
    **RUN이 측정을 마친 뒤에 RULE의 선종 102→122(`0327b2b`)가 워크트리에 합쳐졌다** — 두 트랙이 **다른 시점의 세계**를 쟀다.
    ⇒ 규약 신설: **측정값에는 「어느 커밋에서 쟀나」를 함께 적는다**(`wiki/sim-measurement.md`).
    ★ 결론 자체는 살았다 — 「상단은 이문을 깎는 게 아니라 오래 걸리게 한다」는 두 자 모두 음수인 지금도 성립한다.
  - ★★ **「③이 안 채워진다」가 카리브에서도 「만나서 싸웠고 다 졌다」였다**(등급5 3건 · 회피 0 · 3전 3패).
    ★ **카리브엔 t3가 있다**(갈리사브라) ⇒ **t3가 있어도 진다.** 「그 바다에 큰 배가 없다」가 원인이 아니라
    **러너가 전력을 안 갖추고 두목에게 간다**는 쪽이다. 다음 회차의 1번(R5).
  - ★★ **조용한 사고를 하나 막았다** — 남미 해적을 넣다가 카리브의 `drake`와 **id까지 완전히 충돌**했는데
    `ALL_PIRATES`가 **id 유일성을 안 봤다**. 들어갔으면 `state.slain`이 두 바다를 섞어 **패권 ③이 거짓 통과**한다.
    `check-world` **⑦ 명부id충돌** 신설(실패 조건 · `ALL_MATES`도 함께) — **일부러 중복을 만들어 exit 1을 확인**했다.
  - **콘텐츠 — 아홉 바다의 천장이 나란해졌다.** 선종 122 → **133**(카리브·동남아·인도양 각 8→11 · 중동 +1 · 아프리카 +1).
    ★ **중동이 유일하게 t2였다** → `kalyon`(카륜 · 오스만 홍해 함대 · 수에즈). `originFlag` 홈 할인이 `tierNeeded`를 내려
    **공업력 상한 2를 안 건드리고** t3가 열린다(수에즈·아덴·바스라). 이제 여덟 바다가 t3, 동아시아만 t4.
  - **아프리카 사슬을 닫았다** — `naudamina ← guineiro ← caravel(**지중해**)`이라 **아프리카만** 제 바다에서 최고선을 못 지었다.
    ⛔ `caravel`을 아프리카에 뿌리면 **C-2(그 바다와 무관한 배를 판다)가 되살아난다** ⇒ 아프리카 고유 t1 **`barca`** 신설로 대신했다
    (제원은 `caravel`과 동일 — 사다리의 그 칸이므로). ★ **`shipLockedBy`에 `requiresAlt` 신설** — `caravel`만 몰아 본 옛 판도 통과한다.
  - **교역품** 아프리카 7→13 · 중동 7→13 · **해적 명부** 51→**55**(중동·남미 각 4→7).
    ★ **바다끼리 물렸다**(사용자 지시 *"서로 영향을 받고"*) — 알레포 `rawsilk` 신설로 **중동에서 캔 것이 지중해에서 짜인다**
    (지중해 실크 사슬은 이미 돌고 있었는데 **원료 출처가 빈칸**이었다). 상투메 사탕수수는 이미 있었고 **주석이 코드와 반대로** 적혀 있었다.
  - **화면** — 회차 28 규칙 셋(`backerSlain`·`stirSeen`·`guildBackers`)이 **UI 트랙 종료 직후 커밋돼 아무도 안 본 자리**였다.
    ★ 셋에 각각 붙이지 않고 **관계를 움직이는 유일한 문**(`addRegard`)에 얹었다(`state.regardWhy` · 세력당 최근 한 건).
    이월 부채 넷도 닫았다 — **급여일 모달 넘침 487→0px**(수치가 아니라 `fold()` 규약으로) · 술집 `advance` 문 ·
    부관 유예 누계(코드가 *"누계(화면용)"*라 적어 두고 **한 번도 안 쓰던 값**) · 평판 **남은 반감기**.
  - **하네스** — `.playtest/_shared/` 신설(`graph`·`travel-fail`·`mates`). 「고침이 한 러너에만」이 회차 22·27·28 **세 번 반복**된 자리다.
    합칠 수 없는 자리(원양 진입 지점)는 **왜 못 합쳤는지 주석으로** 남겼다.
  - **근거** — `check-routes` 근거없음 141 → **108**(카리브 18·남미 15). ⚠️ **「카리브·중미에 몰렸다」는 착시였다**
    (콘솔이 표본 6개만 보여준다) — **진짜 무게중심은 동아시아 40**이다. `ROUTE_RISK`는 한 줄도 안 바꿨다(불일치 0건).
  - **문서** — 상한 셋을 한꺼번에 풀었다: 라우터 **37→3,540** · `QUICKMAP-trade` 52→3,884 · `QUICKMAP-world` 663→5,105바이트
    (`wiki/holdings-facilities.md`·`world-data-sync.md` 신설 · 무손실 이동). **`C-17`은 이미 해소돼 있었는데 「구멍」으로 남아 있었다.**

## 2026-08

- **[2026-08-30] #trade #engine #world — 회차 28, 「완주·화면·규칙·지도」 4트랙(서브PM 4).** ⛔ **완주하지 못했다** —
  트랙 셋이 **주간 사용 한도로 동시에 끊겼다**(②만 정상 종료). ★ **한 줄도 안 잃었다**:
  「진행하면서 기록한다」 규약 덕에 세 트랙의 `*-PROGRESS.md`가 끊긴 시점까지 다 쓰여 있었다.
  끊긴 트리 그대로 합본 검증 **통과**(`test-rules` 476 → **486/486** · 검사 열여덟 · 스모크 0 · `check-map` 9/9).
  작업판 정본 `.playtest/round-28/WORK.md`.
  - ★★★ **「패권 ③ 등급5가 안 채워진다」의 원인이 회차마다 달랐다 — 셋 다 같은 화면으로 보였다.**
    「안 나온다」(회차 22) → 「피했다」(27) → **「만나서 싸웠고 아홉 번 다 졌다」(28 · 중동 · 조우 28 · 등급5 9건 ·
    회피 0 · 금고 −1.63M)**. 이제 하네스가 셋을 **갈라서 적는다**(`playtest.mjs`의 `declined` — 돌리기 *전에* 넣었다).
  - ★★★ **자를 바꾸니 상단 판정이 뒤집혔다.** `sim-guild`에 일수 갈래를 더하니 **항차로는 +5.6%**(있으나 마나)인데
    **날로는 −16.1%~−21.3%**다. 기전 — **120일차가 47항차 ↔ 44항차** · **60항차가 183일 ↔ 261일(+43%)**.
    ⇒ **상단이 하는 일은 「이문을 깎는 것」이 아니라 「같은 이문에 더 오래 걸리게 하는 것」**이고
    **항차로 재면 그 시간이 안 보인다.** 좌표를 안 건드려도 회차 27의 기전이 재현된다. → `wiki/sim-measurement.md`
  - ★★ **패권 표의 남은 빈칸은 「시간」이 아니었다** — 남미 ①은 **지형**(권역이 두 조각 · 다리가 카야오↔파나마뿐 ·
    못 간 13곳이 예측과 한 자도 안 틀린다), 아프리카 ④는 **교역권 사다리**(카라벨이 28항구 어디서도 안 열린다).
    둘 다 **브라우저 없이** 게임의 판정 함수에 직접 물어 가려냈다. 카리브 ①② · 동아시아 ③은 채웠다.
  - ★★ **`sim-core`는 무역만 잰다** — `tools/sim-encloss.mjs` 신설(난수 없는 기댓값). 조우가 걷어 가는 몫이
    10항차 **24%** · 30항차 **36%** · 60항차 **76%**(도주만 셈한 하한). 근해 60항차 실판에서 **항차이익은 전부 양수인데
    금고는 −911,825닢**이었고 모델 예측 ≈918,000닢과 **자릿수까지 맞는다.** ⛔ `sim-core`에 난수를 넣지 않는다
    (넣으면 `sim-stat 20`의 재현성이 사라진다).
  - ★★ **「돈이 넉넉한 판」만 재면 안 보이는 자리** — `buyShop`·`buyMill`이 **정가를 먼저 빼고 나서** 입회비를 검사해,
    입회비가 모자라면 **정가만 사라지고 시설은 안 섰다**(판매소 27,780닢 · 가공장 20,600닢 · `buyGrow`만 옳았다).
    `test-rules` ⓕ가 `gold = 1e7`로 시작해 그 구간을 한 번도 안 지났다. 검사 ⓕ-2 신설.
  - ★ **화면이 거짓말하던 자리 넷**(트랙 ②) — `captureShip`은 선원이 모자라면 **말없이 해체**하는데 화면은
    `예인 가능`이라 말했다(선종쌍 9,486 중 최소인원이면 **100%가 해체** · 정원을 채워도 10% 불가 ·
    게다가 **방금 싸움이 끝나 선원이 가장 적을 때** 뜨는 모달이다) · 입항세 셋째 겹 · grip 감쇠 · 보험 보상률.
  - **콘텐츠 — 선종 102 → 122.** 얇은 바다부터 채웠다(카리브 4→8 · 아프리카 5→8 · 남미 5→8 · 동남아 5→8 ·
    중동 5→7 · 인도양 6→8 · 대서양 7→9 · 지중해 15→16 · **동아시아 41은 안 건드림**). 근거 JSON 동반.
    ★ **곡선 불변** — `sim-stat 20` 3회 전부 완주 20/20 · 첫 배 8항차 · **10항차 752닢**.
  - **상단 ↔ 세력** — 값이 아니라 **관계**에서 만나게 했다(`backerSlain` 깃발 뒤에는 나라가 있다 · `stirSeen` 사주는
    임자의 마당에서 벌어진다). 둘 다 뺄셈이라 새 수입원이 아니고 **`js/npc/guild.js`는 한 줄도 안 바뀌었다.**
  - **§A-9(수직계열화)·§A-10(경쟁 세력) 두 절을 닫았다** — 4/4단계 · 3/3단계.
  - **문서 정정 넷** — `changelog` 38.2KB **분할**(→14.3KB) · `WORLD-INTEGRATION-TODO §6`「시뮬이 10항차에서 멈춘다」는
    **재현되지 않는다**(직접 확인 · 90항차 826,677닢) · `§A-8e`의 「#37 고침은 한 번도 안 돌았다」 → 돌았다 ·
    **`insureCover` 「전 구간 90%」는 틀렸다**(90%는 인수업자 몫 `INSURE_LOAD`이고 실효 보상률은 구간마다 다르다 — 근해 중앙 71.2%).
  - ⚠️ **기준선이 또 낡았다** — 「10항차 1,294닢」은 회차 27의 `hash()` 고침 뒤 **752닢**이다(다섯 번 재서 전부 752).
    `§A-9`·`§A-10`에 못을 박았다. ★ **세계가 아니라 자(尺)가 움직였다**가 두 회차 연속 재현됐다.
  - ⛔ **못 한 것: 지도 트랙(④).** 사용자가 *"항구와 해안선 맞는지 캡쳐해가며 제대로 확인, 안 맞으면 새로 만들어 비교"*로
    직접 지시했는데 **한 장도 못 찍고 끊겼다.** `check-map` 9/9는 최소조건일 뿐이고(회차 25에 9/9인데 눈으로는 여덟이 아니었다)
    남은 경고는 **항로가 뭍을 스치는 8건**이다. **다음 회차의 1번.**

- **[2026-08-30] #trade #engine #world — 회차 27, 「완주·화면·규칙」 3트랙(서브PM 4 · 자동 루프).**
  사용자 지시 *"지금까지 구현했던 것 확인 후 **남은 개선건 리스트업**해서 맞는 서브PM 체제로 자동 진행"*.
  `UNIMPLEMENTED.md` 전 절에서 **남은 15건**을 뽑아 셋으로 갈랐다. ★ 이번 편성의 핵심은 **①(완주·계기판)이
  게임 코드를 못 만지게 한 것** — 그래야 *"측정기가 거짓 신호를 낸 것인가 게임이 그런 것인가"*를 가릴 사람이 남는다.
  커밋 다섯 — `54f88d1`(바닥·술집·부두) · `14ebb05`(규칙 셋 + `hash()`) · `beb0c66`(동료 검사기) ·
  `6386351`(화면 셋) · 기록. 작업판 정본 `.playtest/round-27/WORK.md`.
  - ★★★ **계기판 — 회차 26의 해석이 반쯤 뒤집혔다. 세계가 가난해진 게 아니라 항차가 짧아졌다.**
    두 측정 사이 바뀐 파일 18개 중 **시뮬이 import하는 것은 `regions/eastasia/geo.js` 12줄뿐**이고
    (조선 항구 다섯의 x·y) **경제 코드는 한 줄도 안 바뀌었다.** 10항차 시점이 **21일차 → 14일차**라
    **날로 재면 −5%·+21%·+4%·−7%**로 흩어진다. `sim-guild 20`을 세 트리에서 돌려 못을 박았다 —
    **`HEAD+옛 geo.js` −19.2%**(회차 25와 자리째 일치) · **`HEAD` +12.1%**. **좌표 다섯이 상단 판정을
    통째로 뒤집는다.** 기전은 **압력은 날로 쌓이는데 판정은 항차로 한다**는 것 ⇒ 손잡이 표의 결론은
    **「아무것도 돌리지 마라」**였다. → `wiki/sim-measurement.md`
  - ★★ **패권 — 막던 것은 시간이 아니라 「탭」이었다.** 회차 24에 옆패널이 탭 넷이 됐는데 거점 줄은 「거래」,
    동료 계약은 「사람」이라 러너의 `.svc-row` 조회가 **NO_ROW로 조용히 아무것도 안 샀다**(note조차 없다).
    그 판이 **거점 0/28 · 상관 0/3**으로 끝났고 그것이 세 회차 동안 「2/9 — 남은 건 시간뿐」으로 읽혔다.
    탭을 열게 한 뒤 **여섯 권역이 28/28 · 돌린 여덟 권역 전부 상관 3/3**. 그리고 **등급 5 두목은
    「안 나온 것」이 아니라 「피한 것」**이었다(정책 충돌을 넣으니 조우 수는 15로 같은데 등급분포가
    `{1:3,2:7,3:5,4:2}` → `{2:3,3:5,4:5,5:3}`). 근해 장사 페이즈 신설(30/30항차·80일) ·
    갈아타기 자기 강등 **0건** · **「시뮬이 10항차에서 멈춘다」는 재현되지 않는다**(90항차 999,933닢).
  - ★★ **규칙 — `state.js: hash()`가 조용히 고장 나 있었다.** FNV-1a가 **끝자리만 다른 문자열에 거의 같은
    값**을 내 `TAVERN.cycle` 2일도 `usedListings` 3일도 사실이 아니었다(베네치아 0번 자리 = 스무 날 내리
    같은 무리). **어떤 검사도 안 걸렸다 — 한 자리를 시간축으로 훑어야 보인다.** 부관 급여 유예(진짜 구멍은
    **분모**였다 — 못 준 비율에 *떠날 수 없는 사람의 몫*이 섞여 있었다) · 술집 평판(자리 −21.6% ·
    계약금 +30.4% · **265 항구를 다 더럽혀도 빈 술집 0곳**) · 상단↔세력(**경계를 다시 쓰지 않고**
    만나는 자리를 `state.js` 한 곳으로) · 동료 등용은 **이미 배선돼 있었다**(작업판이 낡아 해소 전 원문을
    읽었다 — 다시 만들었으면 같은 이름의 함수가 둘이 될 뻔했다).
  - ★ **화면 — 규칙 셋이 서 있는데 화면이 하나도 말하지 않았다.** 파산 안내의 진짜 자리는 **지도**였고
    (항구·술집엔 카드가 있었다) 부산포 금고 0에서 지도가 *"남은 문은 청산뿐"*이라 말할 때 **규칙에는
    선금 124닢이 서 있었다** — 화면이 사람을 **가장 비싼 문**으로 보내고 있었다. 유예 단추·평판 한 줄·
    넷째 문을 붙였고, `port.js`가 `OFFICER.cut` **상수**를 읽어 지분을 내준 뒤에도 영영 11%라 말하던 것을 고쳤다.
    ★ **말이 맞는 것과 화면이 성립하는 것은 다르다** — 상자를 쌓으니 모달이 **47px 넘치고 단추 셋이 안 눌렸다**.
    술집은 **100×86 한 칸**을 비워 굴뚝·항아리·그물·게시판·문이 전부 들어갔고(패널 뒤 물건 밀도 7.1→**0.7%**),
    부두 포장은 **12px 격자 한 벌**이 265곳에 그대로였던 것을 화풍이 재료를 정하게 했다(+22~47%).
  - **검사 476/476** · imports 915이름 0 · dup 0 · architecture · **`check-mates` 신설**(동료 51명 ↔ 세계·규칙·근거) ·
    **스모크 0**.

- **[2026-08-30] #trade #world — 회차 27 트랙③(규칙·경제): 「규칙이 없는 자리」 넷을 채웠다.**
  다-1 동료 등용 · 다-2 부관 급여 유예 · 다-3 술집 평판 · 다-4 상단↔세력. 산출물은
  `.playtest/round-27/RULE-{DESIGN,PROGRESS,ISSUES}.md`와 전용 관측기 넷(`g-probe-*.mjs`).
  - **다-1은 ⑥ 하나만 남아 있었다.** 작업판이 *"배선 없음 · 남은 일 여섯"*이라 적었으나
    `UNIMPLEMENTED §A-5`는 08-25에 닫혀 있었다(상위 PM이 그 아래 §A-5(원문)을 읽었다).
    ⇒ 아홉 권역 근거 JSON에 `mates` 절 신설(51명 전수 · +1,035줄). **직명과 제도만 사료이고 사람은 지어낸 것**이라
    판정은 그 자리가 실재했는가로 매겼고, **보수·특전 수치는 안 옮겼다**(밸런스 값이라 코드가 정본).
  - **다-2** `data.js: OFFICER.defer` + `state.js(canDeferOfficer·officerCut·settlePayroll{deferOfficer})` ·
    `state.officer.share`(영구 지분) · `state.payroll.officerDue/officerDefer/deferMonths`.
    ★ **값을 실측이 네 번 뒤집었다** — +6%p(유예 중에만)는 *안 미룰 이유가 없는 단추*, 영구 +2%p/달은
    *미룰 이유가 없는 단추*, 「연속 N달」 제한은 **한 달 걸러 미루면 뚫려** 제한이 아니었다.
    채택은 **영구 +0.5%p/달 · 지분 상한이 곧 문지기(평생 두 번)** — 아낀 짐 1,616 − 더 문 값 1,532 = **+84닢**.
  - **다-3** `data.js: TAVERN.rep` + `state.crewRep` · `crewRepAt`/`markCrewRep`.
    자리 −21.6% · 계약금 +30.4% · 일당 +5.2% · **참을성 있는 무리 39.2%→22.4%**(나선).
    ★ **날마다 깎는 후크를 안 달았다** — 읽을 때 반감기로 환산하므로 「두 입구」 함정이 없다.
  - **다-4** `FACTION.gripBlocksGuild`(독점이 상단의 물가 좁힘을 45%로 누른다) ·
    `guildCounter`(두꺼운 상관이 웃돈을 최대 40% 깎는다) · 새 판정 `gripHeld`·`guildCounterAt`.
    ★ **경계를 다시 쓰지 않았다** — `test-rules` ⑬은 *손잡이를 베끼지 말라*는 뜻이라,
    **만나는 자리를 `state.js` 한 곳으로** 정했다(`js/npc/guild.js`는 한 줄도 안 바뀐다).
  - **★★ `state.js: hash()`가 조용히 고장 나 있었다.** FNV-1a가 끝자리만 다른 문자열에 거의 같은 값을 내
    **술집(2일)도 중고 매물(3일)도 안 갈리고 있었다**(베네치아 0번 자리 = 스무 날 내리 「성실한 4명」 ·
    마르세유 0번 자리 = 언제나 빔). avalanche 세 줄로 고쳤다. 어떤 검사도 안 걸렸다 —
    **한 자리를 시간축으로 훑어야** 보인다(`wiki/gotchas.md` #11 신설).
    ⚠️ 이 고침이 `tools/test-rules.mjs` ⑥ 절의 난수 의존 한 줄을 깨뜨린다(규칙은 멀쩡하다) → `RULE-ISSUES.md` §B-2.
  - 화면 트랙이 넘긴 둘도 처리 — **X-3** `recoveryOptions().doors`에 **계약 선금 문**(빌리는 문으로 읽히게
    `owed`·`fine`·`due`까지) · **X-2** `save.js: pruneGhostCities()`(유령 도시 id를 열한 필드 + `boons` 세 겹에서 훑는다).
  - 검증: `sim-stat 20`의 주 지표 넷(완주 20/20 · 첫 배 8항차 · 10항차 752닢 · 체불 0%)이 **한 톨도 안 움직였다**.
    30·60항차는 같은 코드 두 번에 8,668↔11,403 / 141,410↔235,757로 갈려 **전후 비교에 못 쓴다**(회차 26과 같은 결론).
  - 메모리: 도메인 파일 둘이 상한(25.6KB)에 닿아 **무손실 이동 넷**(선단·계약 배수 → `wiki/contracts.md` ·
    압류 비 → `payroll.md` · 삭은 배 → `economy-trade.md` · 현상금/사냥터 → `world-npc.md`)으로 자리를 만들고
    always-load에서 둘(`josa`·`cargoLure`)을 도메인으로 내렸다.

- **[2026-08-30] #engine #art #world #trade — 회차 26, 「항구화면·지도·상단」 3트랙 병렬(서브PM 3 · 자동 루프).**
  사용자 지시 *"서브 PM들 다 고용해서 하네스로 **완수 목표까지** 자동 기획-구현-테스트-개선"*. 회차 25가
  각 트랙에 남긴 「할 일」을 그대로 백로그로 삼았다. 커밋 다섯 — `bfcf1c9`(지도) · `5ca408b`(항구) ·
  `c62de7c`(해저) · `b422a75`(상단) · `8c8dd95`(항적). 작업판 정본 `.playtest/round-26/WORK.md`.
  - **항구 — 상단 마흔아홉이 규칙으로만 서 있었다.** 착수 시점 `port.js`의 `guild` 참조가 **0건**이었다.
    회차 25가 물가 안정화를 수치로 증명해 두었는데 **화면이 안 읽어 플레이어는 존재를 몰랐다**(명부 해적
    「984 게임일에 조우 0회」와 같은 자리). G-1 상단 카드 · G-2 사주(진척 막대 · 12개 팔면 `12/20`,
    채우면 금고 +5,902닢) · G-3 신용장·호위·압박 배너(**탭 밖** — 출항 단추와 같은 규약) · G-6 시세 칸
    `−10.0%(상단)`. ★ **민 폭 딱지를 내 거래압력과 색으로 갈랐다** — 뜻이 정반대인 두 수가 같은 색이면
    한 가지로 읽힌다. 술집 방 7.08→**5.24m**(빈 가로줄 47→24%) · 좁은 창 목표 탭 3.47→**2.88화면** ·
    클릭 스윕 7크기×4탭 **치명 0**.
  - **지도 — 바다가 좁았던 것은 가드가 아니라 해안선의 비대칭이었다.** 지중해 33.0→**38.4%** ·
    중동 29.6→**33.3%**(흑해 2,649→5,230px · 비스케이만 · 아덴만 · 페르시아만 북안). 항구는 한 곳도 더
    안 떴다. ★ **회차 25의 「폭 16~28px 물길은 렌더에서 메워진다(가드 사각지대)」가 원인이 아니었다** —
    최대필터 반경을 키우면 좁은 물길이 옆 넓은 바다 값을 **새로 받아 가드가 도리어 풀린다.** 진짜 원인은
    **깎기 상한 2.7px 대 붙이기 상한 없음 14.1px의 비대칭**이고, 그 비대칭을 없애면 **아홉 장 전부에서
    섬 항구가 늘어난다** ⇒ 붙이기에만 비율 상한을 걸었다. 브라질 동북 뿔 수직화 · 중미 「W」 곡선화.
    **말라바르는 안 한다** — 펴면 코친~크랑가노르 **−70.6%**로, 그 지그재그는 장식이 아니라 **연안 항차
    일수를 지고 있다.**
  - **해저 — 좁은 바다에 심해색이 들어설 자리가 없었다.** 홍해가 「강」으로 읽힌 것은 넓이가 아니라 색이다.
    `shoreW`가 띠 폭을 **뭍 덩어리 넓이로만** 정해 폭 30px 물길이 양쪽에서 상한 7px 띠를 받아 여울+얕은바다가
    d 16.8px까지 갔다. 그 자리 **물의 반폭**을 재서(`boxMaxSep`) **칠할 때만** 깎는다 — 백사 1px과 분위수
    표본은 원래 폭 그대로(`cMid q0.34`·`cDeep q0.67` 불변). 홍해 밝기 143.6→**126.8** · 페르시아만
    165.1→**132.1** · **아라비아해 대조군 90.0→90.0**. 아홉 장 바다%는 소수점까지 그대로다.
  - **상단 — 회사가 흥하고 망한다.** 흥망 다섯(결산·배당 / 항차 손실 / 부실→인수 / 합병 / 재기) —
    문 닫은 상단 0→**3곳** · 임자 바뀐 상관 0→**10곳** · 선단6 49→45 · **상관이 빈 항구 0**.
    ★ **항구에 서 있는 동안 상단이 한 발짝도 안 움직이고 있었다** — `waitDays()`가 `decayGuildFlow`만 부르고
    `worldTick`은 안 불렀다(그 입구는 `map.js` 하나). 10일 대기에 자국 −22.4%·항차 **0회** → −5.0%·**+100회**.
    ⚠️ `guild.js` 주석은 *"입구를 하나로 뒀다"*라고 적고 있었다 — **문서가 코드와 어긋난 자리**.
    명부 40은 **이미 도달해 있었다**(같은 판정 경로로 재는 관측기를 새로 만드니 40/40 · 20일 순찰에 만날 확률
    75~83%). 대신 **죽은 사냥터 두 줄**(이어져 있지 않은 항구쌍)을 찾아 고쳤고 `check-world`가 전수로 잡는다.
    조선 근해 장사는 **게임이 이미 성립시킨다**(근해만 돌아도 30항차 금고가 세계 전체의 111%) — 막던 것은 러너다.
    NPC 배가 플레이어와 **같은 폴리라인**을 탄다(항적 꼬리까지). 대시보드 「상단」 탭 · `save.js`에 상단 상태 일곱.
  - **검사 474 → 476/476** · imports 901이름 0 · dup 0 · architecture · check-map 아홉 권역 개별 반려 0 ·
    스모크 0. ★ **회차 25의 「물가 −20.1%」가 지금 트리에서 재현되지 않는다**(회차 26 착수 시점 20시드에서
    30항차 **+7.7%** · 60항차 **+70.7%**). 사이에 바뀐 것은 **지도 좌표**다 — 같은 명령의 10항차 기준선이
    1,294 → 752닢으로, **같은 세계가 아니다.** → `wiki/economy-trade.md`

