# changelog — tradeship

> 변경 이력 정본(lazy·거의 안 읽음). `claude-memory.md`에는 **현재 상태만** 두고,
> "무엇을 어떻게 고쳤다"는 여기에 쌓는다.
> 상한 40KB — 넘으면 시점 경계로 분할한다. 과거분:
> [changelog-2026-08-29-am.md](changelog-2026-08-29-am.md)(08-29 앞부분 · 회차 22·23) ·
> [changelog-2026-08-25-pm.md](changelog-2026-08-25-pm.md)(08-25 오후·저녁) ·
> [changelog-2026-08-25-am.md](changelog-2026-08-25-am.md)(08-25 앞부분) ·
> [changelog-2026-08-23-24.md](changelog-2026-08-23-24.md)(08-23 ~ 08-24) ·
> [changelog-2026-08-22.md](changelog-2026-08-22.md)(08-22 하루) ·
> [changelog-2026-08-17-21.md](changelog-2026-08-17-21.md)(08-17 ~ 08-21) ·
> [changelog-2026-08-16.md](changelog-2026-08-16.md)(08-16 하루) ·
> [changelog-2026-08-15.md](changelog-2026-08-15.md)(08-15 하루) ·
> [changelog-2026-08-early.md](changelog-2026-08-early.md)(08-08 ~ 08-15 앞부분).

## 2026-08

- **[2026-08-30] #engine #art #world #trade — 회차 26, 「항구화면·지도·상단」 3트랙 병렬(서브PM 3 · 자동 루프).**
  사용자 지시 *"서브 PM들 다 고용해서 하네스로 **완수 목표까지** 자동 기획-구현-테스트-개선"*. 회차 25가
  각 트랙에 남긴 「할 일」을 그대로 백로그로 삼았다. 커밋 다섯 — `bfcf1c9`(지도) · `5ca408b`(항구) ·
  `c62de7c`(해저) · `b422a75`(상단) · `8c8dd95`(항적). 작업판 정본 `.playtest/round-26/WORK.md`.
  - **항구 — 상단 마흔아홉이 규칙으로만 서 있었다.** 착수 시점 `port.js`의 `guild` 참조가 **0건**이었다.
    회차 25가 물가 안정화를 수치로 증명해 두었는데 **화면이 안 읽어 플레이어는 존재를 몰랐다**(명부 해적
    「984 게임일에 조우 0회」와 같은 자리). G-1 상단 카드 · G-2 사주(진척 막대 · 12개 팔면 `12/20`,
    채우면 금고 +5,902닢) · G-3 신용장·호위·압박 배너(**탭 밖** — 출항 단추와 같은 규약) · G-6 시세 칸
    `−10.0%(상단)`. ★ **민 폭 딱지를 내 거래압력과 색으로 갈랐다** — 뜻이 정반대인 두 수가 같은 색이면
    한 가지로 읽힌다. 술집 방 7.08→**5.24m**(빈 가로줄 47→24%) · 좁은 창 목표 탭 3.47→**2.88화면** ·
    클릭 스윕 7크기×4탭 **치명 0**.
  - **지도 — 바다가 좁았던 것은 가드가 아니라 해안선의 비대칭이었다.** 지중해 33.0→**38.4%** ·
    중동 29.6→**33.3%**(흑해 2,649→5,230px · 비스케이만 · 아덴만 · 페르시아만 북안). 항구는 한 곳도 더
    안 떴다. ★ **회차 25의 「폭 16~28px 물길은 렌더에서 메워진다(가드 사각지대)」가 원인이 아니었다** —
    최대필터 반경을 키우면 좁은 물길이 옆 넓은 바다 값을 **새로 받아 가드가 도리어 풀린다.** 진짜 원인은
    **깎기 상한 2.7px 대 붙이기 상한 없음 14.1px의 비대칭**이고, 그 비대칭을 없애면 **아홉 장 전부에서
    섬 항구가 늘어난다** ⇒ 붙이기에만 비율 상한을 걸었다. 브라질 동북 뿔 수직화 · 중미 「W」 곡선화.
    **말라바르는 안 한다** — 펴면 코친~크랑가노르 **−70.6%**로, 그 지그재그는 장식이 아니라 **연안 항차
    일수를 지고 있다.**
  - **해저 — 좁은 바다에 심해색이 들어설 자리가 없었다.** 홍해가 「강」으로 읽힌 것은 넓이가 아니라 색이다.
    `shoreW`가 띠 폭을 **뭍 덩어리 넓이로만** 정해 폭 30px 물길이 양쪽에서 상한 7px 띠를 받아 여울+얕은바다가
    d 16.8px까지 갔다. 그 자리 **물의 반폭**을 재서(`boxMaxSep`) **칠할 때만** 깎는다 — 백사 1px과 분위수
    표본은 원래 폭 그대로(`cMid q0.34`·`cDeep q0.67` 불변). 홍해 밝기 143.6→**126.8** · 페르시아만
    165.1→**132.1** · **아라비아해 대조군 90.0→90.0**. 아홉 장 바다%는 소수점까지 그대로다.
  - **상단 — 회사가 흥하고 망한다.** 흥망 다섯(결산·배당 / 항차 손실 / 부실→인수 / 합병 / 재기) —
    문 닫은 상단 0→**3곳** · 임자 바뀐 상관 0→**10곳** · 선단6 49→45 · **상관이 빈 항구 0**.
    ★ **항구에 서 있는 동안 상단이 한 발짝도 안 움직이고 있었다** — `waitDays()`가 `decayGuildFlow`만 부르고
    `worldTick`은 안 불렀다(그 입구는 `map.js` 하나). 10일 대기에 자국 −22.4%·항차 **0회** → −5.0%·**+100회**.
    ⚠️ `guild.js` 주석은 *"입구를 하나로 뒀다"*라고 적고 있었다 — **문서가 코드와 어긋난 자리**.
    명부 40은 **이미 도달해 있었다**(같은 판정 경로로 재는 관측기를 새로 만드니 40/40 · 20일 순찰에 만날 확률
    75~83%). 대신 **죽은 사냥터 두 줄**(이어져 있지 않은 항구쌍)을 찾아 고쳤고 `check-world`가 전수로 잡는다.
    조선 근해 장사는 **게임이 이미 성립시킨다**(근해만 돌아도 30항차 금고가 세계 전체의 111%) — 막던 것은 러너다.
    NPC 배가 플레이어와 **같은 폴리라인**을 탄다(항적 꼬리까지). 대시보드 「상단」 탭 · `save.js`에 상단 상태 일곱.
  - **검사 474 → 476/476** · imports 901이름 0 · dup 0 · architecture · check-map 아홉 권역 개별 반려 0 ·
    스모크 0. ★ **회차 25의 「물가 −20.1%」가 지금 트리에서 재현되지 않는다**(회차 26 착수 시점 20시드에서
    30항차 **+7.7%** · 60항차 **+70.7%**). 사이에 바뀐 것은 **지도 좌표**다 — 같은 명령의 10항차 기준선이
    1,294 → 752닢으로, **같은 세계가 아니다.** → `wiki/economy-trade.md`

- **[2026-08-29] #engine #art #world — 회차 25, 「지도·항구·상단」 3트랙 병렬(PM 2층 · 에이전트 10+).**
  사용자 지시 넷으로 열렸다 — *"지도담당 PM이 실제로 웹 실행 후 보고 판단해서 알아서 고용"* ·
  *"항구내 디자인 PM 총괄이 백그라운드로 실측으로 보고 개선-확인 재개선 자동으로"* ·
  *"NPC 상단을 설계… 물가도 안정화돼서 주인공이 더 돈 벌기 힘들게"* ·
  *"PM들 역할 목표 하는일 문서로 잘 기록"*.
  ★ **PM이 2층이 됐다** — 상위 PM은 경계·검수·커밋·문서만 하고, 트랙 PM이 제 도메인을 눈으로 보고 실무자를 고용한다.
  조직 정본은 **`.playtest/round-25/WORK.md`**.

  ### 지도 (커밋 `d499718`)

  - **★★ 원인은 지형 코드가 아니라 방식이었다** — 아홉 장 모두 `auto`(도시·항로에서 해안선을 **역산**)로 그려져,
    항구를 잇는 데는 맞지만 **실제 해안선의 형태가 남지 않았다.** 이탈리아가 삼각형이 되고 일본이 흩어진 조각이 된 것이 그래서다.
    형상을 **`assets/map-shape/*.json`(해안선 다각형)에 손으로 적고 거기서 굽는** 방식으로 옮겼다. **여덟 장 완료.**
    뭍까지 평균 **6.5~9.6 → 2.3~2.9px** · 「6px 안에 뭍 0」 항구 **12~30곳 → 0~2곳** · 완료분 `check-map` **반려 0건**.
  - ★ **진단이 그 시작이었다** — `check-map`이 **9/9 통과인데 눈으로 보면 여덟이 세계지도가 아니었다.**
    사용자가 *"안 맞는 게 많아"*라고 한 것을 **검사기는 한 번도 못 잡고 있었다.**
  - **보스포루스를 열어** 에게해→마르마라→흑해가 이어지고, 대서양 아래를 **자로 그은 듯 가로지르던 수로**를
    지브롤터에서 비스듬히 내려가는 골로 바꿨다(최장직선 **30→14px**). 일본은 **혼슈가 활로 서고 시코쿠·사도**가 생겨
    「큰 섬 하나」에서 벗어났고, 내륙 도시 앞 **호수 60→0px**.
  - **★★ 지형이 아니라 좌표가 막고 있었다** — 조선 항구 bbox가 **가로 62 × 세로 44**(거의 정사각형)인데 실제 한반도는
    **세로:가로 ≈ 2.5:1**이다. 그래서 한반도가 「반도」가 아니라 「곶」으로 읽힌다.
    ⚠️ **좌표를 옮기면 권역 내 항해 일수가 바뀐다** — `state.js: distanceBetween`이 `Math.hypot`이고 **`days`를 직접 적는 것은 원양 항로뿐**이다.
    ⇒ **관문(부산포·염포·내이포·쓰시마) 고정 · 어느 구간도 +20% 초과 금지**를 조건으로 걸었고 거리 검증은 통과했다(전 구간 ±20% 이내 · 관문 0%).
  - **★★★ 한반도 — 반도를 지운 것은 지형이 아니라 색과 렌더였다**(커밋 `dcd766d`). 세 회차 동안 다각형을 고쳐도,
    **좌표까지 옮겼는데도** 「곶」이었다. 원인이 둘 다 다른 데 있었다 —
    ① **색**: `CLIMATE.temperate`의 지대 띠가 **위도만 봐서** 반도의 y0~29가 **만주와 같은 모래색**이었다(눈에는 혹만 남는다).
    ② **지형**: 다각형에 **21px로 있던 서한만이 PNG에는 한 픽셀도 없었다** — `coastOctaves`의 `push` 최대치가 **+14.1px**인데
    좁은 물길 가드는 **`seaThick<8`일 때만** 듣는다. ⇒ **폭 16~28px 물길은 통째로 메워진다**(가드 사각지대).
    **세로:평균폭 0.80:1 → 1.43:1**(가로가 더 길었다는 것이 그동안의 실상이다) · 반도 세로 36→**53px** · 강진 섬 항구 **1→0**.
    ⚠️ 목표 2.5:1은 **이 좌표계에서 불가능**하다(위도 9.55px/° 대 경도 12.2px/°로 **가로가 1.6배 늘어난 판** · 폭은
    군산창↔염포 46px이 못박는다). **실제 상한 ≈1.5:1**이고 지금이 그 **95%**다.
  - **규슈 : 혼슈 면적비 1.6 → 3.4:1** · 남미는 대륙을 가르던 곧은 지대 금을 없애고 지대를 3색으로.
    **아홉 바다 전부 ✅** — 뭍까지 평균 **2.1~2.9px** · `check-map` 아홉 권역 개별 **반려 0건**.
    카리브·남미는 **이미 완성돼 있었고 보고만 안 온 것**이었다.
  - **★ 「황해가 가장 어둡다」는 실측이 뒤집었다** — 상자별 평균밝기 황해 **82.2** / 동중국해 49.5 / 태평양 52.8로
    **일곱 중 셋째로 밝다.** 대륙붕 가중치를 4.5배로 올려 아홉 장을 다시 뽑아도 **2점만 움직인다**(문턱이 분위수라
    **원양의 33%는 늘 최심색**). ⇒ `scene.js`를 **파일 단위로 되돌렸다.** ★ 다시 뽑은 아홉 장 중 **일곱의 바이트가 같다** —
    되돌리기가 깨끗했다는 증거다.
  - **G-5 배선 완료** — `scenes/map.js`에 `|| guildFoeOnLeg(from,to)`. 상단의 **압박 함대가 이제 실제 조우 판정에 든다.**
    ⚠️ **위험 칸에 안 섞었다**(섞으면 「더 위험해졌다」로 잘못 읽힌다) — 툴팁에 *"조우 확률은 그대로다. 오는 상대만 바뀐다"*.

  ### 항구 (커밋 `522bb66` — 세 담당의 작업이 한 커밋에 들어 있다)

  - **★ S-5를 닫았다**(회차 24가 *"고치기 전에 배를 키우면 더 나빠진다"*로 멈춘 자리) —
    `scenes/port.js`가 `main.js`의 인셋을 안 써서 캔버스가 창 전체 폭으로 가운데 정렬되고 DOM 패널이 그 위에 얹혔다.
    그리드를 `minmax`로 바꾸고 **`placeStage()`가 배의 요구폭을 주장**하게 했다.
    **배 가림(px): 640 `76→0` · 800 `268→0` · 900 `168→0` · 1024 `88→0`** — 창 10가지 × 아홉 바다 **45조합 전부 0**.
  - **★ 실측이 두 번 결론을 뒤집었다** — ① 배의 요구폭은 176px이 아니라 **100~163px**(불투명 상자는 선종마다 다르고
    가운데 있지도 않다) ② **「가로 넘침 0」은 가독성의 보증이 아니다**(넘침 0인데 품목이 「말/라/게/타」 네 줄 ·
    무접힘 하한 452px, `.tight`로 360px).
  - **회차 24가 고친 결함이 360~480px에서 되살아나 있었다** — `#port-wrap` 넘침 `122→0` · 출항 단추 `hit` `false→true`.
  - 부수: 거점 카드 줄바꿈(왼쪽 글 <120px인 줄 `63→0`) · 사이드 640 항해 `3.45→2.83`화면 · 시장 표 머리 `39→24px` ·
    `fit()`이 상태바 접힘 뒤 다시 돌게(640 캔버스↔상자 `22→0px` — **0.92배 비정수 축소**가 픽셀을 뭉개고 있었다).
  - **한 번 욕심냈다가 실측이 되돌렸다** — 무리까지 요구폭에 넣으니 1440에서 패널이 452→368로 쪼그라들었다. ≥1024px는 회차 24 그대로.

  - **항구 배경 아트**(같은 커밋) — **랜드마크 실루엣 역전을 13화풍에서 해소**(★ **dome 0.60→1.47배** — 돔이 같은 화풍
    건물의 **중앙값보다도 낮았다**) · `BSCALE` **1.55→1.40** · **19:1 굴뚝 3곳 제거** · 단색 벽에 층띠(**새 색 0개**).
    아홉 권역 before/after 렌더로 대조했다.
  - **조선소·여관**(같은 커밋) — 640에서 **조선소 본문이 20px**이라 내용이 한 줄도 안 떴다 → **130px** ·
    술집 `83→166px` · **클릭 경고 `41→0`** · 화면의 **「공업력 Infinity」**도 잡았다 · 공용 `.ctr-sub` 대비 `4.40→4.65:1`.

  - **★★ J-2 판정 — 「자가 둘이고 2.9배 어긋난다」는 어긋난 게 아니라 원근이다.**
    실측: **부두 사람 `0.0708 m/px`**(24px=1.7m) vs **정박선 `0.204 m/px`**(카라벨 108px≈22m).
    **「자가 하나」라는 가정이 틀렸다** — 2D 배경은 **깊이 평면마다 자가 다르고 그게 원근이다.**
    반대 해석(*"배가 너무 작다"*)은 **실행 불가능**해서 기각했다(배를 2.9배 키우려면 1280px 미만 창을 버려야 하고,
    2배조차 640·1024에서 안 들어간다). ⇒ 정본을 **평면별로** 고쳐 `QUICKMAP-art.md` §3에 박았고,
    **J-1(배를 2배로)은 하지 않는다**(원근이 정본이면 지금이 맞고 키우면 오히려 깨진다).
    ★ 이 착각으로 **회차 23·24가 두 번 「층고가 안 맞는다」며 건물을 키웠고 회차 25가 다시 낮췄다.**
  - **J-4 정정(P6)** — 회차 24 문서 두 문장이 실측과 어긋났다: 「앞열 90px」 → 300채 격리 렌더 **111~115px**
    (`hMax` 공식값 ≠ 실제 그려지는 높이) · 「랜드마크 역전 없다」 → 실측 **dome 0.60배**.

  ### NPC 상단 (커밋 `137b0dc`)

  **★★ 물가 안정화가 원리적으로 불가능한 구조였다** — `state.impact`가 **부호 없는 스칼라**라(사도 오르고 팔아도 오른다)
  NPC를 아무리 늘려도 산지는 계속 싸고, `NPC.pressure`를 올리는 것은 물가 안정이 아니라 **전면 벌금**이다.
  ⇒ **부호 있는 항** `guildFlow`를 새로 곱했다(`impact`는 안 건드렸다). **상세는 아래 회차 25 상단 항목이 정본.**

  ### 함정·문서

  - **`gotchas.md`에 둘을 올렸다가 도메인으로 다시 내렸다** — **#9 반쯤 쓴 파일 하나가 남의 트랙을 통째로 세운다**
    (새 모듈이 없는 파일을 import한 구간에 `test-rules`가 죽고 실화면이 `__game is not defined` · **두 트랙이 각각 겪었다**) ·
    **#10 CDP로 붙은 창에 `close()`를 부르면 남의 창이 죽는다**(측정 스크립트는 `process.exit(0)`로).
  - **세션필독 다이어트** — `gotchas.md` **5,196 → 3,702B**(#1 `bake` 캐시 → art §3 · #10 → engine §3 ·
    #6 판별·사례 → `dev-workflow.md`) · `claude-memory.md` **15,787 → 15,327B**(H-1 줄을 결론+포인터로).
    **`check-memory-routing` 규칙 통과 · dead pointer 0.**
  - ⚠️ **측정 함정 하나** — 바이트를 잴 때 `wc -c`는 CRLF를, **JS `s.length`는 한글 한 자를 1로** 센다.
    상위 PM이 후자로 재서 *"전부 상한 이하"*로 오판할 뻔했다. **판정은 `check-memory-routing.mjs` 하나로.**

- **[2026-08-29] #world #trade — 회차 25, NPC **상단(商團)** 층을 새로 넣었다(에이전트 3 병렬).**
  사용자 지시: *"npc 상단을 설계 — 실제 상단처럼 무역로로 부를 축적하고 그 부로 주인공을 괴롭히고
  때로는 경쟁하려고 이용하고 돕기도 하고, **그래서 물가가 안정화돼 주인공이 돈 벌기 더 어려워진다**"*.
  검사 **453 → 474/474** · 작업판 `.playtest/round-25/`(NPC-DESIGN·PROGRESS·ISSUES·BASELINE·AFTER).
  **커밋은 상위 PM이 한다.**

  - **★★★ 「NPC를 늘리면 물가가 안정화된다」가 거짓이었다 — `impact`에는 부호가 없다.**
    `state.js`가 그렇게 적어 두었다(*"방향과 무관하게 항상 불리한 쪽으로 작동한다"*). 사도 늘고
    팔아도 늘 뿐이라 **아무리 날라도 산지는 계속 싸고 수요지는 계속 비싸다** — `NPC.pressure`를
    올리는 것은 물가 안정이 아니라 **전면 벌금**이다. ⇒ **부호 있는 항**을 따로 팠다:
    `state.guildFlow`(+부었다 / −사갔다) + `guildFactor()`가 `shockFactor` 옆에서 곱해진다.
    **`impact`는 한 줄도 안 건드렸다**(그 규칙은 지금도 옳다).
  - **상단은 배가 아니라 회사다.** 명부 **49곳**(아홉 바다 · `js/npc/houses-euro/-asia/-newworld.js`)이
    자본을 들고 여러 항차를 굴리되 **지도에 점으로 안 뜬다** — `state.npcs` 정원을 한 척도 안 건드려
    해적 밀도·조우 확률이 안 움직인다(*"밀도를 올려서 풀지 마라"*). 자본의 근거는
    `content/houses-evidence.json`(선원 연봉 환산)이고 게임 자본은 **로그 압축**한다(`GUILD.capPow`) —
    사료값을 그대로 쓰면 세계가 한 상단의 것이 된다(실측 편차 17.4배로 눌린다).
  - **세력과 겹치지 않는다** — 세력은 *정치·독점*(`gripMarkup`·`rollPoach`), 상단은 *자본과 물류*다.
    `test-rules`가 **소스로** 확인한다(상단 코드에 그 두 이름이 없어야 통과).
  - **★ 실측이 설계를 두 번 갈아엎었다.** ① `flowK 0.85`는 **한 항차로 상한을 네 배 넘겨** 상단을
    **±14% 난수 발생기**로 만들었다 — 가격차가 **+21.3%**(좁아지기는커녕 벌어졌다), 주인공 금고 **−49%**.
    ② 고친 뒤 2시드에서 −15.6%를 보고 끝낼 뻔했는데 **20시드에서 +53%로 뒤집혔다** — 잡음이 아니라
    상단이 **세계의 얇은 조각(전문 품목 넷)**만 만지고 있었던 것이다(최적 플레이는 수백 후보의
    **최댓값**을 고르므로 손 안 댄 큰 차익이 남으면 그쪽을 집는다). ⇒ 상관이 실제로 거래하는
    물건 전부를 후보에(`goodsCap`·`offSpecialty`). 그 밖에 **위험 회피가 규칙에 없었고**
    (`routeRisk`는 두세 칸 건너에 안 먹는다 → 길 위 요율을 더한다), **세 갈래 중 하나만 돌고 있었다**
    (호감이 전부 0 → `temper`가 첫인상, **악명이 상단을 돌려세운다**).
  - **★ 확정 실측**(`node tools/sim-guild.mjs 20` · 같은 시드로 짝지어 상단 껐다 켠다):
    **상단의 무역로 가격차 −17.1%**(요율 낮은 −23.2% ↔ **요율 상위 30%는 −6.3%만** —
    위험 프리미엄이 남는다) · 품목별 세계 가격 퍼짐 −2.1% · **사건(shock) 창 ±0.0%**(큰 기회는 그대로) ·
    **항차이익 −16.2%**(앞 30항차 **−25.0%**) · 30항차 금고 **−20.1%** · **완주 20/20 유지**.
    10항차은 **+0.0%**이고 그것이 합격이다 — 압력은 **부가 쌓이면서** 온다.
  - ⚠️ **항차 ROI(비율)로는 이 변화를 못 잰다** — 차익이 줄면 최적 플레이가 **덜 싣는 쪽**으로 옮겨 가고
    덜 실으면 `MARKET.impact` 벌점도 줄어 **부호가 뒤집힌다**(금고 −20%에 ROI +1.65%p).
    지표는 **항차이익(닢)**이다. ★ 그리고 **짝 없는 `sim-stat`으로는 아예 안 보인다**(시드 퍼짐 6~58배).
  - **세 갈래가 실제로 돈다**(`node tools/probe-guild.mjs`) — 첫인상 괴롭힘 5 · 중립 38 · 도움 6,
    판당 괴롭힘 5.7 · 사주 12.0(치른 것 11.3) · 도움 9.0. **항해일지에도 실제로 뜬다**(600일에 50줄) —
    습격 소문이 상한을 다 먹어 상단 줄이 한 번도 안 뜰 뻔한 것을 잡아 **한 칸을 남겼다**.
  - **남긴 것**: 압박 함대는 규칙으로 서 있으나 **씬이 `guildFoeOnLeg()`를 안 읽는다**
    (`js/scenes/map.js`는 지도 PM 소유). `pirateEnemy()`가 그대로 먹는 모양으로 맞춰 두었으므로
    **한 마디**면 붙는다 → `.playtest/round-25/NPC-ISSUES.md` §D(G-5).
  - 새 파일: `js/npc/houses*.js`(4) · `js/npc/guild.js` · `content/houses-evidence.json` ·
    `tools/sim-guild.mjs`·`probe-guild.mjs`·`check-houses.mjs`. 전부 `dashboard/architecture.mjs`에 등재.

- **[2026-08-29] #engine #trade #art #combat — 회차 24, 「디자인·게임·실제테스트」 3트랙 병렬(에이전트 5).**
  사용자 지시: *"디자인개선 게임개선 실제테스트 3가지 진행"*. 커밋 다섯
  `679cee0`·`5b80d50`·`6b4870c`·`8de4df3`·`ce70386` · 검사 **453/453 유지**(규칙은 안 건드렸다) ·
  작업판 **`.playtest/round-24/WORK.md`**(이 회차 사실의 정본).

  - **★★★ 러너가 「더 약한 배」로 스스로 갈아타고 있었다.** 인도양·중동·아프리카에서 두목(등급5)에
    연패한 원인이다. 로그의 기함 이력이 `거북선(선체 285·포 15)` → `간자(205·12)`였다 — **선체 −28%·화력 −20%**.
    뿌리는 `js/state.js: stowFlagship()`이 **갈아탈 때만** 지금 기함을 `state.fleet`에 적는 것:
    러너의 갈아타기 판정이 `fleet`에서 최고 hp를 고르는데 **활성 기함이 후보에서 빠진다.**
    ⇒ 게임은 `k !== state.shipKey`로 일관되게 거르므로 **게임 결함이 아니라 러너가 규약을 잘못 읽은 것**이다.
    고치고(활성 기함을 후보에 명시) `[FLEET-DOWNGRADE]` 감시를 달았다 — `state.shipKey`를 직접 찍어
    옛/새 로직을 대조 검증(`wouldSwapOld:true` vs `wouldSwapNew:false`).
  - **★★★ 그리고 그것이 전부였음을 실험이 확정했다 — 게임은 멀쩡하다.**
    같은 두목(인도양 등급5 「포르투갈 인도 함대 기함」 hp360·포30·선원130)에 **기함만 바꿔** 3조건 × 3회를
    붙였다(동행 0척으로 통제 · 전투를 직접 열어 조우 RNG 제거 · 매 회 `state`에 물어 기함과 적 수치를 확인):

    | 조건 | 기함 | 결과 | 자취 |
    |---|---|---|---|
    | **A** | 간자 205·포12·선원64 | **0/3 패배** | `205→51` · 포격 3발 뒤 **백병으로 끌려가** 선원 64 대 130으로 진다 |
    | **B** | **거북선 285·포15·선원150** | **★ 3/3 승리** | 포격 7~8발 **완봉(백병 0라운드)** · 손실 23~48% |
    | **C** | 철갑 거북선 420·포22·선원190 | 3/3 승리 | 3회 중 **2회 무피해**(420→420) |

    ⇒ **선체 80(205→285)이 3전 3패를 3전 3승으로 뒤집었다.** 갈림길은 tier4가 아니라 **포격전을 버티느냐**다 —
    205는 3발에 51로 깎여 백병에 끌려가고, 285·선원150은 포격만으로 끝낸다.
    **철갑 거북선 원정까지 갈 필요도 없었다.** 곧 「권역마다 배 천장이 다른데 두목은 다 등급5다」는
    **이 패배의 원인이 아니었다** — 러너가 자진 강등만 안 했으면 이겼다.
  - **★★ 그 강등에 `최고선 ✓` PASS가 찍혀 있었다** — `기함 교체 (ok)`·`H-S2 PASS`가 나란히 찍혀 결함이
    성공 로그 뒤에 숨었다. 회차 23의 「성공을 찍는 로그가 자기를 감춘다」가 **판정 함수 쪽에서** 재발한 것이다.
  - **★★★ H-1(눌림 판정)을 심었고, 그것이 실제로 잡았다.** Playwright `.click()`은 `scrollIntoView`로
    눌러 주므로 **사람은 못 누르는데 자동검증은 통과한다.** `g.start()`에 **H-1a(치명 — `scrollTop`을
    0→max 훑어도 `elementFromPoint`가 한 번도 그 노드를 안 돌려줌) / H-1b(경고 — 어딘가에선 닿음)**
    두 등급을 넣었다. 두 등급으로 가른 이유는 **`position:sticky`가 페인트 시점 효과라 스크롤 계산에
    안 잡히기 때문**(sticky 형제가 덮고 있어도 `scrollIntoView`가 스크롤을 안 시켜 거짓 양성이 난다).
    감지기를 심고 **자기 검사부터 검증**했고(단추를 `top:5000px`로 밀어 잡히는지), 실제로 640×360 항구의
    「⚓ 출항하기」를 `F-7`로 잡았다.
  - **★★ 그 F-7의 원인 — 항구 그리드가 최소 792px을 요구했다.** `#port-wrap`이 고정 px 세 칸
    (`452px 1fr 292px`)이라 **640px 창에 애초에 안 들어갔다.** 「⚓ 출항하기」가 `rect.right 728 > innerWidth 640`
    — **그 화면에서 나가는 유일한 길**이 안 눌렸다. 회차 24 이전부터 있던 결함이다(`4556957`에서 확인).
    `@media (max-width:760px)`로 `320px 1fr 160px`. 고침 전후를 같은 스크립트로 대조 확인
    (`4556957` → `inVp:false·hit:false` / `8de4df3` → 둘 다 `true`).
  - **★ 항구 사이드패널을 탭 넷으로 갈랐다**(항해·거래·목표·사람). 640×360에서 `scrollHeight` **1,346px(5.5화면)
    → 기본 탭 514px(2.1화면)**, 1280×720에서는 **딱 1.0화면**. 두 회차째 잘라내기로 안 줄던 이유는
    큰 카드 넷(조선소 227·부두 144·정비 289·상관 197px)이 **접지 않는 카드**여서였다.
    탈출구(`salvageCard`)와 「⚓ 출항하기」는 탭 밖에 고정.
  - **★ 「⚓ 출항하기」의 sticky를 걷었다** — 스크롤되는 `#port-side` 안의 `position:sticky`라
    첫 진입(`scrollTop=0`)에서 「전부 수리」를 덮어 오조작이 났다. `#port-side`를 두 줄 플렉스로 갈라
    (`#port-side-scroll` + `.btn-sail`) 제 칸을 줬다. `scrollTop` 0→331을 10px씩 훑어 **모든 위치에서 닿음** 확인.
  - **★★ 한반도 엔딩 `docks 0/9` — 문턱은 정상이었다. 값을 한 줄도 안 깎았다.**
    회차 23의 「전리품만 팔면 항구당 183~513닢 vs 문턱 2,000닢」을 그대로 읽으면 문턱을 내려야 한다.
    깎기 전에 「조선 아홉 항구에서 팔 만한 짐이 있는가」를 `tools/probe-joseon-civic.mjs`(신설)로 쟀다 —
    **「기회주의 상인」(`only=world`) 250항차·3,918일 → `civicOf>0` 0/9** vs
    **「고향 바다 상인」(`only=homeloop` — 조선 9항구 + 쓰시마·하카타·등주) 700항차·1,542일 → 9/9 전부.**
    세계를 떠도는 쪽이 0/9인 이유는 문턱이 아니라 **일본·명 마진이 늘 더 커서 조선으로 안 돌아오는 것**이었다.
    ⇒ `CIVIC.dues`·`TARIFF_OVERRIDE`·`MARKET.depthPerSize` 그대로. 막은 것은 **러너에 조선 근해 장사 페이즈가 없는 것**이다.
  - **★ 항구 배경 건물의 축척을 키웠다** — 회차 23이 부두 사람을 21px로 줄이자 드러난 자리다.
    사람 실질 키 **24px = 1.7m**(0.0708 m/px) 기준: 앞열 건물 **58px(4.1m) → 90px(6.4m)** · 상한 64→108px ·
    창 세로 간격 5~6 → **12~14px**(착시 층높이 0.4~0.6m → **1.4m대**). **랜드마크 6종도 함께** 키웠다
    (안 키우면 새 건물이 종탑보다 커져 실루엣이 뒤집힌다). 13양식 전후 대조 · 4배 확대 검수 · 아홉 바다 실화면 확인.
  - **★ 여성 차림을 세 계열로 갈랐다** — 유럽식 보디스 하나뿐이라 아체의 락사마나가 코르셋·깃털 모자로 나왔다.
    `corset`(지중해·대서양·카리브·남미) / `wrap`(중동·아프리카·인도) / `sash`(동남아·동아시아),
    tier4의 「인상적인 모자」도 계열별. `scenes`가 회차 23부터 `sex`를 넘기고 있어 **배선 없이** 붙었다.
  - **정정 — §D 「시뮬이 10항차에서 멈춘다」는 이미 해소돼 있었다**(`sim-trade` 90항차 완주 ·
    `sim-stat 20` ×4회 20/20). **낡은 문서였을 뿐**이다.
  - **오검출 둘을 실측으로 기각했다** — 완주 러너가 640×360에서 올린 「급여일 모달 소프트락」·
    「조선소 탭이 항해일지 푸터에 가려짐」. ① 모달은 바깥 `.modal`(`overflow-y:visible`)이 아니라
    **안쪽 `.modal-box`가 스크롤 컨테이너**다(`max-height:80vh·overflow:auto` · 선재 규칙).
    ② 조선소 탭 `[257,288]` vs `#logbar` `[325,360]` = **37px 여유** — `fit()`이 `#stage.clientHeight`로
    재는데 그 값은 **이미 hud·logbar를 뺀 높이**라 구조적으로 못 겹친다. PM이 **메인 트리와 워크트리
    스냅샷 양쪽에서 같은 스크립트로** 대조해 확정했다 — **커밋 차이가 아니라 「무엇을 재고 있는가」의 문제**였다.
  - **★ 회차 24의 한 줄 — 결론이 네 번 물렸고, 매번 실측이 이겼다.**
    두목전 패배의 원인 추정이 `HUNTMIN 예산 소진` → `권역 tier 천장`(반만 맞음) → **`prepareFleet()` 강등**(확정)으로
    두 번 바뀌었고, 640 발견 둘은 오검출로 정정됐다. ⇒ **「무엇을 재고 있는가」를 먼저 확인한다** —
    스크롤 판정은 대상에서 `parentElement`를 타고 올라가 **실제 스크롤 컨테이너**를 찾아야 하고,
    클래스 이름으로 짐작하지 않는다.
  - 남은 것: **고친 갈아타기 로직으로 완주 재실행**(이번 판은 고치기 전 로직으로 돌았다) ·
    **집 바다 공사 완공 전에 떠나는 러너 정책**(염포 303일·부산포 525일 완공인데 165일차에 원정 —
    **철갑 거북선을 애초에 지을 수 없는 판**이었다) · 조선 근해 장사 페이즈 · S-5(항구 인셋).
