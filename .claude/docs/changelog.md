# changelog — tradeship

> 변경 이력 정본(lazy·거의 안 읽음). `claude-memory.md`에는 **현재 상태만** 두고,
> "무엇을 어떻게 고쳤다"는 여기에 쌓는다.
> 상한 40KB — 넘으면 시점 경계로 분할한다. 과거분:
> [changelog-2026-08-16.md](changelog-2026-08-16.md)(08-16 하루) ·
> [changelog-2026-08-15.md](changelog-2026-08-15.md)(08-15 하루) ·
> [changelog-2026-08-early.md](changelog-2026-08-early.md)(08-08 ~ 08-15 앞부분).

## 2026-08

- **[2026-08-21] #story #docs** — **"소설 확인" — 문서 셋이 제 결정을 못 읽고 있었다.**
  하루 만에 다시 열어 보니 세 자리가 낡아 있었다. 어제의 결론이 문서에 안 돌아온 것이다.
  - **VERDICT §3-2 "사람이 결정할 것 셋" 중 둘은 이미 결정돼 있었다.** 파일 수정 시각이 증거다 —
    VERDICT가 23:16이고 `SYNOPSIS`·`ENDING`·`GAME-LINK`가 23:21~23:22이다. 라플라타 5부 선두 배치는
    시놉시스에, 철갑 거북선 tier 4(거북선은 안 건드린다)는 §8 A-2에 확정 반영돼 있었다.
    남은 미결은 **A-1b 하나**이고 그것도 소설이 아니라 게임 구현의 미결이다.
  - ★ **그 A-1b를 코드로 확인하니 문서가 말한 것보다 컸다.** `advanceDays()`는 **`js/scenes/map.js:318`
    한 곳에서만** 불린다 — 게임은 "정박이 공짜"인 게 아니라 **항구에 시간이 아예 없다.**
    비용 한 줄이 아니라 시간 모델 신설이다. 반대로 배관은 절반 깔려 있다(발생주의 급여 + 월 정산 + 체불·이탈).
    없는 것은 **항구에서 날을 흘리는 입구 하나**뿐. → `QUICKMAP-trade.md` §3에 함정으로 박았다.
  - **TIMELINE §1이 교차 판정의 정정을 하나도 안 싣고 있었다.** 표가 초안 시점 값 그대로라
    그것을 보고 쓰면 종 24,000(→중고 12,480)·샤벡 5,200(→3,000)·"속도를 화물칸으로"(→거꾸로였다)를
    그대로 쓰게 된다. **여섯 자리에 `→ 정정`**을 박고, 5부 머리에 라플라타 재배치 주석을,
    6부 진수 행을 **염포·철갑 거북선**으로 고쳤다(초안은 부산포·거북선이었다).
  - **OUTLINE 27~61장 재작성.** 40장 구판의 3부 「아홉 바다」 14장을 버리고 확장판 3~6부 35장으로
    다시 짰다 — **총 61장·36만 자**(3부 8 · 4부 13 · 5부 8 · 6부 6). 각 장에 게임 규칙 한 줄(▷)과
    TIMELINE D번호를 붙였다. 집필 순서는 **각 부 결말 일곱 장 먼저** — 그 일곱만으로 축약본이 된다.
  - **`story/FLEET-LEDGER.md` 신설(D7 처리).** 배를 다섯 번 잃고 여섯 번 사는데 장부가 없었다.
    척수 계단 `1→2→0→5→0→1→0→1→3` · 선원 계단 · 이름 여섯 칸 · 거점 여덟 줄.
    ★ **0이 세 번 나오고 그중 하나가 900일 이어진다**는 것은 이 표에서만 보인다.
    선원을 잃는 이유가 다섯 번 다 다르고(돈·더 나은 삯·병·몰수·**원칙**) 마지막 하나가 결론이라는 것도.
  - 어제 만든 `story/` 24문서가 **커밋 없이 하루를 났다** — 먼저 올리고 작업했다(`3e2eb1d`·`9259a2d`).
  - 메모리: `claude-memory.md`의 story 항목에서 문서 열거를 걷어냈다(QUICKMAP story 행·`story/README.md`와
    **포인터 중복**이었다). 15,348B로 상한(15KB) 아래에 두었으나 **헤드룸이 12B뿐**이라 다음 편집은 감사부터 돈다.

- **[2026-08-18] #art #docs #tooling** — **의뢰서가 AI에게 비율을 말해 주지 않고 있었다. 검수 기준도 낡아 있었다.**
  3차 반려(AI 생성)의 진짜 첫 관문은 그림 실력이 아니라 **규격**이었는데, 정작 작업자가 받는
  바다별 의뢰서엔 비율·해상도 얘기가 한 줄도 없었다. 대표 문서 §4-B의 "정사각형은 어떻게 잘라도
  안 된다" 한 줄이 전부였다.
  - **§1-A 신설(바다별 9장)** — 16:9 고정 · 쓸 수 있는 크기는 정수배 셋(800×450·1200×675·**1600×900**)뿐 ·
    흔한 AI 프리셋이 왜 안 되나(1024×576은 2.56배·1344×768은 3.36배) · 정사각형을 살리려 시험한
    다섯 경로 표 · **text2img 금지, `-x4.png`로 img2img**(denoise 0.25~0.35) · 최근접 이웃 축소.
  - **§6 검수를 실제 검수기와 맞췄다** — "반경 6px에 바다와 뭍" 넷만 적혀 있어 **F-2에서 추가한
    거리 검사 둘(28px 초과 실패 · 12px 초과가 절반이면 실패)이 빠져 있었다.** 3차가 통과한 그 구멍이
    의뢰서에 그대로 남아 있던 셈이다. 정본을 `check-map.py`로 명시하고 "세 번 중 두 번이 기계 검수를
    통과했다"는 경고를 함께 넣었다.
  - 나머지 셋 — §0을 **3차 반려까지**로(2차에서 멈춰 있었다) · 기준판을 **넉 장 표**(`-x4` 덧그릴 판 ·
    `-coast` 지킬 실루엣)로 · 색 수 **64색 이하 → 256색 이하**(기준판 자체가 130~257색이라 자기모순이었다) ·
    §2-A(그리지 말 것)를 바다별 의뢰서에도(AI가 글자·나침도를 스스로 넣는다).
  - **`mapcheck.html`에도 같은 거리 검사를 넣었다.** 의뢰서가 "같은 기준"이라 말하는데 브라우저 판정만
    옛 기준이었다. 실제로 띄워 확인 — `뭍까지 평균 6.3px · 좌표계 일치 (12px 초과 2/40)`.
  - **평균 8.5px → 6.4px** — 항구가 264곳으로 늘며 내려왔다(권역별 5.1~7.3px). 아홉 장을 다시 재
    의뢰서·`check-map.py` 주석을 고쳤다. changelog의 8.5px은 3차 반려 당시 실측이라 그대로 둔다.
  - ★ **`WORLD-MAP-BRIEF.md`는 제목만의 문제가 아니었다** — 본문 전체가 **도시 16곳 시절의 지중해 한 장**
    의뢰서였다(§5가 "이 16점이 설계도"). 08-17에 고친 것은 §0 한 줄뿐이었다. `world-map.webp`는
    **게임 어디서도 참조하지 않는다**(전체 grep 0건). 지우지 않고 ⛔ 보관용 배너를 달고,
    `BRIEF-MAP.md` §1의 "세계 지도 ✅"를 **"발주된 적 없음"**으로 정정했다.
  - ⚠️ **`tools/enhance_map_texture.py`(untracked)가 지도 아홉 장을 덮어 놓았다 — 미결**(`UNIMPLEMENTED.md` F-8).
    주석은 "Perlin-like"인데 `sin(x·0.15)·cos(y·0.15)`라 **육지가 41.9px·17.9px 격자로 반복**된다
    (세로 42px 자기상관 **0.688**). 사하라와 사바나의 남북 구분도 사라졌다(`CLIMATE[].zones`를 안 읽는다).
    `check-map.py`는 **9/9 통과**시킨다 — 기계 검수가 최소조건이라는 세 번째 사례다.
  - 한때 바다별 프롬프트도 생성하게 만들었다가 **되돌렸다** — 의뢰서에 이미 있는 것을 아홉 벌 더
    만드는 일이었다. 프롬프트는 파일이 아니라 그때그때 의뢰서에서 뽑아 준다.

- **[2026-08-17] #content #world #tooling** — ★ **아홉 바다를 채웠다: 항구 175 → 264곳, 항로 254 → 421개, 교역품 78 → 83종.**
  권역이 서로 import하지 않는 구조라 **여덟 권역을 병렬로** 늘렸다(지중해 28곳이 기준이었다).
  | 권역 | 항구 | 새 항로 | 비고 |
  |---|---|---|---|
  | 동아시아 | 18 → **40** | +35 | 사용자가 "너무 작다"고 지적 → 명 8(상하이·난징·등주·톈진·원저우·차오저우·펑후·경주) · 조선 6(의주·군산창·강진·제주·내이포·염포) · 일본 7(쓰시마·아카마가세키·오노미치·오사카·쓰루가·사도·가고시마) · 대만 1 |
  | 대서양·북해 | 20 → 28 | +21 | 포르투·카디스·라스팔마스·낭트·플리머스·헐·리스·비스뷔 |
  | 인도양 | 20 → 28 | +18 | 새 품목 **코이어**(야자 밧줄 — 못이 아니라 밧줄로 판자를 꿴 바다라 조선대가 사간다) |
  | 아프리카 | 17 → 28 | +19 | 새 품목 0 — 남의 바다 물건이 팔릴 자리를 만들었다(발트 밀랍 → 카슈에우, 빌바오 철 → 기니 6항, 몰디브 카우리 → 위다에서 화폐) |
  | 중동·홍해 | 18 → 28 | +20 | 홍해 북단(수에즈·엘토르·쿠세이르) · **사파비 깃발이 처음 물가에**(곰브룬·반다르콩) |
  | 동남아 | 18 → 28 | +19 | 새 품목 **안식향** · 마카사르~반다 직항(9.0) 대신 부톤 경유(6.5+8.5)가 열려 "빠른 길 vs 안전한 길"이 생겼다 |
  | 카리브 | 18 → 28 | +18 | 새 품목 **사르사파리야** · 세관 없는 항구가 2 → 5곳이 되어 **밀무역이 예외가 아니게** 됐다 |
  | 남아메리카 | 18 → 28 | +15 | 새 품목 **원면·우지·육포**(우지는 광산 양초로, 육포는 미타 배급으로 팔린다) |
  - **부산포 시작이 넓어졌다** — 이웃이 2곳(마포·하카타)에서 **5곳**(+내이포·염포·쓰시마)이 되고 **29닢짜리 내해 항로**가 생겼다.
  - **경제는 버텼다**(`sim-stat 20`): 완주 20/20 · 체불 0% · 첫 배 10항차. 초반이 살짝 눌리고
    후반이 벌어졌다 — 10항차 876닢(이전 1,072) · 30항차 **10,596닢**(이전 7,497).
  - ★ **검증기 둘이 콘텐츠를 막고 있었다 — 최상위 원칙 위반이라 고쳤다.**
    `check-routes`는 **근거없음**으로, `check-evidence`는 **관세 무근거**로 exit 1을 냈다. 그러면
    "항구를 늘리려면 그 항구의 사료를 먼저 찾아야" 한다. 실패로 남는 것은 *코드와 근거가 어긋난 것*
    (불일치·유령·요율없음·배선)뿐이고, 미조사는 **경고**로 내렸다. 실제로 에이전트 하나가 이 벽 때문에
    지시 범위를 넘어 근거 파일을 건드려야 했다.
  - `check-map.py`도 고쳤다 — **내해·육로 항로(`ROUTE_RISK: null`)를 관통으로 세던 오탐**을 없앴다
    (부르사~이즈니크가 오래 그렇게 걸려 있었다). 지형 손질 3건: 제주 섬이 반경 6px을 다 채워
    "물이 안 닿는 항구"가 된 것을 4×3으로 줄이고, 등주~의주를 육로(`null`)로, 코펜하겐~스톡홀름
    직항은 스칸디나비아를 관통해 **고틀란드 경유**로 바꿨다(근거 JSON도 같은 커밋에서 이관).
  - 검수: 지도 9장 재생성 후 **9/9 통과** · `check-world` 264/264 도달 · 나머지 검증 전부 통과.

- **[2026-08-17] #ui #start #docs** — **아홉 바다에서 골라 시작한다(기본 부산포). 의뢰서 다섯 장을 실제와 맞췄다.**
  - 타이틀에 **3열 아홉 칸**이 뜨고, 고르면 `resetGame(at)`이 판을 그 부두에서 다시 만든다
    (시작 조건이 한 곳에만 있으므로 타이틀 문구·항구·선단 기록이 함께 따라온다).
    자리는 실클릭으로 고른 아홉 곳이다 — 막다른 주머니(마포·사카이·카이로·이스파한·세부)를 피했다.
  - **기본은 부산포**(`DEFAULT_START`). 조선은 사무역을 금해 열린 부두가 하나뿐이고 항로가
    마포·하카타 둘뿐인데, **그 폐쇄성이 곧 이 시작지의 성격**이다. `?start=`(개발용)는 그대로 이기고
    그때는 고르는 줄을 띄우지 않는다. → `UNIMPLEMENTED.md` A-3(인물·조건은 아직 하나라 `절반`)
  - 확인: 카리브를 고르면 시장이 소가죽·로그우드·설탕으로 바뀌고 NPC 40이 다시 깔린다.
    리로드하면 부산포로 돌아온다. **스모크도 부산포에서 완주**한다(마포 항해 → 매각 1,007닢).
  - **의뢰서 정합** — `PORT-BACKGROUND-BRIEF.md`가 아직 "**지중해** 교역기 · 항구 16곳"이었다(항구는 175곳,
    6장 적용). `BRIEF-NPC.md`는 "그림은 지중해 것 하나"였는데 선원 한 벌·초상 둘이 이미 붙어 있었다.
    `WORLD-MAP-BRIEF.md`는 "도시 16곳·항로 28개"(실제 175·254). 셋을 실제와 맞추고
    생성물(`map-briefs/`·`npc-briefs/`)을 다시 뽑았다.

- **[2026-08-17] #art #map** — ★ **AI 지도를 쓰는 다섯 경로를 전부 시험했고, 축척이 벽이었다.**
  "화면에 맞추면 되지 않나"는 물음에서 시작해 사용자가 짚은 **레터박스**까지 다 해봤다:
  ① 16:9 크롭 → 동지중해가 화면 밖 ② 세로 압축(3.73배) → 이탈리아 반도·아드리아해·에게해 소멸
  ③ **레터박스 → 왜곡 0으로 지형이 온전했다**(274×225 + 좌우 63px 바다색). 여기까지는 성공이다
  ④ 항구 좌표를 그림에 재배치 → 28곳 중 **26곳이 의미대로 앉았다**(아드리아에 베네치아·나일에 알렉산드리아).
  그런데 **항로 52개 중 18개가 반도를 관통**했고(제노바~나폴리 100%·살로니카~이스탄불 93%),
  경유 재설계로도 **이스탄불이 어느 도시로도 안 닿았다** ⑤ 육지 침식 → 3px에 관통 4개로 줄지만
  육지가 9.6%가 되어 바다 90%(검수 상한 80%).
  - ⇒ **원인은 그림이 아니라 축척이다.** 400×225에서 1px ≈ 10~15km이고 연안 항로는 5~20km라
    1~2px로 뭉개진다 — 사실적 해안선과 직선 항로 판정은 이 해상도에서 양립하지 않는다.
    **게임 지도가 도식인 이유가 이것이다.** `QUICKMAP-art.md` §3에 재론 금지로 적었다.
  - ⇒ 성립하는 유일한 의뢰는 **덧그리기**다. `tools/gen-map-tracing.mjs`를 만들어 권역마다
    **4배 확대판**과 **해안선만 남긴 판**을 뽑는다(9권역 × 2장). art §3이 "빠진 것은 좌표를 이미
    만족하는 실루엣 기준판"이라 지적한 자산이고, 의뢰서 §2·§4-B에 배선했다.
  - 부수 정정: `이스탄불~부르사`·`부르사~이즈니크`는 **이미 내해·육로**(`ROUTE_RISK: null`)라
    육지를 지나는 것이 정상인데 검수기가 관통으로 셌다 — 재설계 대상 21개가 실제로는 18개였다.

- **[2026-08-17] #art #map #tooling** — ★ **"검수 통과"가 거짓이었다 — 3차 납품도 반려다(되돌렸다).**
  - 후처리한 지중해 판을 넣고 `check-map.py`가 **통과**라고 해서 적용했는데, 사용자가 화면을 보고
    "항구 위치가 안 맞는다"고 했다. 도시 좌표를 그림 위에 찍어 보니 **베네치아·바르셀로나·발렌시아·
    나폴리·아테네·알렉산드리아가 바다 한가운데 떠 있었다.** 코드가 그린 판에서는 전부 해안선에 붙어 있다.
  - **왜 통과했나.** 도시 검사가 "반경 6px에 물이 있나"만 봤다 — **바다 한복판이면 늘 통과한다.**
    육지가 하나도 없으면 `섬 항구`로 조용히 집계했고(26곳!), 그것을 실패로 세지 않았다.
    항구는 "물에 닿는 곳"이 아니라 **뭍과 물이 만나는 곳**이다.
  - ⇒ **검사를 실측으로 다시 세웠다.** 도시에서 가장 가까운 뭍까지의 거리를 재니
    코드판 **평균 8.5px**(28px 초과 0곳) vs AI판 **평균 29.5px**(로도스 52 · 알렉산드리아 64).
    두 검사를 넣었다: ① 개별 도시가 뭍에서 **28px 초과**면 실패 ② **12px 초과가 절반을 넘으면**
    "지형이 좌표계와 다르다"로 실패(코드판은 20% 안쪽). 새 검사로 AI판은 **반려 2건**, 코드판 9장은 통과.
  - **부수로 진짜 결함을 하나 잡았다** — 동아시아 **쌍서**(닝보 앞바다 밀무역 섬)가 그림에 섬이 없어
    항구가 물 위에 떠 있었다(31px). `landmass`에 사각형을 넣어도 안 됐다 — 그쪽은 저해상 격자라
    작은 섬이 스무딩에 먹힌다. `auto.pinIsles`(확대 후 좌표에 직접 찍는 경로)를 내어 해결했다.
  - **교훈**: 기계 검수는 최소조건이다. 1차는 좌표가 틀려 반려, 2차는 검사 항목만 역설계해 반려,
    3차는 **검사의 빈틈을 통과**했다. ⇒ 이제 납품을 받을 때 **좌표를 그림 위에 찍어 눈으로 대조**한다
    (`QUICKMAP-art.md` §3에 절차를 적었다).

- **[2026-08-17] #art #map** — 지중해 지도 3차 납품(AI 생성) 후처리 (**결과는 위 항목대로 반려·원복**)
  - 파일은 리포에 없었다 — 다른 AI의 작업 폴더(`~/.gemini/antigravity-ide/brain/…/map_mediterranean_*.png`)에
    있었고, 이미 적용돼 있던 npc·ports 원본도 같은 곳에 있었다. 1024×1024 · 119,190색.
  - ~~좌표 정합은 맞았다~~ **틀린 판정이었다** — 크롭 25가지를 훑어 "도시 28곳 전부 물가"를 얻었지만
    그것은 **바다 한가운데라 물이 넘쳤던 것**이다(위 항목). 크롭으로 맞출 수 있는 문제가 아니었다.
  - ★ **그런데 게임이 그리는 것이 함께 그려져 있었다** — 글자 6개(ROME·CONSTANTINOPLE…)·항로선·항구점·
    나침도 2개·배 5척. 그대로 넣으면 도시 이름이 두 번 겹치고 배경의 배가 영원히 멈춘다.
    **검수기는 이걸 못 잡는다.** ⇒ `tools/clean-map-art.py`로 지웠다(재사용 가능하게 도구로 저장 —
    나머지 8장이 오면 한 줄이다). 가른 기준: 항로선↔해안선은 `B-G`(50 vs 8), 나침도·배는
    바다에 둘러싸인 구멍 중 `B>85`, 글자는 흰 획 + 주변 검은 외곽선.
  - 밟은 함정 셋 — ① `lum = (R*299+G*587+B*114)//1000`을 **int16**으로 계산해 오버플로(67,645가 2로 나왔다),
    선 검출이 통째로 엉망이었다 ② 지운 자리를 **가장 가까운 픽셀 복사**로 메우니 지름 100px 나침도 자리에
    **검은 원**이 남았다 → 바깥에서 한 겹씩 평균으로 확산 채움 ③ 육지 위 글자를 바다색으로 메워
    바다 비율이 78→82%로 늘어 반려됐다(검수 상한 80%).
  - `check-map.py`의 `relative_to()` 크래시는 **재현됐다** — 조건이 "상대경로"가 아니라 **리포 밖 경로**였다
    (납품물을 다운로드·후처리 폴더에서 검수하면 반드시 밟는다). 앞선 "재현되지 않는다"는 정정이 틀렸고 다시 고쳤다.
  - **의뢰서 두 장이 낡아 있었다.** `BRIEF-MAP.md`는 항구 배경을 "❌ 미납"이라 적어 뒀는데 실제로 6장이
    적용돼 있었고, `BRIEF-NPC.md`의 키 규칙은 실제 id와 달랐다(`figure-shibakan` ↔ `eas-ningbo-kanhe`).
    둘을 실제와 맞추고, **§4-A "그리지 말아야 하는 것"**(글자·항로선·항구점·배·나침도 · 400×225 정수배)을
    새로 넣었다 — 이것만 지켜지면 후처리가 필요 없다.
  - 그림 유입 대기 항목을 `UNIMPLEMENTED.md` **§F 한 절로 모았다**(F-1~F-7: 지도 8장 · 후처리 잔재 ·
    인물 초상 70명 · 병종 얼굴 · 항구 배경 10곳 · 검수기 우회 방지 · 하네스 뷰포트 밖 클릭).

- **[2026-08-17] #combat #ui #balance** — **"사람은 이길 수 있는 상대만 싸운다"를 전제로 조우를 다시 봤다.**
  - ★ **판단 재료가 화면에 없었다.** 조우 모달은 상대의 이름·급까지만 말하고 선체·선원·포는
    **전투에 들어가서야** 보였다 — 피할지 싸울지를 정보 없이 고르는 것은 도박이다.
    ⇒ `map.js: foeVersusLine()`이 고르기 전에 나란히 보여 준다(`상대는 선체 80 · 선원 22 · 포 6문,
    내 배는 선체 47/55 · 선원 14 · 포 2문. 이쪽이 밀린다. 돛을 돌리면 쉽지 않다`).
  - ★ **도주 성공률에 적이 들어가 있지 않았다.** 식이 `(내 속력 − 1)`이라 노를 젓는 갤리와 둔한 캐랙이
    똑같이 따라왔다(어느 상대든 61%). ⇒ `(내 속력 − 적 배 속력)`으로 바꾸고 식을
    `state.js: fleeOdds()` 한 곳으로 모았다 — 전투 도주 단추와 조우 안내가 **같은 것을 본다**
    (보여 준 가망과 실제 판정이 어긋나면 안내가 거짓말이 된다). 0.05~0.95로 묶어 확실한 도주도 포획도 없다.
    시작배 기준 갤리 50% · 캐랙 62%, **두 번 안에 66~79%**라 회피는 성립한다.
    ⇒ §3-6(첫 조우가 초반 배보다 세다)의 처방이 바뀐다 — 적을 깎는 대신 **회피가 성립하게** 했다.
    → [wiki/playtest-log.md](wiki/playtest-log.md) §4-7
  - **인물 초상 두 장도 같은 함정에 있었다**(시박태감·왕직). 의뢰서 §4가 자리를 규정했는데
    게임에 그 자리가 없었고(항구 인물은 텍스트 목록, 전투엔 적 초상 없음), 인물 키도 명부 id와 달랐다
    (`figure:shibakan:idle` → `eas-ningbo-kanhe`). ⇒ `figureSprite`/`pirateSprite`를 내고 두 자리를 만들었다.
    그림 없는 사람은 개별 키로 굽지 않고 직업·세기 실루엣을 공유한다(71명 × 캐시 낭비 방지).
  - **`check-map.py`가 cp949 콘솔에서 판정 전에 죽던 것**을 `sys.stdout.reconfigure`로 고쳤다.
    메모리에 "상대경로 `relative_to()` 크래시"로 적혀 있던 것은 재현되지 않아 정정했다.
  - 하네스: `sail`이 **시작 시점의 모달**을 못 봐 조우해 놓고 `NO_ROUTE`로 죽던 것을 고쳤다.
    `.modal-box`를 그냥 잡으면 `display:none`으로 상주하는 **항해일지 모달**이 걸린다 — 보이는 것만 센다.
  - **스모크가 늘 FAIL이었다** — `playtest.mjs`가 곡물을 **두 번** 사려는데 매매는 10개 단위라
    선원을 태운 뒤 148닢으로는 첫 번에 금고가 빈다(두 번째 단추가 `disabled`). 게임이 아니라
    시나리오가 현재 경제와 안 맞은 것이라 1회로 고쳤다 — **회귀 테스트가 항상 실패하면 신호가 죽는다.**
    지금은 전 단계 통과(남은 브라우저 오류 1건은 `favicon.ico` 404로 무해).
  - `tools/clean-map-art.py`를 `dashboard/architecture.mjs`에 등재했다(`check-architecture.mjs`가
    "파일이 있는데 계층 트리에 없다"로 잡았다 — 메모리가 경고한 그대로다).

- **[2026-08-17] #playtest #art #tavern** — **여덟 바다에서 시작해 훑었고, 만들어 둔 그림이 잠들어 있는 것을 찾았다.**
  - ★ **`char-sailor-eastasia.png`가 로드되면서도 화면엔 한 픽셀도 안 나오고 있었다.** 콘솔은
    `[assets] 20/20개를 갈아 끼웠다`고 말했지만 그것은 *파일을 읽은* 개수였다. 코드가 굽는 키는
    `char:<유닛>:<포즈>:<배색>`(`…:idle:navy`)인데 `manifest.json`은 배색 자리에 권역명을 적어 둬서
    (`…:idle:eastasia`) **아무 데서도 부르지 않는 키**였다. ⇒ `unitSprite`에 네 번째 인자 `faceKey`를 내어
    **키에만 권역 자리를 만들고 배색은 그대로 뒀다**(그림 없는 바다는 코드 생성 폴백 유지).
    술집·항구 부두·전투 양쪽 갑판이 `regionOf(state.at)`를 넘긴다 — 광저우 술집에 삿갓 선원이 앉았다.
    남은 것은 `sailor` 외 병종 PNG와 술집 배경. → [wiki/playtest-log.md](wiki/playtest-log.md) §4-6
  - **같은 술집에 같은 이름이 두 번 앉았다.** `tavernCrews`가 자리마다 이름을 독립 해시로 뽑아
    광저우에 `황(黃)씨 형제`가 6명·5명으로 둘 있었다(자리 최대 5 · 풀 8이면 흔하다).
    ⇒ 이미 앉은 이름은 **옆으로 한 칸씩 비켜 간다**(가장 작은 풀도 5라 반드시 빈 이름을 찾는다).
    확인은 화면 하나가 아니라 **175항구 × 57일자 = 9,975건 전수**로 했다 — 중복 0.
  - **권역별 시작 대장정을 끝냈다**(동아시아·동남아·인도양·중동·아프리카·남미·카리브·대서양 여덟 곳).
    어디서 시작해도 56닢짜리 첫 항로가 있고, 시장·술집 이름·조선소 선종·소문이 다 그 바다 것이다.
    동아시아는 일곱 항목을 끝까지 굴려 **왜구 소선단**까지 봤다(무국적·해골기·노 달린 선체).
    타이틀이 시작지를 말하는 것도 확인(§4-3 해소). → [wiki/playtest-log.md](wiki/playtest-log.md) §4-5
  - **`check-map.py`가 판정 전에 죽고 있었다** — Windows 콘솔 기본이 cp949라 `—`를 출력하다 `UnicodeEncodeError`.
    `sys.stdout.reconfigure(encoding='utf-8')`로 해소. 메모리에 "상대경로 인자 `relative_to()` 크래시"로
    적혀 있던 것은 재현되지 않아 정정했다(`QUICKMAP-art.md` §3).
  - **하네스(`tools/playtest-live/`)를 실사용에 맞게 고쳤다.** ① `사기`가 스무 줄·`태운다`가 넷이라
    글자로 고를 수 없던 것을 **행·카드 이름칸 첫 줄을 얹어**(`곡물 · 사기`) 해결 — 금액만 다른 형제도
    같은 단추로 보고 문맥을 붙인다 ② 지도 항로는 `span.rn`이라 태그로 안 잡히던 것을 **`cursor:pointer`**까지
    후보로 보게 하고, 배지·수치 조각은 그 행에 흡수 ③ 목록 출력을 접어서 낸다(한 화면 51줄 → 11줄)
    ④ `start <도시id>`(에셋 베이크 대기 + 타이틀 문구 + **틀린 id 경고**) · `sail <도시>`(도착·조우까지 지켜본다)
    ⑤ 커서를 화살표에서 **반투명 원**으로. → [wiki/playtest-harness.md](wiki/playtest-harness.md)

- **[2026-08-17] #playtest #balance #ui #docs** — **사람 속도로 직접 눌러 봤고, 밸런스 판단 도구가 거짓말을 하고 있었다.**
  - ★ **`sim-core.mjs`의 플레이 전략이 곡선을 망가뜨리고 있었다.** "선체 60% 미만이면 전액 수리"가
    누수 `2pt/일`짜리 시작배(낡은 바사)에 걸려 **하루 28닢**(선원 여섯 급여 9.8닢/일의 세 배)을 태웠다.
    4항차째 336닢이 날아가고 매입 자본이 회복되지 않아 **20판 중 18판이 배를 한 척도 못 샀다.**
    게임은 멀쩡했는데 도구가 "성장 불가"라고 말하고 있었던 것 — 커밋을 여섯 개 거슬러 이분 탐색한 뒤에야
    범인이 시뮬 쪽임을 알았다. ⇒ **누수 있는 배는 수리하지 않는다**(hp는 1 밑으로 안 깎이므로 항해는 막히지 않고,
    실제 플레이어도 삭은 배 대신 갈아탈 돈을 모은다). 완주 2/20 → **20/20판**, 배 0척 → 5척,
    30항차 금고 0 → 7,497닢. 사다리 복구: 코카 15항차 · 카라벨 17 · 브리간틴 25 · 캐랙 35.
  - ★ **1회 실행을 근거로 삼을 뻔했다.** 같은 커밋(`31f13aa`)이 한 번은 90항차 완주, 다음 번은 11항차 정지였다.
    그래서 `tools/sim-stat.mjs`(N판 → 완주율·체불·첫 배·금고 중앙값)를 만들어 판단을 분포로 바꿨다.
  - **`START_GOLD`를 다시 재고 200을 유지했다.** 옛 근거("완주 8/12")는 잘못된 전략으로 잰 값이라 폐기.
    새로 재니 **175↔180이 절벽**(그 아래는 첫 항해도 못 뜬다)이고 **180~210은 10항차 금고가 1,072로 같다** —
    시작 자금은 난이도 손잡이가 아니다. 초반 압박이 옅어진 진짜 원인은 **삭은 배 무한 운항**(→ `UNIMPLEMENTED.md` C-13).
  - **조사 표기 9곳을 없앴다.** `낡은 바사을(를)` · `코카(으)로` · `캐랙을(를)`. 헬퍼가 없던 게 아니라
    **문장을 만드는 곳이 씬만이 아니었다** — `state.js`는 모듈 방향상 `ui.js`를 못 부른다.
    ⇒ `js/josa.js`(아무것도 import 않는 leaf)로 내리고 `ui.js`가 re-export(기존 import 경로 유지).
  - **매매 수량 안내가 목록에 가려 안 보였다** — 규칙(`기본 10개 · Shift 전량 · Ctrl 1개`)은 시장 패널 *맨 아래*라
    품목 20줄 넘는 항구에선 스크롤 밖이었다. `captionSide`를 `top`으로 올리고 단추에 툴팁을 붙였다.
  - **금고가 비어도 아무 표시가 없었다** — `advanceDays`가 `Math.max(0, gold - now)`라 부족분이 소리 없이 증발했다.
    못 낸 액수를 항해일지에 남기고, 지도 항로 목록에서 감당 못 하는 항로를 붉게 짚는다(`.rd.short`).
    ⚠️ 값은 그대로다(빚도 출항 차단도 아직 없다).
  - **좁은 창에서 조선소 카드가 깨졌다** — 이름이 `낡은 바 / 사`로 끊기고 매각 단추가 패널 밖으로.
    재현 조건은 폭이 아니라 **정박선+승선선 2척으로 단추가 쌓일 때**였다. `.yard-ship`에 `flex-wrap`, 이름에 `nowrap`.
  - **검증 하네스를 문서로 굳혔다** — 이미 있던 `tools/playtest.mjs`(배치·`--smoke`)와 새로 만든
    `tools/playtest-live/`(창을 살려 두고 한 스텝씩·화면 커서 오버레이)의 역할을 갈라 `wiki/playtest-harness.md`에 적었다.
    OS 마우스(`SetCursorPos`)는 이 환경에서 페이지에 닿지 않아 **CDP 입력 + 페이지 안 커서 그림**으로 간다.
  - **권역별 시작 가능성을 쟀다** — 아홉 바다 모두 56닢짜리 출발지가 있다. 막다른 주머니
    (사카이 199닢·마포·카이로·이스파한·세부·남미 4곳)만 첫 항차부터 막히는데 그것은 고증대로다.
  - 새로 연 것: `UNIMPLEMENTED.md` **C-13**(삭은 배 무한 운항) · **C-14**(권역이 이름만 갈리고 얼굴은 지중해 하나).

- **[2026-08-20] #story #docs #agents** — **게임 데이터 위에 장편소설을 얹었다 — 그리고 그 검수가 "게임에 후반이 없다"를 증명했다.**
  사용자 요청으로 `story/`를 신설했다. 대항해시대 조선 인물이 아홉 바다를 돌아 나라에 부두를 내는
  장편 《구해기(九海記)》. **게임 데이터를 하나도 바꾸지 않고** 그 위에 사람만 얹는 것이 규약이다.
  - **시놉시스 6부 구성** — 바다마다 *제도 하나 · 위기 하나 · 발견 하나 · 대가 하나*. 제목이 곧
    주인공의 수첩이고(액자), 아홉 장 중 **셋은 조선에서 실패**한다. `story/SYNOPSIS.md`
  - **권역별 소재집 450항목**(9권역 × 50: 발견 8·위기 10·보상 6·성장 6·인물 12·이벤트 8) —
    집필 9 + 묶음 PM 3 + 총괄 1, **13 에이전트 워크플로**. `story/regions/`
    PM이 잡은 최대 결함: *"세율 낮은 옆 항구로 짐이 샌다"*가 일곱 권역에 있어 **제2해와 제7해가 같은 말**이었다
    → 제7해를 세율이 아니라 **자격**(명부·저울·통역)의 장으로 다시 세웠다.
  - **시간축·게임레벨 교차 검수 7 에이전트** — 연표 초안 3 → 통합 1 → 경제·해금전투 2 → 교차판정 1.
    **총 6,317일 = 17년 4개월**(25세 → 42세) 확정. 충돌 18건(게임 12 · 소설 2 · 미구현 승격 4),
    치명 결함 13 전원 처리. `story/TIMELINE.md` · `story/level/VERDICT.md`
  - ★ **가장 큰 산출은 소설이 아니라 게임 사양서였다.** 1~4부는 게임 실측 곡선과 정확히 맞는데
    5·6부에서 깨진다 — 게임의 부가 31~45항차에서 꺾여 평탄해지기 때문이다. 그 격차를 메우는 것은
    벌이가 아니라 **지출**이고, 그것이 A-1·A-1b·A-2다. `story/GAME-LINK.md §8`이 값까지 확정한
    그 후반 사양서다(거점 종류별 가격·유지비 연 6% · 공업력 승급 3중 비용 · 등록 플래그).
  - ★ **"짐을 쪼갠다"가 지금 게임에서 성립하지 않는다**는 것을 소설이 드러냈다 — 시장 압력이
    하루 32%만 회복해(`MARKET.decay 0.68`) 나눠 팔아도 벌점을 못 피한다. A-1 창고의
    *"보관 중인 화물은 압력을 누적시키지 않는다"* 한 줄이 이것을 뒤집는 유일한 장치다.
  - **엔딩 확정** — 조선 여덟 항구를 깨우고 **철갑 거북선(tier 4 신규)**으로 닫는다. `story/ENDING.md`
    거북선(tier 3)은 `tierNeeded`가 원산국 항구의 tier를 1 낮게 쳐 **부산포 공업력 2로 이미 열려 있어**
    히든이 아니다 — 그래서 히든의 자리를 한 등급 위로 올렸다. tier 4는 조선 항구에서 industry 3,
    그 밖에서는 4가 필요한데 **세계에 그런 항구가 없다** → 조선만, 그것도 키운 항구에서만 짓는 배.
    진수지는 부산포가 아니라 **염포**(배후가 달천 철장 · 1→3 두 계단).
  - 이 작업으로 `changelog.md`가 40KB를 넘어 08-16분을 [changelog-2026-08-16.md](changelog-2026-08-16.md)로 분리했다.
