# changelog — tradeship

> 변경 이력 정본(lazy·거의 안 읽음). `claude-memory.md`에는 **현재 상태만** 두고, "무엇을 어떻게 고쳤다"는 여기에 쌓는다.
> 상한 40KB — 넘으면 시점 경계로 분할. 과거분(2026-08-08 ~ 08-15 앞부분)은 [changelog-2026-08-early.md](changelog-2026-08-early.md).

## 2026-08

- **[2026-08-16] #balance #combat #events** — **돌발 이벤트·전투 보상 감사 — 세계가 열 배가 됐는데 보상은 지중해 한 바다 시절 그대로였다.**
  - **먼저 잴 도구부터 만들었다 — `tools/sim-events.mjs`.** 초·중·후반(200 / 4,000 / 20,000닢) 상태를 실제로 만들어 화물을 싣고, `jettisonCargo`·`banditRaid`·`payToll`·`pickEnemy`·`pirateEnemy`를 **실물 함수로** 돌려 한 건의 크기를 **자산 대비 %**와 **항차 순이익 대비 배수**로 낸다. 씬에 박힌 값(표류물·폭풍)만 `MIRROR` 블록에 거울로 두었다.
  - **잡아낸 것 ① 전리품이 성장 사다리를 통째로 건너뛰었다.** 세기 1 좀도둑 하나 = 금화 300~1,160 + 나포선 ≈ **시작 자산의 573% · 초반 항차 순이익의 39배**. 첫 배로 하나만 잡으면 코카(1,100)를 샀다. 원인은 전리품이 **진 자의 크기**로 정해지는데 뜻은 **이긴 자의 크기**로 읽힌다는 것 — 해적을 가난하게 만드는 대신 **옮겨 실을 수 있는 양**에 상한을 걸었다(`state.js: spoilsCap/capSpoils/capLoot`, `data.js: SPOILS_SHARE 0.30 · TAIL 0.12 · FLOOR 60`). 상한 위로 꼬리가 남아 현상금 큰 놈이 여전히 더 값나간다. 결과: 초반 119% · 중반 28~107% · 후반 6~47%. **`pickEnemy`(떠돌이)와 `world.js: pirateEnemy`(명부) 둘 다** 같은 규칙을 통과한다.
  - **잡아낸 것 ② 낡은 바사가 브리간틴(4,200닢)을 통째로 끌고 갔다.** 나포선에 **나포 선원**을 요구한다(`data.js: PRIZE_CREW 0.35` · `state.js: spareCrew/prizeCrewNeed`). 모자라면 해체값만 받고 그것도 상한을 탄다. 씬은 이미 "끌고 갈 인원이 없어 해체해 팔았다"는 갈래를 갖고 있어 화면 변경이 필요 없었다. **불가능이 아니라 대가다** — 바사도 열둘을 태우면(선실 16) 브리간틴을 몰고 간다. 실측: 선원 6명 → 해체 204닢 / 12명 → 선단 편입.
  - **잡아낸 것 ③ 세계를 넓히자 "대박은 사건에서 나온다"가 조용히 죽어 있었다.** `SHOCK.events[].perDay`는 **세계 전체** 하루 발생 건수라, 도시가 16 → 175가 되며 한 도시가 사건을 겪는 주기가 20개월 → **216개월**로 벌어졌다. 사건이 드물어진 게 아니라 **닿을 수 없는 곳에서만** 일어나게 된 것이다. `SHOCK.densityBase 16`으로 도시당 빈도를 지중해 시절과 맞췄다(`state.js: rollShockEvents`).
  - **곡선 대조(같은 트리에서 A/B · 24시드×90항차)** — 환산 전 항차 ROI 중앙값 7.1%(목표 밴드 8~16% **아래**)·완주 258항차·적자 31.0% → 환산 후 **12.5%**·**541항차**·적자 21.4%. 20시드 자산 곡선에서는 환산 전 **20항차를 넘긴 판이 0/20**(전부 낡은 바사에서 끝)이었고 환산 후 5/20이 넘겨 3판이 90항차 75,432닢까지 갔다. **`WORLD-INTEGRATION-TODO.md` §6("시뮬이 열 항차에서 멈춘다")의 절반은 이것이었다** — 중앙값은 여전히 11항차라 나머지 절반(수리비·시작자본)은 남아 있다.
  - **근거·검사기**: `voyage-evidence.json`에 `spoilsVsAssets`(밴드 15~100% · verdict gameplay)와 `shockDensityPerCity`를 추가하고 `check-voyage.mjs`가 대조하게 했다 — 상한 배선이 끊기거나(`world.js`에서 `capLoot` 제거) 밀도 환산이 사라지면 실패한다. 실측 47%로 밴드 안.
  - **대시보드 해적 탭은 상한 전 정가를 보인다** — 배들끼리 비교하는 자리라, 재는 동안만 금고를 크게 잡아 상한이 안 걸리게 했다(공식을 재구현하지 않기 위해).
  - **못 고친 것(씬 소유)**: 표류물(금화 60~300 + 화물 3~11개 ≈ **1,174닢 고정**)과 폭풍 선체 피해(6~19pt ≈ 182닢 고정)가 `scenes/map.js`에 박혀 있다. 고정액이라 초반엔 자산의 **587%·91%**, 후반엔 6%·1%다. 표류물은 7% 가중치라 **14항차에 한 번** 뜨는데 초반 판이 11항차에 끝나므로, 사실상 절반의 판이 "코카를 주는 이벤트"를 만난다. 값을 `data.js`로 빼는 것이 다음 수.
  - 검증: `test-rules`·`test-payroll`·`test-world`·`test-tavern`·`check-voyage`·`check-evidence`·`check-prices`·`check-routes`·`check-wages`·`check-world`·`check-architecture` 전부 exit 0.

- **[2026-08-15] #memory #audit** — **세션 마감 품질 패스.** `memory-optimizer` §2(중복·stale 감사, 필수) + §1(라우터 감사 — 이번 세션에 행을 다수 수정했으므로 필수)을 돌렸다. §3 세션필독 다이어트는 `gotchas.md` 3,941B < 4KB라 **판정해서 제외**.
  - **리프(wiki) stale 8건.** always-load만 보면 토픽 문서가 조용히 낡는다 — 이번엔 급여를 발생주의로 바꾼 것이 리프에 반영되지 않아 `officer.md`가 "급여는 1일차부터 **나간다**"·"`advanceDays()`가 징수"로 남아 있었다(지금은 쌓이기만 하고 30일마다 `settlePayroll()`이 치른다). `economy-trade.md`도 `total`을 그날 나가는 액수처럼 설명하고 있었다.
  - **이전 세션에서 놓친 stale도 함께 잡았다** — 교역품 기준가 표(유리세공 155·비단 310·향신료 225 → 실제 190·420·330) · 대포 값(340닢 → 240/420/720) · 플류트 `crewMin`(14 → 9) · 입항세 절(두 겹 구조 미반영) · `CREW_WAGE` 정본 경로(state.js → data.js).
  - **파생값을 서술에서 걷어냈다**(§2 규칙). "항해비 구성은 일당 20%·보급 22%…"와 차익 구조 예시 표는 밸런스를 만질 때마다 거짓이 되는 값이라 **대시보드가 그때그때 내도록** 위임했다. 실제로 낡아 있던 자리다.
  - **brittle count 위임 2건** — 관세 지정 목록(늘리면 즉시 stale · `check-evidence.mjs`가 출력한다)과 기질 종수(콘텐츠 확장이 명시된 방향이라 반드시 는다).
  - **§1 인라인 상세 축약 1건** — "대시보드 탭이 무엇을 보여주나" 행이 5탭 내용을 382자로 나열하고 있었다. 같은 내용이 `file-map.md`에 실존함을 `grep`으로 확인하고 포인터+가드 181자로 줄였다.
  - 개명 잔재 정리(`shared.mjs` → `shared.js`, 코드 주석 2곳).
  - 검증: 트립와이어 전 항목 통과(`claude-memory.md` 15,332/15,360) · dead pointer 0 · 검증 10종 PASS.
  - auto-memory 2건 확장 — 구조 문서도 기계가 지킨다(`data-with-evidence-layer`) · **지정값이 실측에서 성립하지 않으면 숫자가 아니라 의도를 따른다**(`balance-by-feel-then-verify`). 새 파일 대신 기존 항목에 붙여 중복을 막았다.

- **[2026-08-15] #dashboard #architecture** — **오버뷰 탭 신설 — 구조·동적 생성물·세이브 대상을 기계가 지키는 화면으로.** 사용자 요청("대시보드 관리 항목들도 코드·데이터 분리 잘 된 거 맞지 · 동적생산하는 것들 · 데이터에 저장되는 것들 · 전체 아키텍처 트리구조를 오버뷰 탭에").
  - **먼저 분리 상태를 실측했다.** 계측 4개(`measure`·`pirates`·`ports`·`wages`)는 DOM 참조 0으로 규약대로였고, 렌더 파일의 산술은 전부 **SVG 좌표**지 규칙 재구현이 아니었다. 다만 **`shared.mjs`가 `document`를 쓰고 있었다** — 이 폴더 규약은 `.mjs`=계측(node로도 돌아간다) / `.js`=렌더인데 이름만 계측 계열이라 "node로 돌려도 되는 줄" 오해를 부른다. **`shared.js`로 개명**했다(import 4곳).
  - **`dashboard/architecture.mjs` 신설** — 계층 트리(값→규칙→화면 + 그림·근거·검증·대시보드 7계층 55파일) · 동적 생성물 14종 · 상태 필드(세이브 대상)의 정본. DOM을 안 쓰므로 계측 계열이 맞다.
  - **`tools/check-architecture.mjs` 신설이 핵심이다.** 구조 설명은 문서로 두면 **조용히 낡는다** — 파일을 추가하고 트리에 안 적으면 오버뷰는 계속 옛 구조를 보여주고 아무도 모른다. 그래서 **실제 파일**(유령·누락·중복)과 **실제 `state` 키**(상태 필드 불일치)를 대조해 실패시킨다. 근거 JSON을 `check-evidence.mjs`가 지키는 것과 같은 꼴이다. 실제로 만들기 전의 `overview-view.js`를 유령으로 잡아냈다.
  - **동적 생성물을 세 갈래로 갈라 적었다** — **캐시되는 것**(스프라이트 6종·6MB LRU) · **결정론 생성**(술집 무리·중고 매물·계약·시세 노이즈 — `hash`라서 **세이브에 넣지 않는다**) · **난수로 굴리는 것**(해상 이벤트·시장 충격·NPC·이탈 — 결과가 상태에 남으므로 세이브 대상). "무엇을 저장하고 무엇을 저장하지 않는가"의 판단 기준이 화면에 남았다.
  - 오버뷰가 게임 화면을 그리지 않아 캐시 실측이 0인 것은 정상이라, **게임에서 잰 참고표**(실제 플레이 0.98MB · 항구 16곳 5.98MB · 전조합 5.80MB — 상한 없을 때 12.5MB)를 함께 붙였다.
  - 검증 10종 전부 PASS · 브라우저 실동작 확인.

- **[2026-08-15] #tariff #refactor #perf** — **입항세를 항구별로 열고, 값과 규칙을 갈랐다.** 사용자 요청("관세는 최소로 구현하고 대시보드 항구별 탭에 관세 · 항구별 데이터·코드 분리 확인 · 동적으로 생성되는 게 많지 않은지").
  - **입항세 두 겹**: 규모별 기본율(`TARIFF`) + 그 도시만의 오버라이드(`CITY_TARIFF`). 지금 지정한 곳은 셋뿐이고(알렉산드리아 8.5% · 이스탄불 7.0% · 제노바 4.5%) 나머지는 기본율로 굴러간다 — **비어 있는 도시가 "빠진 것"이 아니다.** 사료(맘루크 10~20%)를 그대로 옮기면 게임은 매각에만 물어 그 항구가 통째로 죽으므로, 성격이 드러날 만큼만 올리고 `verdict: gameplay`로 남겼다.
  - `content/city-evidence.json`에 `cities[id].tariff` 근거 계층을 붙이고 `check-evidence.mjs`가 **불일치·유령·무근거·배선끊김**을 잡게 했다. 배선끊김 검사가 핵심이다 — 데이터만 맞고 `baseTariff()`가 기본율을 돌려주면 게임은 아무것도 안 바뀐 채 검증만 통과한다.
  - **`baseTariff()` 신설** — 항구의 성질을 적는 자리(대시보드 표·근거 검증)는 이쪽을, 실제로 무는 값은 `tariffRate()`를 쓴다. 후자는 부관 특전이 곱해져 **탭을 여는 순서에 따라 6.0%가 3.9%로 바뀐다**(대시보드가 실제로 겪은 버그).
  - 대시보드 항구 탭에 **입항세 절** 신설 — "지금 매기는 값" vs "규모만으로 정하면" vs 차이, 그리고 판정·근거·출처.
  - **값과 규칙을 갈랐다.** `state.js`(로직)에 흩어져 있던 **튜닝 상수 26개**(임금·보급·유지비·보험·급여 주기·시작 자금·조우 확률 환산·조선소 계수·중고·나포)를 주석째 `data.js`로 옮기고, `state.js`가 그대로 re-export하게 했다 — **기존 import 30여 곳을 하나도 안 고쳤다.** 밸런스를 만지려고 1,500줄짜리 로직 파일을 열 일이 없어졌다.
  - **동적 생성물을 실측했다.** 실제 플레이 경로(항구→술집→조선소)는 35개 0.98MB로 가볍지만, **항구 16곳을 다 돌면 배경만 5.6MB**(352KB씩)이고 선박 조합까지 열리면 **12.5MB**였다. 해제가 없었다.
    - `bake`에 **6MB LRU 상한**을 넣었다(`touch`로 접근 순서 갱신 → 넘치면 오래된 것부터 버림). 매 프레임 쓰는 스프라이트는 항상 최근이라 안 버려진다. 최악이 **5.8MB에서 멈춘다**(재굽기·화면 정상 확인).
    - 술집 테이블 전경이 400×225(352KB)로 구워져 **95%가 투명 픽셀**이었다 — 항구 배경 한 장과 같은 메모리를 테이블 둘에 쓰고 있었다. 200×34(26KB)로 줄였다.
  - 검증 9종 전부 PASS · 대시보드·술집 브라우저 실동작 확인.

- **[2026-08-15] #game #payroll #balance** — **급여를 발생주의로 · 못 주면 짐을 들고 떠난다 · 정산 화면 신설.** 사용자 요청("반란 보다는 불만으로 돈되는 무역물을 들고 도망가는걸로" · "급여정산 화면도 필요 — 유지비 수입 무역품 구매여부 현재 적재한 무역품 상황").
  - **급여가 `advanceDays`에서 금고를 안 건드린다.** 날마다 `state.payroll.due`에 쌓았다가 **항구에서** 30일마다 치른다. 전에는 매일 금고에서 빼서 **체불이라는 사건 자체가 생길 수 없었다** — 금고가 비면 `Math.max(0,…)`로 0에서 멈출 뿐 선원은 아무 일 없이 일했다. 물자·유지비·보험은 그대로 즉시 차감(외상이 안 되는 것들)이고, 정산은 바다에서 하지 않는다(돈을 줄 데가 없고 항해 중에 터뜨리면 손쓸 방법이 없다).
  - **반란이 아니라 이탈이다.** 배를 빼앗기는 사건은 넣지 않았다. 사람이 조용히 사라지고 **값나가는 짐을 들고 간다**(`stealCargo` — 폭풍 투하가 싼 것부터인 것과 정반대다. 버리는 쪽은 선장이 고르고 훔치는 쪽은 도둑이 고른다). 그래서 체불의 대가가 "게임 오버"가 아니라 **"다음 장사 밑천이 줄어드는 것"**이 된다.
  - **참을성이 갈림길이다.** 술집에서 심어 둔 `CREW_TRAITS[].temper`가 드디어 규칙에 물렸다 — 40%를 못 준 달에 성실한 무리(0.85)는 불만 0.10, 주정뱅이(0.25)는 0.52로 **5배** 차이다. 싸게 태운 대가가 여기서 돌아온다. 문턱(0.55)을 넘어도 **확률 굴림**이라 반드시 떠나지는 않는다 — 확정으로 두면 한 번 밀린 달에 갑판이 통째로 비어 회복할 여지가 없다.
  - **정산 화면**(`js/payday.js`) — 왼쪽 장부(수입 5·지출 9 항목·이 달 손익) · 오른쪽 선창(품목별 평가손익·"여기서 팔면 얼마"·**다음 장사 밑천**) · 아래 갑판 분위기. 닫을 수 없다(안 주는 것도 선택이지 회피가 아니다). 항구 사이드패널에는 **급여 카드**를 상시 띄웠다 — 그날 닥쳐서야 알면 이미 늦다.
  - **장부는 `book()` 하나로만 적는다.** 매출·관세·성과급처럼 한 거래에서 세 갈래로 갈리는 것이 있어 호출부마다 더하면 반드시 빠뜨린다. 여기서 버그 1건 — **매출을 관세 뗀 뒤(`gain`)로 적으면서 관세를 지출로 또 적어 관세가 두 번 잡혔다**(장부가 금고와 8닢 어긋남). `raw`로 고쳤고 `test-payroll.mjs` ⑥(장부↔금고 정합)이 잡아냈다.
  - **시작 자금 150 → 200.** 사용자 지정은 150이었으나 급여를 뒤로 미룬 뒤 실측하니 **아슬아슬이 아니라 불가능**이었다 — 30항차 12판에서 완주 **1판** · 체불 7판. 매입 자본이 100닢 남짓이라 한 항차 이익이 항해비를 못 넘는다. 200이면 완주 8/12 · 체불 2/12로 의도한 구간에 앉고, 250이면 완주 12/12 · 체불 0으로 긴장이 사라진다.
  - **시뮬도 정산을 한다**(`sim-core`, 입항해서 팔고 난 뒤). 빼면 급여가 영영 안 나가 사실상 공짜가 되고 자산 곡선이 통째로 부풀어 오른다.
  - 검증 `tools/test-payroll.mjs` 신설 · 기존 9종 전부 PASS · 브라우저에서 정산·체불·이탈(화물 절도까지) 실동작 확인.

- **[2026-08-15] #game #tavern #balance** — **술집 신설 · 선원 0명으로 시작.** 사용자 요청("술집에서 선원 등용 구현하자" · "최초 선원은 없어" · 시작자금 150).
  - 부두 사이드패널의 **"선원 고용 (55닢/명) [5명]" 버튼을 술집 씬으로 대체**했다. 값만 있고 선택이 없던 자리다 — 누르면 5명이 붙고 끝이라 화면에 남길 판단이 없었다.
  - 자리마다 **인원·기질(7종)·계약금·요구 일당**이 다르고 **값이 두 갈래**다(계약금은 지금, 일당은 항해 내내). 싼 무리는 백병 병종이 `sailor`라 갑판에서 쓸모가 없다. 기질은 `wageMul·advMul·troop·temper`가 한 묶음.
  - **`state.crew`(숫자)를 인원의 정본으로 유지**하고 `state.bands`를 기록으로 덧붙였다. 개별 선원 객체로 가면 전투·시뮬·대시보드까지 파급되는데, 무리 단위면 파급이 `voyageCost` 한 줄(`avgCrewWage()`)이다. 둘이 어긋나는 자리(전투·폭풍 사상)는 `trimBands()`가 맞추고, 그 호출을 **`trimLoadout()` 안에** 넣어 배선을 한 곳으로 모았다 — crew가 줄어드는 자리는 전부 이미 그 함수를 부르고 있었다.
  - **시작 조건**: 금화 900→**150**(`START_GOLD`) · 선원 10→**0** · 갑판 배치 초기값도 비웠다. 첫 행동이 매매가 아니라 술집행이 된다.
  - **계약금 8닢은 역산한 값이다.** 처음 14닢으로 뒀더니 150닢으로 여섯을 태우는 데 92닢이 나가, 남은 58닢으로는 **가장 짧은 항로(제노바 2일·항해비 43닢)조차 화물을 못 실었다** — "아슬아슬"이 아니라 막다른 길이었다. 8닢에서 6명 52닢 → 항해비 43닢을 치르고 곡물 2칸이 남는다. `tools/test-tavern.mjs` ⑤가 이 조건을 지킨다.
  - **시뮬이 술집을 모르고 있었다.** `sim-core`가 선원을 안 태워 첫 항차부터 인원 부족으로 속력이 깎였고, 90항차를 돌려도 **배를 한 척도 못 샀다**(15항차 자산 724닢·전 선종 미구입). `manCrew()`를 넣어 실제 플레이어처럼 술집을 먼저 보게 하니 코카 29항차·90항차 20,661닢으로 곡선이 돌아왔다.
  - 그림은 **배경+전경 2겹**이다. 배경 한 장에 테이블을 그리면 인물이 항상 위에 그려져 테이블이 발치에 깔린 널빤지가 된다 — `tavernFrontSprite()`를 인물 뒤에 덧그려 하반신을 가린다. 또 처음에 배경을 400폭 전체에 그렸다가 **카운터·두 번째 창·그물이 DOM 패널(x≥196)에 통째로 가려져** 왼쪽이 휑했다. 조선소(x≥206)와 같은 실수라 `QUICKMAP-engine.md` §3에 가드로 남겼다.
  - 검증: `tools/test-tavern.mjs` 20종 신설(결정론·항구 성격·기질별 값 분포·첫 항차 성립·명부 동기화·중복 등용 방지) · 기존 검증 6종 + test-rules/test-world 전부 PASS · 브라우저 실동작 확인(등용 → 자리 비움 → HUD·항해일지 반영).
  - 남은 것: **급여 월말 정산(30일)과 체불 시 불만·도망·반란**. `CREW_TRAITS[].temper`가 그 임계로 쓰이려고 이미 들어와 있으나 아직 어떤 규칙에도 안 물려 있다. 시작 금화 150닢이 빠듯한 것도 급여가 선불이기 때문이라 둘은 한 묶음이다.

- **[2026-08-15] #principle #memory** — **최상위 원칙 신설: 콘텐츠는 풍부하게, 금액은 근거에 충실하게.** 사용자 지적("컨텐츠를 제약하면 안되 하지만 금액 부분은 근거에 충실하게" · "배·무역품·도시·해적 종류는 풍부할수록 좋다, 근데 경제 시세는 현실적이어야 돈 버는 재미가 있다").
  - 근거 계층을 만들면 검증기가 **콘텐츠를 억제하는 방향으로 자란다**는 것이 드러났다. `check-evidence.mjs`가 "근거 없음"을 실패로 잡고 있어 **새 도시·품목을 넣으려면 조사부터 끝내야 하는 구조**였다.
  - 실패/경고를 갈랐다 — **실패**는 *코드↔근거 불일치·유령 항목·'확인됨' 판정인데 무출처·규칙 자기모순*, **경고**는 *미조사·짧은 basis·분포 밴드 이탈*. `check-voyage.mjs`도 같은 기준으로 썼다(ROI·톤당승조원은 전부 경고, 보험 미보상·내해 무위험·충격 배선 끊김만 실패).
- **[2026-08-15] #game #balance #evidence** — **조사만 하고 안 적용했던 것들을 적용했다**(근거 `.claude/docs/wiki/research-voyage-returns.md`).
  - **플류트 `crewMin` 14→9.** 톤/승조원이 12.1로 코카(13.0)와 사실상 같아 선종 개성이 죽어 있었다. 18.9가 되어 사료 밴드(16~22) 안 — "적은 선원으로 많이 싣는 배"가 비용에서 드러난다.
  - **보험이 실제로 보상한다.** 폭풍이 심하면 짐을 던지고(공동해손) 잃은 값의 `INSURANCE_COVER`(=요율 계수 30%)만큼 받는다. 그전까지 보험료만 걷고 보상 사건이 없어 **사실상 세금**이었다. 화물 손실 빈도는 시드 20판 평균 **20항차에 1건** — 보험료 중앙값 5%·전손률 4.7%가 독립적으로 가리키는 값과 일치.
  - **큰돈이 사건에서 나온다.** `state.shocks` 신설 — 기근·봉쇄(↑) · 풍작/독점 붕괴(↓) · **NPC 상인이 털리면 그가 대던 항구**(↑). 값이 내려가는 사건을 함께 둔 것이 중요하다(오르기만 하면 "기다렸다 팔면 된다"가 되어 판단이 사라진다). 후보 도시·품목은 매번 `CITIES`에서 만들어 **콘텐츠를 늘리면 사건도 저절로 는다**. 지도에 "뱃사람들의 소문" 패널을 붙였다.
  - **내해·육로에 뭍의 위험.** 해적을 뺀 구간에 노상강도(값나가는 것부터 집어간다)·통행세(금화를 문다)를 넣었다. weight 0으로 두고 `rollSeaEvent`가 명시적으로 고른다 — 확률표에 넣으면 합 100이 무너져 조우 빈도가 통째로 흔들린다.
  - 근거 정본 `content/voyage-evidence.json` + 검증 `tools/check-voyage.mjs` 신설. **실측값은 근거 파일에 적지 않는다** — 콘텐츠가 바뀔 때마다 조용히 거짓이 되므로 검사기가 그때그때 산출해 밴드와 대조한다.
- **[2026-08-15] #balance #tools** — **제안 2건을 실측으로 폐기했다.**
  - "중앙값 ROI가 사료의 절반(5.5%)이니 올려라" → **적재 규율의 문제였다.** `planFor`에 `minMargin`을 넣어 재니, 0이면(총이익 최대화·마지막 칸 마진 0) 5.0%이고 0.15면 **10.1%로 사료 밴드 안**이다 — 같은 경제, 같은 시드. `MARKET.impact`를 0.36→0.22로 낮추는 처방은 중앙값을 1.7%p 올리는 대신 90항차 자산을 34k→**100k**로 부풀린다.
  - "내해가 무위험이라 실효 조우율이 18%→10.3%로 내려갔다(37%가 무위험 구간)" → **1회 실행의 표본오차였다.** `sim-risk.mjs`가 시드 없이 한 판만 돌렸다. 시드 20판 평균은 **18.6%·내해 통과 7%**로 종전과 사실상 같다. 그 틀린 값이 메모리·QUICKMAP·wiki 4곳에 결론으로 박혀 "대체 이벤트 도입"이라는 작업 후보까지 만들어냈다. `sim-risk.mjs`에 시드 인자를 넣어 다음 사람이 1회 실행을 못 하게 막았다.
- **[2026-08-15] #dashboard #game** — **항구 탭 신설**(경제·해적·보수에 이어 네 번째). 항구별 교역품·공업력·부동산 앵커·근거 현황.
  - **교역 항목이 많다고 좋은 항구가 아니다** — 목록을 값이 아니라 **근거의 신뢰도 순**으로 쌓는다(확실한 수요 → 확실한 산지 → 근거가 약한 것). 게임의 항구 시장 목록도 같은 순서로 바꿨다(`js/evidence.js`가 근거 JSON을 읽는다 — 못 읽으면 조용히 원래 순서, `assets.js`와 같은 fail-soft).
  - 부수 결함 — 처음엔 `state.js: tariffRate()`를 썼는데 부관 특전(`tariffOff`)이 곱해져 **경제 탭을 먼저 열었는지에 따라 6.0%가 3.9%로 바뀌었다.** 규모별 기본율(`TARIFF`)을 직접 쓰도록 고쳤다.
- **[2026-08-15] #evidence** — **도시 근거를 채웠다.** 출처가 달린 항목 47→**69/79**. '확인됨·바로잡음' 판정인데 출처가 없던 11건을 처리(8건은 출처를 찾아 붙이고 3건은 `probable`로 정직하게 강등 — `palermo·weapon`은 오히려 반대 증거가 나왔다), 동지중해 4항구(부르사·이즈니크·베이루트·알렉산드리아)를 재조사해 `beirut·fur`·`beirut·glass`·`alexandria·glass`·`athens·oliveoil`을 승격. basis 40자 미만 0건.
- **[2026-08-15] #art** — **지도 2차 납품도 반려.** 무손실 WebP(png와 픽셀 100% 동일)와 좌표 정합은 고쳐졌고 `check-map.py` 전 항목을 통과한다. 그러나 **도시·항로 그래프의 볼록껍질을 바다로 칠한 그림**이라 반도·섬 실루엣이 없다(고유색 8색, 1차 PNG는 48색). 검사 항목만 역설계한 것 — 빠진 것은 "좌표를 이미 만족하는 실루엣 기준판"이다. `manifest.json`은 비웠다(1차 반려 때 비활성화했다고 기록해 두고 실제로는 활성 상태로 커밋돼 있었다).

- **[2026-08-15] #memory #evidence** — **근거 정본을 카테고리별로 갈랐다.** 사용자 지적("물가·교역품·월급·부동산은 카테고리가 다르잖아").
  - `price-evidence.json` 하나에 교역품 물가·임금·부동산·유지비가 섞여 있었고 임금은 `wage-evidence.json`과 **중복**이었다. 무엇을 고쳐야 하는지가 흐려지는 구조다.
  - 넷으로 분할: **`goods-evidence.json`**(교역품 물가·화물 1칸의 실물 정의·대조 2축의 정본) · **`wage-evidence.json`**(임금 — 사료 임금 5종을 흡수해 한곳으로) · **`asset-evidence.json`**(선박가·집세·주택값 + "선원 연봉의 몇 배" 지표) · **`upkeep-evidence.json`**(선체·무장 유지·적하보험·화물 유인). 도시·항로까지 합쳐 근거 파일은 6개가 됐고 **파일 이름만 봐도 어디를 고칠지 정해진다.**
  - `check-prices.mjs`가 세 파일을 읽도록 고쳤고(임금 목표는 `check-wages.mjs` 소관으로 넘겨 경계를 지켰다), 대시보드 물가 절은 `goods-evidence.json`만 fetch한다. 데이터 유실 0(leaf 152개 대조) · 잔여 참조 0 · 검증 6종 PASS.

- **[2026-08-15] #memory #router** — **QUICKMAP을 접근패턴으로 재분할하고 라우팅 행에 코드 경로를 붙였다.** 사용자 지적("퀵맵은 파일 경로와 토픽문서 연결, 특정 토픽을 빠르고 정확하게 탑재하게")에서 출발.
  - **`QUICKMAP-game.md`가 20.3KB·92행**으로 경고선(8KB·30행)의 2.5배까지 자라 있었다 — 세션마다 한두 행씩 더한 것이 조용히 누적된 결과다. 경제 세션이 전투·조선소 라우팅까지 통째로 읽고 있었다.
  - co-access로 갈랐다: **`QUICKMAP-trade.md`**(경제·물가·NPC·계약·고증 4종·대시보드) / **`QUICKMAP-combat.md`**(전투·선박·조선소·무장). 도메인은 art/trade/combat/engine 4개.
  - **§1을 `키워드 | 읽을 문서 | 코드 정본` 3열로** 바꿨다. 전에는 문서만 가리켜 "어느 파일을 열지"는 §2를 다시 훑어야 하는 2-hop이었다.
  - 1세션 라우팅 로드 40.2KB → **trade 34.1KB(−15%) · combat 26.6KB(−33%)**. 무손실(사실토큰 대조 — 실제 유실 3건은 복구) · dead pointer 0 · `claude-memory.md` 14.3→13.7KB.
  - 이번에 드러난 실수 4건을 **글로벌 `memory-optimizer` 스킬에 반영**했다: ①행에 코드 정본 병기 ②행을 추가한 세션은 그 자리에서 도메인 파일 재측정(누적 방치 방지) ③감사 때 리프(토픽 문서) stale도 grep으로 함께 점검 ④파생값을 서술 문서에 박지 말고 검증 스크립트가 출력하게. 무손실 대조 시 경로 토큰 basename 정규화도 SKILL.md에 추가.

- **[2026-08-15] #game #balance #evidence #research** — **물가·임금을 당대 사료 비율로 재조정.** 사용자 지적("부관 한 달 월급이 너무 많은 거 아냐? 선원 일당 2.4닢도 그 시세에 맞나")에서 출발해 15~16세기 지중해·이베리아의 밀·식료품·부동산·임금·선박가를 조사하고 경제 전체를 다시 맞췄다.
  - **지적이 정확했다.** 조사 결과 임금이 물가·선박가 대비 3~10배 과중했다 — "선원 연봉으로 배를 몇 척 사나"로 재면 게임 11배 : 사료 30배(스페인 캐랙 건조비 610두카트 ÷ 선원 연봉 20두카트). 부관은 거기에 직위 배율 3배가 더 얹혀 있었다(6.67배 vs c.1500의 2.0~2.3배). 부관 월급 480닢은 시작 금화 900닢으로 두 달이면 거덜나는 값이었다.
  - **보수**: `CREW_WAGE` 2.4→**1.2**, `OFFICER.wage` 16→**2.6**(배율 2.17배로 사료 구간 안). 결과적으로 부관 수입의 **96%가 성과급**이 되어 오히려 고증(사관의 실수입은 봉급이 아니라 사무역 quintalada)에 더 가까워졌다. 예전에 세웠던 "수치 말고 직함을 고치자"는 처방은 **폐기**했다.
  - **물가**: 교역품 12종 기준가를 사료 비율로 재조정(곡물 20 고정 · 소금 ×2.4 · 향신료 ×16.5 · 비단 ×21 · 금괴 ×35). 후추는 무게당 밀의 30배(자루 하나가 장인 연봉)지만 그대로 넣으면 향신료 항로가 경제를 삼켜 **절반쯤만 반영**했다 — 방향은 사료, 폭은 게임. 와인은 반대로 사료가 훨씬 싸서(벌크 0.3배) 최저선까지 내리지 않았다. 계약 보수(`CONTRACT.value`)도 평균 물가 상승(×1.23)에 맞춰 함께 올렸다.
  - **줄어든 압박을 성장 비용으로 옮겼다** — 임금만 내리면 후반이 풍족해지므로(실측 90항차 +65%) 세 갈래를 신설했다: **선체 유지**(기함도 `upkeep`을 문다 — 배가 클수록) · **무장 유지**(대포 1문당 경0.5·중0.9·장1.6 — 무장이 공짜가 아니어야 "어디까지 무장할까"가 선택이 된다) · **적하보험**(화물가치 × 항로요율 × 0.3). 보험 요율은 `ROUTE_RISK`를 그대로 쓴다 — **그 숫자의 본래 뜻이 보험료**인데 지금껏 조우 확률에만 쓰고 있었다.
  - **값나가는 짐은 두 번 대가를 치른다** — 사용자 구상("돈 되는 곳엔 해적이 꼬인다")을 규칙으로 옮겨 `cargoLure`를 넣었다(화물 9,000닢당 조우 +5%p·상한 +14%p). 초반엔 곡물·소금을 안전한 이웃으로 나르니 0에 가깝고, 향신료·비단을 싣기 시작하면 같은 항로도 표적이 된다.
  - **곡선**: 1항차 1,366 · 5항차 824 · 10항차 1,714 · 30항차 8,002 · 60항차 25,505 · 90항차 32,245닢. 초반은 여전히 빠듯하고 후반 증가폭은 완만해졌다(유지비 7갈래가 함께 늘기 때문). 90항차 누적 항해비 구성은 일당 20% · 보급 22% · 선단 21% · 보험 18% · 선체 11% · 무장 6% · 부관 1.6%.
  - **시뮬 현실화 1건** — 배 구입 시 운영자금을 금화의 8%만 남기던 것(`0.92`)을 30%로 고쳤다. 화물 매입 자본이 커진 뒤로는 배를 사면 실을 것을 못 사 **절반이 파산**했다(실측). 배는 화물을 나르는 수단이지 목적이 아니다.
  - 근거 계층 `content/price-evidence.json`(사료 앵커·목표 비율·화물 1칸의 실물 정의)과 기계검증 `tools/check-prices.mjs` 신설 — 절대액이 아니라 **비율**을 지킨다. 대시보드 탭을 **보수·물가**로 넓히고 교역품 상대가격을 사료 원값과 겹쳐 그렸다.
  - 곁다리로 시작 화면 문구가 **"1500년, 카라벨 한 척과 3,200닢"**이었던 것을 고쳤다 — 연도 고정은 최상위 지침 위반이고 시작 조건도 사실이 아니었다(낡은 바사 + 900닢).
  - 검증: `test-rules` 65 · `test-world` 10 · `check-evidence`·`check-routes`·`check-wages`·`check-prices` 전부 PASS.
  - **문서 품질 패스에서 stale 4건을 더 잡았다** — ①`economy-trade.md`의 무역 곡선 표가 옛 배 순서(카라벨 4항차·라구사 50항차)여서 실측으로 교체(코카 2 · 카라벨 12 · 브리간틴 24 · 캐랙/플류트 36 · 갈레온 55 · 아르고시 74) ②`dev-workflow.md` 검증 목록에 `check-wages`·`check-prices`·`check-map`이 빠져 있었다 ③`world-npc.md`의 계약 보수 공식이 코드와 어긋나 있었다(×1.30~1.55 → 실제 `payMul` 1.22~1.42 · 선금 30% → 25%)이고 "보수를 먼저 정하고 수량을 역산한다"는 핵심 구조가 그 절에 없었다 ④`QUICKMAP-game.md`(현 `QUICKMAP-trade.md`) §3에 이번 교훈 4개 추가(임금을 내리면 후반이 풍족해진다 · `ROUTE_RISK`는 조우확률과 보험료 두 곳에서 쓰인다 · 교역품 값을 올리면 매입 자본이 커져 시뮬이 파산한다 · `GOODS[].base`를 고치면 근거도 같은 커밋에서).
- **[2026-08-15] #dashboard #evidence #balance** — **대시보드에 보수 탭 신설**(경제·해적 아래 세 번째). 최근 15~16세기 급여 조사를 화면으로 옮겼다.
  - 화면 6절: 하루치 보수 사다리(부관 16닢 : 선원 2.4닢 = ×6.67) · **사료 대조**(c.1500 ×2.0~2.3 / 1634-35 ×5.7 구간 위에 게임 값 마커) · 에이미 수입 구성(급여·성과급 누적 면적 + 성과급이 급여를 앞지르는 항차) · 항해비 중 부관 몫(45/24/21/**10**%) · **부관 유무 짝지어 비교** · 근거표.
  - **대조는 배율로만** 한다는 것을 구조로 못 박았다 — 게임 화폐 ‘닢’은 실화폐가 아니라 절대액 비교가 성립하지 않는다. 근거 JSON에도 caveat로 적었다.
  - 근거 계층을 도시·항로와 같은 3층으로 깔았다: `content/wage-evidence.json`(앵커 2·발견 3) + `tools/check-wages.mjs`(코드 값·파생 배율·판정 형식 검사). 급여만 고치고 근거를 안 고치면 사료 그래프가 옛 배율 위에 새 값을 찍는다.
  - 계측 `dashboard/wages.mjs`는 **Math.random을 시드 RNG로 잠시 갈아 끼워** 같은 세계를 부관 있음/없음으로 두 번 돌린다(`state.officer = null`). officer.md가 기록한 "그냥 두 번 돌리면 부호가 뒤집힌다"를 코드로 막은 것.
  - **재측정이 stale을 잡았다** — 40쌍 실측에서 20항차 판정이 **+1,421닢(24/35승) → −1,220닢(10/40승)**으로 뒤집혀 있었다. 몰타 추가·항로 위험도 개편이 중반 구간을 내려앉힌 것. 초반 3·5항차 값(−178·−322)은 그대로라 초반은 결정적이고 중반이 콘텐츠에 민감하다는 뜻이다. officer.md 표를 정정했다.
  - 판정문(warnbox)을 고정 문구에서 **실측 생성**으로 바꿨다 — "20항차부터 뒤집힌다" 같은 문장은 콘텐츠가 바뀌면 조용히 거짓이 된다.
  - 그리는 김에 공용 결함 하나: `.legend`가 색 견본용 flex라 문장을 넣으면 낱말마다 14px씩 벌어졌다 → `.legend.para`(block) 신설.
- **[2026-08-15] #art #brief #review** — **지도 1차 납품 검수 → 반려.** 실제 지중해를 정확하고 아름답게 그렸는데 게임에는 못 쓴다.
  - **원인은 그림이 아니라 의뢰서였다.** 게임 지도는 실제 투영이 아니라 도식인데(위경도 최소제곱 잔차 **최대 55px**) 1차 의뢰서에 그 경고가 없었다. 그림쟁이는 진짜 지중해를 그렸고, 그래서 도시 15/16곳이 해안선 밖(나폴리·아테네·로도스·알렉산드리아는 바다 한복판, 팔레르모·튀니스·몰타는 사하라 내륙), 항로 25/28개가 육지를 관통했다. 육지 비율 70%(화면 아래 25%가 통짜 사하라).
  - 그 위에 **손실 WebP** — PNG는 48색으로 정확한데 webp가 10,862색으로 번졌다(15% 픽셀이 8단계 이상 차이). 게임이 4배 확대하므로 안티에일리어싱을 다시 넣은 것과 같다.
  - 처방 셋: ① 의뢰서 **§0-A 신설**("좌표를 먼저 놓고 지형을 맞춰라" + 1차 검수 결과표 + 무손실 webp 저장법) ② **기준판 3종**(`assets/map-reference/coord-target.png`·`-4x.png`·`rejected-overlay.png`) ③ **기계 검수** `tools/check-map.py` — 규격·색수·도시 해안선·항로·바다 소음을 판정하고 좌표는 `geo.js`에서 직접 읽는다(의뢰서와 어긋날 일이 없다).
  - 납품물은 `assets/map/`에 보관하되 `manifest.json`에서 비활성화했다 — 붙여 두면 항구 점이 사막에 찍힌다.
