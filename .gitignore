# Claude Code 로컬 설정 (문서는 .claude/docs 아래를 그대로 올린다)
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db
desktop.ini

# 편집기
.vscode/
.idea/
*.swp

# 런타임 산출물
node_modules/
*.log

# 작업 중 임시본 — 여럿이 동시에 일할 때 각자 만드는 진단 스크립트와 캡처
tools/_*.mjs
.playtest/
_probe*
probe_tmp*

# 하네스가 덮어쓰는 캡쳐 — 회차마다 바뀌는 산출물이라 이력에 남길 값이 없다.
# 추적 중이라 매 회차 git status에 떠서 "미커밋이 하나 더 있다"는 오해를 낳았다.
tools/playtest-live/.out/

# 서브에이전트 격리 워크트리
.claude/worktrees/
