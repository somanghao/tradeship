// npc/config.js — 세계에 몇 척이 돌고 얼마나 사납게 구는가 (NPC 담당 영역)
//
// ★ 여기 숫자만 고쳐도 세계의 밀도와 성격이 바뀐다. 코드를 읽을 필요가 없다.
//   행동 방식 자체를 바꾸려면 `js/npc/behavior.js`,
//   생성·시간진행·습격 처리는 `js/world.js`를 본다.
//
// 밸런스를 건드렸으면 반드시 곡선을 함께 본다:
//   node tools/sim-trade.mjs      자산 추이
//   node tools/test-world.mjs     세계가 굴러가는지
//   http://localhost:8891/dashboard/   시세·물동량이 실제로 움직이는지

export const NPC = {
  traders: 9,          // 동시에 도는 상인 수
  pirates: 4,          // 배회하는 해적 수

  /* 같은 구간에서 마주쳤을 때 해적이 덮칠 확률(하루당).
     올리면 항로가 위험해지고 상인 수가 줄어 시세 압력도 함께 줄어든다. */
  raidBase: 0.16,

  /* NPC 거래가 시장에 남기는 몫. 1.0으로 두면 아홉 척이 매일 사고팔아 시세를 계속
     불리하게 만들어 플레이어가 벌 곳이 없어진다(시뮬레이션에서 5~15항차 자산이 바닥을 겼다).
     세계가 살아 있다는 감각은 주되, 시장을 통째로 선점하지는 않게 절반만 남긴다. */
  pressure: 0.5,

  /* 상인이 한 번에 싣는 양 — 화물칸의 이 비율만큼(둘 사이에서 무작위).
     전부 채우게 두면 한 항구가 한 번에 짓눌린다. */
  loadRatio: [0.35, 0.70],

  /* 후보 항로 중 상위 몇 개에서 무작위로 고르는가.
     1로 두면 모든 상인이 같은 최적해로 몰려 한 항구만 계속 짓눌린다. */
  pickTop: 4,

  /* 그 미만이면 아예 싣지 않는다 — 푼돈 거래로 로그를 채우지 않기 위해 */
  minLot: 5,
};

/** 상인이 타고 다니는 배 (data.js SHIPS의 키).
    큰 배를 섞으면 한 번에 나르는 양이 늘어 시세 압력이 세진다. */
export const TRADER_SHIPS = ['caravel', 'fluyt', 'brig', 'carrack'];

/** 해적이 타는 배 — 빠르고 가벼운 쪽 */
export const PIRATE_SHIPS = ['brig', 'caravel'];

export const TRADER_NAMES = [
  '산타 마리아', '레드티', '골든 로즈', '루나 디 마레', '세인트 조지',
  '비앙카', '알 부라크', '스텔라', '메르쿠리오', '포르투나', '아르고',
];

export const PIRATE_NAMES = ['검은 갈매기', '붉은 이빨', '해골 깃발', '살렘의 늑대', '자칼'];

/** 시작 자금 [최소, 최대] */
export const PURSE = {
  trader: [900, 3500],
  pirate: [300, 1200],
};
